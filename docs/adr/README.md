# Architecture Decision Records

One record per architectural decision. Every architectural decision gets an ADR (`project-rules.md` #8; `agent-workflows.md` W6 — an ADR for anything with alternatives considered, written in the same commit as the change).

## Format

Each record mirrors the structure of [`006-ios-first-release-sequencing.md`](006-ios-first-release-sequencing.md):

- **Title** — `# ADR-NNN: <decision>`
- **Bullets** — Status / Date / Deciders / Related
- **Context** — the forces and the alternatives considered
- **Decision** — what was chosen, stated plainly
- **Consequences** — **Positive**, then **Negative / accepted trade-offs** (an optional **Neutral** section for reference points)

Conventions:

- **Numbering:** zero-padded `NNN`, monotonically increasing, never reused. **Numbers are claimed by the earliest-created record, not by merge order** — sessions run concurrently on separate machines/trees, so two can draft the same number (S038 addendum). If you find your number taken, renumber yours and say so in the ADR's bullets. *Worked example:* **029 was drafted as 028**, found PR #95 had claimed 028 four hours earlier, and renumbered — both are in the index below, in number order rather than merge order, which is why 029's own bullets carry a numbering note.
- **One decision per file.** Split, don't bundle.
- **Statuses:** `Accepted` · `Superseded (by-NNN)` · `Deprecated`. Records are immutable once accepted — a reversal is a new ADR that supersedes the old one; the old record's status flips to `Superseded (by-NNN)` and its body stays intact.

ADR-001–005 were backfilled at M0.2 (Session 002) from the `architecture.md` §11 decision log; the decisions themselves date to project genesis (Session 000).

## Index

| ADR | Decision | Status |
|---|---|---|
| [001](001-flutter-over-native.md) | Flutter over a native Swift/Kotlin pair — one codebase for both markets | Accepted |
| [002](002-firebase-over-supabase.md) | Firebase over Supabase — offline + FCM + Remote Config maturity outweighs Postgres ergonomics | Accepted |
| [003](003-revenuecat-for-entitlements.md) | RevenueCat for cross-store entitlements, one purchase covers both partners | Accepted |
| [004](004-eu-firestore-region.md) | EU (europe-west multi-region) Firestore region | Accepted |
| [005](005-couple-scoped-data-model.md) | Couple-scoped data model — no global social graph | Accepted |
| [006](006-ios-first-release-sequencing.md) | iOS-first release & validation sequencing — Android re-sequenced to M6.5 | Accepted |
| [007](007-de-gate-build-from-content-validation.md) | Build de-gated from content validation — gates become spend/launch instruments; personal-use-first | Accepted |
| [008](008-apple-signin-via-credential-seam.md) | Sign in with Apple via a credential seam, not `signInWithProvider` — keeps the flow fakeable and emulator-testable | Accepted |
| [009](009-solo-mode-content-and-persistence.md) | Solo mode — bundled schema-shaped packs, `createdAt`-anchored local day rotation, Firestore `soloAnswers` | Accepted |
| [010](010-content-authoring-home-and-sync.md) | `content/packs/` as the single pack authoring home; validator-owned one-way byte-sync into the app bundle | Accepted |
| [011](011-rollover-pack-source-and-scheduling.md) | Rollover reads build-time-bundled packs; single hourly UTC sweep with timezone buckets; `packConfig {packId}` with `solo_tr` placeholder; evergreen-only selection | Accepted |
| [012](012-reveal-trigger-streak-and-push-policy.md) | Reveal trigger `onDocumentCreated` + transactional `revealedAt` latch; streak with weekly ISO grace refill; push payload policy (quiet hours suppress, discreet default AR-ON, no content in payloads) | Accepted |
| [013](013-revenuecat-webhook-entitlement-mirror.md) | RevenueCat webhook → couple entitlement mirror: verbatim-token auth behind the first env/secret seam (fail-closed), revoke-only-on-EXPIRATION projection, uid→coupleId resolution with the existing-user hard stop, per-subscriber lanes with a `(event_timestamp_ms, id)` LWW total-order guard | Accepted |
| [014](014-paywall-purchases-seam-and-premium-gating.md) | Paywall + purchases seam + premium gating: `PurchasesRepository` speaks the SDK's model types (the fake mints real objects), auth-wired `logIn` with an anonymous-purchase guard, fail-closed unconfigured posture, mirror-is-the-only-unlocker paywall states, one `PremiumGate` widget on `isPremium` with pack selection as the first gated surface | Accepted |
| [015](015-transfer-events-and-the-gift-decision.md) | `TRANSFER` events (a real one is a **400** today — the envelope contract becomes per-type): a transfer never entitles a gainer (the event carries no product/expiry), revoke the loser's lane only on positive evidence the entitlement left the couple, every ambiguity holds; and "gift your partner" is not a store feature — the couple-scoped mirror IS the gift, so no gift UI ships (Family Sharing stays OFF — irreversible) | Accepted |
| [016](016-coach-safety-spine-provider-seam-caps.md) | AI coach v0 safety spine: normalization-based crisis detector (pure, one red lever per fold step), `coachProxy` pipeline with crisis-pre-scan-before-any-rejection, provider-agnostic fail-closed port (fixtures only), transactional caps with a self-read-only daily lane (DV posture), no-text/no-uid log types; the M5.2 wire contract frozen | Accepted |
| [017](017-coach-chat-ui-window-persistence-personas.md) | Coach chat UI: one premium surface with a persona switcher (free tier renders NOTHING — spacer included, free goldens byte-identical), a client-side **help-sticky latch** on the server's `kind:'help'` signal (window retention alone cannot carry the guarantee — the pre-code review's blocking find), ephemeral in-memory threads torn down on sign-out (the private-thread decision — founder owns retention), disclaimer single-homed in ARB behind a new local-flag seam, code-first content-free app taxonomy for the frozen wire, server-side register-aware persona scaffolds with safety lines on the ★ gate | Accepted |
| [018](018-device-privacy-layer.md) | Device-privacy layer: one `PrivacyGuard` in `MaterialApp.builder` gating the WHOLE app (Offstage+TickerMode — which also *pauses* the subtree's Riverpod subscriptions, so gated content is not fetched); the PIN in the **Keychain**, not prefs, because the Firebase session is Keychain-resident and survives reinstall (a prefs lock would evaporate while the session it guards lived); salted SHA-256 with a deliberate **no-KDF** honesty note (10⁶ falls to any offline search — the real controls are the Keychain at rest and attempt-bounding online); biometric is a **revocable accelerator** (`biometricOnly: true` or the device passcode is an app-PIN side door; `evaluatedPolicyDomainState` re-checked every mount, any enrollment change auto-revokes); recovery signs out FIRST and is honestly recorded as destructive-and-detectable, not "never a bypass"; a neutral **brand-free** app-switcher shield (closing ADR-017's deferral); discreet iOS icon bounded honestly (the image changes, `CFBundleDisplayName` cannot) | Accepted |
| [019](019-data-rights-export-cascade-delete.md) | KVKK/PDPL data rights: **the couple is terminal** — A's hard delete removes the entire shared thread, both halves (B's couple-thread answers are mixed personal data about A; the author-split alternative recorded for the founder), B keeps everything that was ever B's alone; one idempotent **resumable** cascade callable (the `deletions/{uid}` cursor is AUTHORITATIVE on re-drive — the rev-2 blocking fix; the detach txn also **seeds the partner's cursor** — rev 3's serious concurrency fix, per-uid cursors alone could not carry the both-partners claim; sweeps confirm-or-throw; own-subtree before invites so the dead profile fail-closes `createInvite`; auth deletion LAST and idempotent via `deleteUsers`), kill-mid-cascade convergence test-pinned per step; partner notified via an atomic server-truth `users/{B}.coupleEnded` field + the in-app terminal notice — **deliberately NO push** (a real-time ping to a possibly-abusive partner at the victim's escape moment; DV review verdict); entitlement "handled" = the mirror dies with the couple (absent doc = free) while **billing continues until cancelled in App Store — said plainly, never claimed otherwise**; export = in-app versioned JSON of strictly the requester's own data (supersedes both "email" parentheticals — no new operator dependency), no premium gate; the `resolveDiscreet` override finally filled (`users.notificationPrivacy` via a settings Function + rules freeze); zero client delete rules loosened | Accepted |
| [020](020-store-identity-and-metadata-posture.md) | Store identity & metadata posture: App Store name **"Hayati"** (provisional until the founder's trademark/availability search — vetted alternates recorded); `CFBundleDisplayName` stays "Hayati App" — the less-identifying-label question is FOUNDER-OWNED (ADR-018 D6 handoff), variants drafted not decided; category Lifestyle, no secondary; the 12-point over-claim blacklist governs all store copy (never "hide the app", never therapy/dating framing, spice never advertised); URL files ship EMPTY behind a loud `--allow-empty-urls` lint flag (no domain/privacy policy exists — operator-recorded, never faked); repo-local `tool/store_metadata_lint.dart` replaces credentialed precheck in the ubuntu quality job; `CFBundleLocalizations` [en,tr,ar] + localized `InfoPlist.strings` close ADR-018 D7's deferral; all copy joins operator item 1's native-review gate | Accepted — **D1 and D2 RESOLVED by the founder** (ADR-032 D6): the store name and `CFBundleDisplayName` are both **İkimiz**, one of D1's own vetted alternates; `name.txt` is now pinned by `tool/release_lane_lint.dart` because `deliver(force: true)` would otherwise rename the live listing, and D2's required re-audit of the discreet-icon copy was performed (no change needed) |
| [021](021-release-lane-fail-closed-signing-boundary.md) | The release lane: tag `v*.*.*` + `workflow_dispatch` triggers (rev 2: a first-time workflow registers only on the default branch — the acceptance dispatch runs post-merge as `--ref main`, recorded not reinterpreted); `preflight` (metadata lint + tag↔pubspec pin) → `integration` (existing emulator suites, ci.yml recipe) ∥ `build-report` (release --analyze-size + size budget) → `sign-upload` with **`environment: release`** (rev 2 — environment secrets are invisible without the binding) whose FIRST step is an all-or-nothing **secrets gate that fails CLOSED and loud** (names missing secrets, points at operator item 4; no `if:`-skip that renders green); App Store Connect **API-key cloud signing** (3 secrets, .p8 via xcodebuild auto-discovery — `flutter build ipa` already passes `-allowProvisioningUpdates`); version truth = tag must equal pubspec `X.Y.Z`, build number = pubspec `+N`, no CI auto-increment; E2E matrix deferral written into test-suite.md (E2E-2 impossible before items 0+4); Gemfile.lock debt stands until the signing job first runs; fastlane runtime artifacts gitignored | Accepted — **D5 superseded by [032](032-release-signing-on-fastlane-match.md)** (cloud signing → fastlane `match`), and **D3's build-number clause superseded by 032** (CI synthesizes it); D3's tag↔pubspec clause stands and was restored by 032 |
| [022](022-cold-start-posture-and-bootstrap-deferrals.md) | Cold-start posture: the PRD's <2s is a mid-range-Android M6.5 gate — the iOS pass instruments honestly and **refuses the fake CI assertion**; rev 2: only the tz-DB parse defers post-frame (the rev-1 App Check deferral DELETED — review finding PERF-1: warm signed-in boots open the profile Firestore listen at first-frame build, zero interaction — App Check stays pre-frame overlapped with Crashlytics), prefs ∥ Keychain reads overlap via `Future.wait`, `configureIfKeyed` deliberately does NOT move (warm-boot identity-sync landmine — first live-key session owns it, trap documented at the call site); the pre-frame await set (all FOUR awaits, incl. `configureIfKeyed` — rev 2 fix of PERF-2) is PINNED by a mutation-checked source-sentinel test; build size gets an exact number + 200 MB pathology cap via `tool/build_size_report.dart` in the release lane; startup timings are CI diagnostics (`BootTrace`, kReleaseMode-stripped), the real numbers ride M6.5 + operator item 4's stopwatch checklist; crash-free posture audited (M1.3 mechanism confirmed), not rebuilt | Accepted |
| [023](023-consent-surface-and-legal-bundle.md) | Consent surface & legal bundle: **exactly one explicit consent** (the special-category-conservative reading of free-text reflections + coach + transient crisis detection — three lawyer questions recorded: classification, conditioning, post-withdrawal retention), never a toggle farm and never a consent wrapper on the contract-basis core; the aydınlatma is a **notice, not a checkbox**, riding `ProviderActions` itself so every sign-in surface (incl. the cold-open invitee) carries it by construction; consent state is **server-owned `users.consent {version, acceptedAt, ageAttested}`** via a `recordConsent` callable that **stamps its own version** (no client-claimed version = no bump brick), under the M6.2 create-forbid+freeze invariant, a **three-way version source-sentinel** (app const == functions const == docs version), and a **rules consent predicate on the special-category answer writes** (presence-only, version-blind — bound recorded); the gate exposes **export + delete + sign-out** so a decliner keeps every right; withdrawal is prospective by DV doctrine (stored corpus survives until deliberate deletion — lawyer question C); `docs/legal/` byte-synced to bundled assets under a drift test, rendered dependency-free through an injected-bundle seam; DPA inventory with honest per-service region cells + the **KVKK SCC + 5-day Kurum filing** and **Kurul adequate-measures** founder/lawyer items; privacy manifest deferred to the submission cluster as scope creep (issue-tracked) | Accepted |
| [024](024-ci-slack-notifications.md) | CI → Slack (**rev 2**): ONE tested notifier script (`tool/ci/slack_notify.sh` + 14 self-tests & shellcheck in `quality`) instead of the reference's four drifting copy-pastes; **all policy lives in the script, never in a YAML `if:`** — a policy the self-test cannot see is a policy protected by nothing; a noise policy that keeps the channel un-muted (failures always; successes only on main/dispatch/release — PR successes silent, because the session already watches PR checks and the post-merge `integration-emulator` red is the one CI event in this repo with NO reader); absent secret = clean quiet SKIP, never a warning annotation and never a red — the notifier has **no vote on the build**, enforced at TWO levels (`exit 0` inside the script + `continue-on-error: true` on the job, which covers the checkout/runner failures the script cannot reach), with the **W4 exception recorded and bounded** (D3.1); **D8 — push events key concurrency on the COMMIT**, because ref-keying meant the session's own close commit cancelled the post-merge run carrying `integration-emulator` and the superseding docs-only run skipped it (the ADR's premise was false without this); repository-scoped secret, NOT `environment: release` (the S022 invisible-secret lesson, running the other way); classic Incoming Webhook `{"text":…}` contract; no log excerpts ever; **D9 — a never-pushed local branch hardcodes a LIVE webhook in plaintext: superseded, and rotation is an operator action**; and the honest bound — the mechanism ships, the live proof is operator-gated (founder mints the webhook) | Accepted |
| [025](025-uipro-refactor-scope-and-invariant-firewall.md) | The UI/UX Pro Max refactor, scoped: the skill was **actually run** and its own prescribed workflow is **rejected** (`SKILL.md:346` hardcodes the wrong stack; the REQUIRED `--design-system` step proposes a *light pink* palette, Noto-as-primary, an App-Store-landing pattern and a `MASTER.md` calling itself "Global Source of Truth" — four direct contradictions of brandkit v1.0, which stays the constitution); only the corpus's App-UI checklist is adopted, transcribed into the ADR so the arc does not depend on a tool whose corpus can change under it (`init` has no version pin); a **48-surface inventory** classifies every surface F/G/N/S and yields the twelve-item prohibition list; the two mechanism gaps the inventory found are the arc's first work — **ADR-018 D3's no-dialog lock constraint has no source sentinel** (a comment + one recovery-path test) and **brandkit→Dart tokens are a hand-transcription with no drift test**; the refactor thesis is evidence-based (feature code is already token-clean — 0 hardcoded styles — but `hayatiTheme` sets only 6 sub-themes, so `AlertDialog`/`Card`/`BottomSheet` fall back to `surface` and render *flat `night`* while `SnackBar` renders on **`sand`**); eight slices after a mandatory pixel-free **slice 0** that builds the firewall, lock surfaces last and parity-only, and a goldens rule that makes W4 checkable — **declare the expected golden set before regenerating; anything outside it is a defect, not churn** | Accepted |
| [026](026-seasonal-question-windows.md) | Seasonal question windows: the window predicate keys off the day doc's own **dayKey**, never `now`, so overlapping sweeps agree on the verdict; Hijri via ICU **islamic-umalqura** over UTC-noon (zero dependencies — the same `Intl` primitive `day-key.ts` trusts) behind a **loud availability guard**, because an unsupported calendar does not throw — `Intl` silently resolves to **gregory**, which would fire ramadan every September forever with nothing red, so the module refuses to answer and every Hijri window reports CLOSED (fail-closed on content, fail-open on availability: evergreen selection keeps running and the sweep logs it once + surfaces `seasonalCalendarUnavailable`); the vocabulary is **CLOSED** (`ramadan`/`eid_fitr`/`eid_adha`/`new_year` — `eid` deliberately dropped: two feasts two months apart) and gated in **five** readers incl. `validateSchemaAgreement`'s drift check, because a free-string tag nothing recognises is a question never selected, silently, forever; selection prefers in-window seasonal **UNSEEN-first** — always-preferring would loop five Ramadan questions for thirty days while the curriculum sat idle — then evergreen, then in-window recycle; Umm al-Qura's ±1-day divergence from local observance is recorded and deliberately **not padded** (padding `eid_fitr` backwards would greet Eid on the last fasting day); server-only by design (ADR-011's contemplated client prefetch was never built — a Dart mirror would need its own Umm al-Qura source and parity fixture, priced as a follow-up) | Accepted |
| [027](027-bundle-id-rename-hayati-app-squatted.md) | iOS bundle-id rename `com.hayati.app` → **`com.beyondkaira.hayati`** — the working-title id proved **squatted on Apple** (refused to team `UH7MXG7Z94`, the founder's only account; same pattern as the `hayati-dev` GCP squat), and the bundle id is not user-facing so the rename is cheap (unlike the store name, ADR-020); a **single** id still serves both flavors (no per-flavor split); the deterministic half (pbxproj + `Appfile` + Info.plist invite name + integration fixtures) lands now, the **Firebase-minted half regenerated in the SAME merge** (`firebase_options_{dev,prod}` iOS block, `google_sign_in_config` iOS client ids, `REVERSED_CLIENT_ID` schemes, the bootstrap-test expectation) because the founder had already registered the new iOS app in both projects — the planned two-phase split never existed in a commit (rev note, ADR-029 D5); the parked `DEVELOPMENT_TEAM` question is resolved by **ADR-029 D1**; **Android stays `com.hayati.app`, its id decision deferred to M6.5** (ADR-006 — Play uniqueness is separate and permanent, decided with the Play Console open) | Accepted |
| [028](028-live-coach-adapter-anthropic-and-reconsent.md) | The live coach adapter (M5.3) + the re-consent it triggers: provider is **Anthropic (Claude API) direct** — not an aggregator/router (a router is a second processor in a special-category data path, harder KVKK/PDPL, silent-failover risk); model **Sonnet 5** with thinking **disabled** (brief warm reply, safety lives in the crisis detector, cost predictable); `AnthropicCoachProvider` honors the frozen port's two obligations **by construction** — reads `LLM_API_KEY` at request time (absent → fail-closed `unconfigured`, deploy-safe), and **never copies upstream text into an error** (`classifyUpstream` branches on type only; a `refusal`/empty completion is an outage, not a crisis; the handler post-filter still runs); `coachProxy` declares `secrets: ['LLM_API_KEY']` (fails closed + loud until the secret exists). The **re-consent** (ADR-023's recorded M5.3 trigger): bump `CURRENT_LEGAL_VERSION` **1→2** across all three sentinel sources in one diff, name Anthropic in the six legal docs + the in-app `consentProcessors` notice (frozen-sentence digest re-stamped deliberately) + `app/assets/legal/` byte-sync, re-gate every user — done at **zero user cost** (pre-launch); the DPA inventory's placeholder LLM row becomes the active Anthropic row with a KVKK SCC leg + PDPL TRA leg (founder/lawyer). Honest bound: the notice claims *we* store nothing and Anthropic doesn't train — not "Anthropic retains nothing" (ZDR opt-in strengthens it) | Accepted |
| [029](029-release-lane-signing-enablement.md) | Release-lane signing enablement: **`DEVELOPMENT_TEAM = UH7MXG7Z94` is COMMITTED** — a Team ID is an identifier, not a credential (it is published in every distributed IPA's embedded profile, already sat in four repo docs, grants nothing without the private key, and withholding it would have cost a FOURTH operator secret to hide a public string), narrowing only the `Appfile`'s over-broad comment while `architecture.md` §9's "zero keys in repo" stands untouched; `CODE_SIGN_STYLE = Automatic` pinned **explicitly** though it is the default, because ADR-021 D5's whole cloud-signing mechanism is INERT under Manual and one Xcode click writes it; both defended by a **per-block** pbxproj source-sentinel that deliberately REJECTS the mold's global-count mechanism (the blocking review find: `CODE_SIGN_STYLE = Automatic` already occurred 3× in RunnerTests and 0× in Runner, so a global count would be green when broken, red when correct, and green when re-broken) with an 11-row mutation matrix whose two must-stay-GREEN rows are what prove the scope is real; the fresh-runner distribution-certificate cap **recorded not pre-solved** (observable on the first run, unknowable from Linux); and four stale claims about ADR-027's own diff corrected — including the founder-facing one the review caught this ADR leaving behind | Accepted — **D2's rationale amended and D4 closed by [032](032-release-signing-on-fastlane-match.md)**: the pin stands but now defends the dev box rather than a cloud-signing mechanism the lane deliberately overwrites, and the certificate-cap risk is gone because `match` runs readonly |
| [030](030-functions-runtime-node-22.md) | The Functions runtime moves **Node 20 → 22**, closing dated issue #96 (`nodejs20` is **decommissioned 2026-10-30**, after which deploys simply fail) and clearing it off the path to the first prod deploy rather than leaving it on it; **22 not 24 because firebase-tools' own lifecycle table shows both decommission 2028-10-31** — 24 buys only a quieter 2027, no extra runway — and 22 is the major the dev box can actually run, so the suite was validated on the real target instead of pushed to CI and hoped for; **the deliverable is the ICU RE-VERIFICATION, not the version string**, because ADR-026's whole premise is that `Intl` resolves an unsupported calendar to `gregory` SILENTLY and a runtime upgrade is exactly the event that moves ICU underneath the product — verified `islamic-umalqura` resolves on ICU 78 full-ICU with a real Hijri date, the 48 seasonal-window tests (incl. the degradation test, whose mechanism needed re-checking because a changed property descriptor would have silently disarmed it), and both halves of the byte-pinned day-key fixture; scope held to the runtime + its types with `firebase-admin ^13`'s now-void reason CORRECTED but its upgrade deferred to #107; and every one of the seven surfaces that merely NAMED Node 20 corrected too, only three of which enforced anything | Accepted |
| [031](031-firebase-admin-14.md) | `firebase-admin` **v13 → v14**, closing #107 — the `^13` pin existed only because *v14 requires Node ≥22*, the constraint ADR-030 lifted; **the honest headline is `@google-cloud/firestore` v7 → v8** (plus google-gax 4→5, gaxios 6→7), i.e. a **Firestore CLIENT major on the very library ADR-019's resumable cascade runs its transactions and cursors through** — a reviewer reading "firebase-admin bump" would mis-price it, so the ADR is written for the reader who prices it right; `tsc` cannot see a changed retry policy or cursor semantic, so the deliverable is the **verification**: the cascade suite against the REAL emulator (58 — the resumable cursor, the partner-cursor seeding txn, kill-mid-cascade convergence, `deleteUsers` idempotency) and the entitlement LWW order-independence property (109), plus 979/50 overall; `firebase-functions` v7 deliberately NOT bundled for the third time (one major per change, so a bisect is one step); bounds recorded — emulator is not a production soak, and the suites cover the paths ADR-019/013 chose to pin | Accepted |
| [032](032-release-signing-on-fastlane-match.md) | Release signing on fastlane **`match`**: written AFTER THE FACT for PR #117 and saying so — cloud signing cannot work on a hosted runner (no Xcode Apple ID, empty keychain: *"No valid code signing certificates were found"* — it can fetch a profile, never conjure the certificate's private key), so the Apple Distribution cert **and its private key** now live encrypted in a **separate private repo** and the build signs MANUALLY; **`architecture.md` §9's "zero keys in repo" is restated honestly — custody MOVED, it did not vanish**, and two composable secrets equal the ability to sign as this team; `match(readonly:)` closes **#99** by removing the cert-minting mechanism rather than mitigating it; version truth **splits** — the build NAME returns to pubspec (a hardcoded `--build-name=0.1.0` had made preflight's tag↔pubspec gate guard a string the build ignored) while the build NUMBER stays CI-synthesized, superseding ADR-021 D3's "no auto-increment magic" on the evidence of **six consecutive failed release runs** each needing a fresh number, with the lost idempotency and the pubspec-`+4`-vs-shipped-**109** trap both written down; the fail-closed gate is re-pointed at the five secrets the job actually consumes (ADR-021 D4's promise had silently narrowed to the retired mechanism's three) and the orphaned `.p8` step is KEPT on ADR-029's own no-blind-edits-to-signing-from-Linux precedent while its redundant second secret goes; **`store_metadata` gets its own narrower credential check** — routed through the match-aware helper it aborted before `deliver` on EVERY release inside a `continue-on-error` step that reported success, so store metadata had **never once been delivered** (the S042 addendum-25 shape: a bound justified by one caller reused on another); the store name becomes **İkimiz** in the SAME diff, because fixing the credential bug alone would have let `deliver(force: true)` rename the founder's live listing back; and the whole lane's self-agreement becomes a per-PR ubuntu lint with 50 mutants, since `release.yml` runs only on tags and could otherwise drift with every required check green | Accepted |
| [033](033-bidi-isolation-at-the-content-text-seam.md) | Bidi isolation at the **string boundary** (FSI/PDI), not the widget seam — a bidi-neutral terminator at the edge of an LTR run inside an RTL paragraph takes the *paragraph* direction, and `app_ar.arb` interpolates a partner name into the MIDDLE of an Arabic sentence, which a `Directionality` ancestor structurally cannot isolate; applied at **render only** (nothing persisted, exported or shared carries `U+2068`/`U+2069`), to **content** and deliberately NOT chrome, and emitted only when the directions actually differ — the unconditional version churned 27 LTR goldens by splitting the shaping run, which ADR-025 D8 calls churn to fix rather than declare; the **mirror case** (Arabic content in LTR chrome) was already covered by committed goldens the ADR had claimed did not exist; `intl`'s `Bidi.detectRtlDirectionality` rejected as a majority heuristic that disagrees with FSI in both directions, and its first-strong ranges miss Arabic Extended-A **silently** → `// DEBT:` + **#137** | Accepted |
| [034](034-dependency-advisory-posture.md) | Advisories are gated on what a change **INTRODUCES**, not on what the tree contains: `npm audit`'s absolute output moves for reasons **no commit caused**, so a `--audit-level` gate reddens `main` for a third-party publication nobody can fix that hour — ADR-024's lesson, applied. `tool/ci/npm_audit_delta.py` audits the **base and head lockfiles in the same run**, so a publication event appears on both sides and **cancels**, making the check structurally unable to cry wolf; keyed on **GHSA id** because npm's "12 vulnerabilities" here is **2 advisories + 10 wrappers** (issue #131's own title counts wrappers); **fail-closed** on its own failure, with the no-base-commit skip named as a real hole. Deliberately NOT built: a **baseline file** (ADR-025 D8's shape — a declaration nothing enforces), a **cron** (GitHub disables scheduled workflows after 60 days of inactivity, i.e. during exactly the quiet period it would exist to watch), or **Slack routing** (the notifier's payload has no field for advisory content and D2's noise policy would suppress it). `npm audit fix` cleared **2 of 3** distinct high advisories, not 7 of 7; the remaining high is **unfixable** — npm says `fixAvailable: true` but `brace-expansion@5.0.8`'s CJS entry exports an object, so the override breaks `minimatch` at runtime (measured with a passing control) — and hangs off a `rimraf` **google-gax declares and never imports**; the `firebase-admin@10.3.0` downgrade is refused (ADR-030/031) and the working `uuid` override declined as unreachable three ways. The half a cron would have covered is **Dependabot**, which is **disabled** on this public repo → operator action | Accepted |
| [035](035-rename-to-ikimiz.md) | The rename **Hayati → ikimiz**, and why it is a copy rewrite rather than a substitution: (a) Turkish suffixes change because the stem now ends in a CONSONANT — `Hayati'yi`→**`ikimiz'i`** not `ikimiz'yi`, `Hayati'nin`→**`ikimiz'in`**; (b) **"ikimiz" is an ordinary Turkish word ("the two of us")**, so four sentences collided semantically and were REWRITTEN — "ikimiz ikinizin arasında kalsın" mixes *our two* with *your two*, "ikimiz iki kişi için tasarlandı" reads "the two of us were designed for two people" — and where the brand could not sit without ambiguity it was dropped from that sentence; (c) the brand becomes a **Latin wordmark inside RTL chrome**, which would have falsified **ADR-033 D3's** premise that a chrome sentence's first-strong direction already equals the paragraph's, so every Arabic string is restructured to LEAD with an Arabic word (`تطبيق ikimiz`) rather than adding isolation controls. **The legal version is deliberately NOT bumped** — processors, data categories, purposes and controller are unchanged, only the trade name moved, so the frozen-sentence digest is re-stamped with that reasoning recorded next to the constant and nobody is re-gated. Goldens **declared before regenerating** (ADR-025 D8): 91 modified / 0 added / 0 deleted across 7 suites, matched one-for-one. Bundle ids, Firebase project ids and every internal symbol are deliberately untouched | Accepted |
| [036](036-ikimiz-domain-and-universal-links.md) | **`ikimiz.beyondkaira.com`** on Firebase Hosting (no VPS, no nginx — the project is already all Firebase). The pages are **GENERATED from `docs/legal/` at deploy time and never committed**: that text is already byte-synced into `app/assets/legal/` under a drift test (ADR-023), and a committed HTML copy would be a THIRD thing to drift. The Markdown subset is only what the corpus measurably uses, an unrecognised line becomes a paragraph so legal prose is never silently dropped, and **link syntax is deliberately unimplemented** because the corpus has zero links but does contain `[FOUNDER LEGAL ENTITY — to be completed by the founder]`, which a link parser would hide inside an anchor. A **placeholder gate refuses to publish an unfinished policy** (both directions tested; the live channel refuses the escape flag outright). Invite links move from the custom scheme `hayati://invite/<code>` — unclickable in WhatsApp, inert without the app — to **`https://ikimiz.beyondkaira.com/i/<code>`** with an AASA file and an associated-domains entitlement; **the old scheme is still parsed** because links already sent live in chat history forever, and `inviteLinkFor()` becomes the single constructor with a round-trip test. Deploy is **dispatch-only** — merging is not consent to publish. Closes **ADR-020 D5's** empty-URL ratchet in the same diff | Accepted |
| [037](037-testflight-friends-auto-assignment.md) | Every release build is attached to the external **Friends** group automatically — a **named exception** to S052's rule that a merge must never email a real person, because the founder asked for exactly that; creating testers stays dispatch-only, since that is what emails a NEW person. Deliberately **not** `pilot(distribute_external: true)`, which would need `skip_waiting_for_build_processing: false` and park the expensive macOS runner in Apple's queue on every release — the upload stays fast and a new `--assign-build-number` polls on cheap ubuntu until Apple reports the build **VALID**, because a build still `PROCESSING` has no installable asset and attaching it would report success while delivering nothing (the `store_metadata`/#140 shape, met a third time). **Non-blocking**: a slow Apple queue must not redden a release whose binary already shipped (ADR-034's reasoning), so a timeout prints how to re-run instead of failing. Honest bound stated rather than papered over: external testers still receive **nothing** until the founder fills the Test Information page and Beta App Review passes — the tool names the missing fields after every assignment | Accepted |
| [038](038-testflight-beta-review-readiness.md) | **Test Information is writable, so a session writes it.** Measured: a VALID build (110) had sat in TestFlight since 2026-07-27 and the ONLY gap to five external testers was four empty `betaAppReviewDetail` contact fields — a name, an email, a phone. The tool's own docstring called those gaps "founder-owned copy that no session can write for them"; half right, and the wrong half is the expensive one, because the FACTS are the founder's while the WRITING is a PATCH on an API CI already authenticates against. The four values arrive as **`release`-environment SECRETS, never as `workflow_dispatch` inputs** — this repository is PUBLIC and dispatch inputs are recorded in world-readable run metadata, so an input box would publish the founder's mobile number permanently; the tool prints field NAMES and `set`/`unchanged`/`missing`, never a value, and that guarantee **gets a sentinel test** (ADR-024's `assert_no_leak` precedent, the S020 rule the design review caught this ADR asserting without). Submission is **dispatch-only**, refuses while `review_readiness()` reports any gap (an avoidable rejection is recorded against the app forever), and de-duplicates by **reading `externalBuildState` first** rather than on a status code — the first draft claimed "Apple returns a conflict", which the review found unmeasured with 409 and 422 both claimed in the wild, so the error path survives only as a race backstop demanding an error family **and** a phrase. The state enum is **open**: printed verbatim, with only two fail-safe subsets named in code (already-through-the-gate → skip; the two export-compliance states → refuse and say which). `--status` finally prints Apple's **reviewer** (`externalBuildState`) and per-build group membership instead of only its **encoder** (`processingState`) — a build can read VALID forever and reach nobody. `release.yml` writes the contact on every release but **never submits**, and the write is deliberately **non-fatal**: wiring it revealed that failing closed there would abort before the build assignment and silently repeal ADR-037's guarantee while `continue-on-error` kept the release green | Accepted |
| [039](039-fail-open-boot-bounded-waits-and-a-reachable-invite-host.md) | **The boot always ends in a frame, every wait is bounded, and the invite link points at a host that answers.** The founder's *"loading screen is always on"* had no single defect behind it — it had a CLASS: every blocking wait from launch to paired was unbounded and every blocking screen was a dead end, so any of them could produce that report and none could produce an actionable one. (1) A throw in any of ADR-022's four pre-frame awaits escaped `main()`, so `runApp` was never called and iOS held the **launch storyboard forever** — no frame, no error, and no crash report, because the reporter is itself one of the four; the three steps with a real degraded mode now fail open where they are (App Check is a *server-side* enforcement signal, so a client blocking its own boot on it protects nobody; Crashlytics downgrades to a no-op that still **presents** errors, since a silent one would blind the console on exactly the boots that went wrong) and the remainder renders `BootFailureApp` with the raw throwable behind an expander — **ADR-022's sentinel is untouched and still passes, because a `try` block adds no `await`**. (2) `SlowLoadEscape` keeps the bare spinner 8s then reveals an escape *without removing the spinner* (the load has not failed); it exists for `OnboardingGate`, where a Firestore **document** listener on an uncached doc raises **no event at all** — not an empty snapshot, not an error — until the server answers, so a first sign-in that could not reach Firestore sat behind a spinner with no retry, no error and no sign-out; it now offers retry **and** sign-out. (3) Interactive sign-in bounded at 2min (the clock covers a *human* typing an Apple ID password), typed **network** so the copy is actionable, and the timeout **releases the manual-op gate** rather than cancelling — so a late success lands from the now-unsuppressed stream with no second tap. (4) The zero-auth preview GET, the one request with no deadline anywhere (`package:http` has none; callables get one from the SDK), bounded at 15s — a **stalled** connection raises neither SocketException nor ClientException. (5) **Measured: every invite link the app has ever emitted lands on a browser certificate error** (`ikimiz.beyondkaira.com` still resolves to the founder's VPS) and the AASA is served nowhere, so links are now built on **`ikimiz.web.app`** — same Hosting site, Google-issued TLS, no DNS record needed; the custom domain stays a first-class *parsed* host so nothing already sent breaks and the cutover is one boolean, and the parsed set stays **closed** because any site can serve `/i/<code>`. (6) `--invite-only` is **not an exception to the placeholder gate** — it publishes no legal document at all, so the rule holds by construction — and it is deliberately **not** channel-gated, because going live with invites while the legal blanks are open is the point; the invite page's Get-the-app button is conditional on the App Store id being real, since a dead button on the one page an invitee sees is worse than none. (7) `hayatiTheme` memoized: `MaterialApp.builder` sits **above the Navigator**, so every route push handed `Theme` a new instance and rebuilt every `Theme.of` dependent in the app. Deliberately NOT done: the App Attest entitlement (prod activates App Attest with no entitlement — real, but `match` is readonly, so a wrong guess makes signing the next mystery; D1 renders it harmless and it is an operator item), the `PrivacyGuard` shield (audited; a plain dark fill is not a loading screen), and any deploy | Accepted |
| [040](040-ship-without-universal-links-rather-than-block-on-a-portal-capability.md) | **A working build today beats a perfect link nobody can install.** `Friends` was attached to **build 110**, and build 110 came from `fa990e6` — *before* ADR-036 and ADR-039 — so the five people the founder wants testing would install the exact build that produced both bug reports: the permanent loading screen, and an unclickable `hayati://` invite link. Cutting a fixed build ran into one line: ADR-036's `com.apple.developer.associated-domains` must also be in the **provisioning profile**, the capability must be enabled on the App ID in Apple's portal, and `match` fetches profiles **readonly** by design (ADR-032) so CI cannot add it. **Measured: no build has ever been signed with that entitlement**, so the capability has never been exercised and the failure mode is a codesign-time mismatch that aborts the release. The entitlement is therefore **removed**, and the invite loop still closes end to end — the sender shares a real https link (tappable everywhere the custom scheme was not), it opens the **live, verified** invite page which shows the code with a copy button, and the invitee enters it via "Have a code?" → `PartnerPreviewScreen`, a path that already existed and was already tested. The cost is stated rather than minimised: **one extra step, for the invitee only**. This does **not** repeal ADR-036, whose load-bearing claim was that a link be clickable in a chat app and land on something real — an https link that opens a working page is still both; universal links are the polish that removes the last step. Kept deliberately: the app still **parses** all three hosts (so links sent during this window open the app the moment a claiming build is installed), the **AASA stays served and is already correct** (Apple fetches it at install time, so having it live *before* the claiming build is the right order), `kInviteLinkUsesCustomDomain` and the closed host set, and the `hayati://` scheme. Restoration is additive — ordered steps live in `Runner.entitlements` itself, where someone will be standing when the question occurs to them. Deliberately NOT done: leaving the entitlement in on a hope, which on failure converts "an unvisited portal page" into "the release is broken and nobody knows why" — the ADR-039 class of mystery, and exactly what ADR-032's readonly rule exists to prevent | Accepted |
| [041](041-deployed-versus-merged-firestore-rules.md) | **A check that guards the working tree is not a check on production.** `functions-rules` proves `firestore.rules` against an emulator that loads the WORKING-TREE file, so it was honestly green for 18 days while both projects served the 07-09 M2.1 ruleset and six milestones of rules sat merged and undeployed — found by a founder bug report, not a gate. `tool/ci/rules_drift.py` asks `firebaserules.googleapis.com` what is RELEASED and diffs it byte-exactly against the ref; **no committed `last-deployed.sha` marker** (ADR-025 D8's shape, and it would fail reassuringly because the lane whose omission IS the bug would be the one updating it). Exit codes are a **taxonomy** — 0 match / 1 drift / **2 could not measure**, never 0 without comparing — and coverage is asserted, not assumed: a named database's `cloud.firestore/{db}` release fails **closed** rather than yielding a partial green. **ADR-034's asymmetry does NOT transfer** (an advisory is a third party's act; drift is our own omission and always actionable), so the check **votes** — but post-merge on **`main` only**, never per-PR, and on **every** push rather than only rules-touching ones, because the narrower trigger goes green while production is still stale. **The green-without-measuring hole is closed structurally** (D6): a job-level `if:` cannot read `secrets` and an all-steps-skipped job reports GREEN, so the credential probe is its own job and `rules-drift` is either MEASURED or **visibly SKIPPED** — no third outcome. `deploy-rules.yml` is **dispatch-only** (rules are the authorization boundary; merging is not consent), **prod requires typing the project id**, and it measures → deploys → **reads back**. Credentials split by privilege: read-only `FIREBASE_RULES_VIEWER_SA` for the watcher (so it can never cause the drift it reports; `firebase.readonly` verified against the API discovery doc), admin for the deployer. Proven on the live platform in both directions, incl. exit 1 against the **actual M2.1 bytes** from `d913722`, and the deploy exercised end to end on dev; the job's measuring path is **unarmed until one operator secret exists**, which is said plainly rather than claimed | Accepted — closes **#140**, residual re-filed rather than buried (D8) |
| [042](042-push-token-lifecycle-and-the-two-hours-the-founder-asked-for.md) | **The feature is ticked ✅ and has never once run.** M3.4 shipped three push kinds, an injectable `MessagingPort`, quiet hours, discreet mode and 35 tests — all real, all correct — over a delivery path that does not exist: no `firebase_messaging`, no `aps-environment`, no `UIBackgroundModes`, no APNs delegate methods in `AppDelegate.swift` (**unwritten, not deferred**), and **no writer of `users.fcmTokens` anywhere**, so every send is a counted `skippedNoToken` and nobody has ever received a notification (lesson **79**). **Amends ADR-012 D3** in four places — the kind vocabulary (3→4), the token-storage deferral (discharged), the sweep-pass inventory (2→4) and the at-risk eligibility rule — while **preserving its one hard constraint, ONE couples read per sweep, by construction**: the hour-8 pass iterates the same `CoupleBuckets` the handler already threads into both existing passes, so a third pass is one more argument, not a new machine. **D1 — `fcmTokens` becomes server-owned in FACT.** It is client-writable today at create *and* update while three separate places call it server-owned in comments; two callables (`registerPushToken` / `unregisterPushToken`, on the `recordConsent` shape: `requireUid` → validate → `{kind:'ok'|'profile-missing'}` → `update()` never `set(merge)`) plus the field joining **both** freeze clauses. The decisive argument is neither bounding nor the create-mint gap but that **a token is device-scoped and a user is not**: if A signs out and B signs in on the same phone, B's registration must EVICT that token from A's document, which is a cross-document write only the admin SDK can do — so **registration is authoritative and sign-out cleanup is best-effort**, and the privacy property survives a cleanup that never ran, because a defence that depends on the departing client behaving is not a defence. **D2 — the entitlement lands LAST, behind a measured fact.** `match` is readonly (ADR-032) so a build claiming an entitlement its profile lacks fails at codesign — ADR-040's exact history, one capability over — and **CI cannot see it coming**, because `ios-build-smoke` runs `--no-codesign`, so it detonates only in the macOS release job. The portal capability is now *measurable* rather than founder-reported (`appid_capabilities.py`, ADR-041's 0/1/**2-could-not-measure** taxonomy, because a key lacking the Certificates/Identifiers role answers 403 and calling that "absent" invents a blocker); the app half ships behind a `PushTokenSource` port — the device-side twin of `MessagingPort` — so step 4 is an adapter, not a rewrite, and the whole token lifecycle stays provable on Linux with no plugin, no Mac and no APNs key. **D3** adds a `dailyQuestion` kind at couple-local hour 8 — *"08:00 TSİ" read as couple-local*, since a fixed `Europe/Istanbul` constant is right for exactly one couple and silently wrong for the Gulf audience — sitting **exactly on the right-open quiet-hours boundary**, which is elegant and fragile enough that an off-by-one either way suppresses the entire feature with every test green, so it is mutation-checked in both directions. **D4 — the 16:00 nudge REPLACES the 20:00 one rather than joining it**: today's push protects a *streak* (`streak.count > 0`), the founder's protects a *relationship* and must fire for a couple with no streak at all, and since the 16:00 population is a strict superset, re-pointing loses no couple, gains every couple without a streak, and keeps the nudge count at one per day — which is the property that decides helpful from nagging. ADR-012 §10's cost claim is **amended rather than quietly widened** (the day-doc read goes from per-couple-with-a-streak to per-couple). **D5** clears tokens inside the cascade's existing `users/{uid}` delete — no new step, no cursor change — and ADR-019's **no-push** DV invariant, until now untestable, gains an assertion that the port receives nothing during a deletion. **D6** keeps the permission prompt off the boot path entirely (ADR-039: an indefinite wait on a human, with one shot per install), capturing tokens through the `purchasesIdentitySync` initial-read-then-listen pattern. Four consequences recorded now because each is otherwise someone's later surprise: the PIN store **already predicted** background launch breaking its `unlocked_this_device` Keychain read (SEC-3, so `UIBackgroundModes` must not be added before that is decided); tokens are project-scoped while the bundle id, entitlements file and Xcode scheme are shared across flavors; a notification tap is a **deep link into locked content** and ADR-018 must gate it; and **#136 stops being latent the moment a push lands**, on a server-composed/OS-rendered seam ADR-033 does not currently answer for | Accepted |
| [043](043-deployed-versus-merged-cloud-functions.md) | **The same blind spot as ADR-041, one deploy target over.** `firebase-functions-hash` turned out to be fully derivable from a checkout, so `tool/ci/functions_drift.py` reproduces all 13 prod hashes exactly and names a deployed function that is not this ref — the gap that let every one of the 13 drift unnoticed while CI was green. | Accepted |
| [044](044-apns-readiness-and-the-one-shot-token-capture.md) | **An adapter that violates its port's contract and documents the violation is a defect with a comment on it.** iOS cannot mint an FCM token until APNs answers, and that handoff completes AFTER `requestPermission()` returns — so the single `getToken()` call was issued inside the exact window iOS guarantees it fails, caught, logged, and never retried. D1 asks the platform whether it is ready and keeps that decision ABOVE the port; D2 makes the retry bounded (ADR-039 D2) and off the critical path. | Accepted |
| [045](045-notification-hours-and-the-quiet-window-that-would-have-eaten-them.md) | **The founder's hours — question 09:00, unanswered nudge 22:00 — and the quiet window that would have silently swallowed the second one.** Only the ANNOUNCEMENT moves to 09:00 (rollover still assigns at local midnight, because `dayKey`, the streak and the reveal are all keyed on it); the quiet window HAD to move 22:00 → 23:00 or our own guard would have eaten the nudge the founder asked for. | Accepted |
| [046](046-the-notification-permission-state-is-app-state-not-a-debugprint.md) | **Nobody has ever received a notification, every layer is verified working, and the four ways it dies on the device are indistinguishable from each other and from success.** Measured 2026-08-16: `allUsers` HAS `roles/run.invoker` on `registerpushtoken`, `aps-environment` is present and wired, build 119 carries both fixes, the sweep composes two pushes a day — and Cloud Logging shows ZERO HTTP requests ever reaching the function. Never installed / prompt declined / granted-but-no-token / callable threw all ended in a `debugPrint` a TestFlight build routes nowhere, and iOS shows its dialog ONCE per install so a decline is permanent. Five named states, read (never requested) from the OS, shown and made actionable in Settings; the ADR-044 retry becomes repeatable; `AppDelegate` stops relying on method swizzling alone for the one runtime link ADR-042 left UNVERIFIED. | Accepted |
| [047](047-positive-evidence-of-publication-not-a-grep-for-apples-error-string.md) | **`continue-on-error` is not the bug; an UNREAD failure is** (lesson 69). `deliver` failed identically on nine consecutive releases with Slack silent, because a non-blocking STEP failure is structurally invisible to a notifier that reads `toJson(needs)`. The fix asserts POSITIVE evidence of publication per locale — expected from `fastlane/metadata/`, actual from App Store Connect — rather than grepping Apple's error string, which goes quiet the day the wording changes AND has no positive fixture to be written against (deliver dies before the upload phase). It found more than #204 claimed: `tr` is absent AND seven of `en-US`'s nine fields disagree with this ref, so the committed copy has never been published at all. | Accepted |
| [048](048-the-functions-deploy-lane-and-the-clean-tree-it-enforces.md) | **The last deploy target that was a hand-typed command nothing tracked** — the gap that cost S063 the whole push feature and S068 thirteen drifted functions at once. `deploy-functions.yml` is dispatch-only, and **prod is additionally pinned to `refs/heads/main`** because deploying a branch to production would *manufacture* the exact drift `functions-drift` exists to report. Three decisions came out of reading the installed firebase-tools rather than its docs: **`--force` is never passed** (it deletes functions absent from the source with no prompt, and is one flag meaning four unrelated things); the subset selector is **constructed, never forwarded** (`getEndpointFilters` silently drops a selector missing its own `functions:` prefix and returns *no filter — deploy everything* when all are dropped, so one comma separates "this function" from "all thirteen to production"); and the **read-back runs `if: always()` and still votes**, because a Functions deploy is N independent operations that the CLI will partially apply *on purpose*, so the dangerous state exists only after a failure. `functions_drift.py` gains `--only` — narrowing **both** verdicts, with out-of-scope functions **recorded but never examined** so an unrelated `gcfv1` cannot abort a dispatch that never named it — and `--require-clean-tree`, which turns #206's "clean by construction" into an instrument. The IAM role set was **measured against the IAM API and refuted the vendor documentation**: `roles/firebase.admin` does carry the Cloud Functions permissions; what it lacks is `actAs`, `cloudscheduler` and `secrets.getIamPolicy` — and `secretmanager.viewer` is asked for rather than `admin`, which is the only role that could read `LLM_API_KEY`. Prod re-measured **clean**, correcting ADR-043's 62-foreign-file finding | Accepted — closes **#206**, ships **unarmed** until operator 2(e)(iii) |
