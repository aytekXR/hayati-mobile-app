# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing toolchain note (M2.1–M2.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`) — the functions emulator never compiles TS. Full suite: `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'` from the repo root. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id, `demo-hayati` — emulator-backed clients must build URLs/options from it).

## Objective — Session 010: M2.4 — solo mode (7 solo questions + invite nudges), closing M2

Final slice of M2 (`implementation-plan.md`). M2.3 landed in Session 009: the full pairing loop — `joinInvite` (transactional, race-rejecting, typed `details.reason` surface), `users.coupleId` frozen in rules, partner preview screen (deep-link + manual entry, pre-auth mount), gate precedence `coupleId` → pending invite → share screen, paired-home placeholder. This session gives the **unpaired** user a reason to stay (`mvp.md` IN #2, `prd.md` F1: "7 days of solo reflection questions with persistent invite nudges; app is honest that it's better together"):

1. **Solo home screen** replacing `InviteShareScreen` as the gate's unpaired fallback (share flow stays one tap away — the nudge routes to it): shows the day-N solo reflection question (N = days since a **decided anchor** — recommend the `users/{uid}.createdAt` server stamp; decide + document), an answer-entry surface, a **persistent invite nudge** (banner/CTA to the share screen, honest "better together" framing per brand voice), and day-8+ behavior decided (recommend: cycle stops, nudge becomes primary; decide + document).
2. **Content source (decide + document, do NOT build the M3 pipeline):** 7 solo questions × TR/AR/EN. Recommend a tiny bundled JSON asset shaped like `content/schema/question-pack.schema.json` (presages M3's format; ARB stays UI-copy-only) with a pure loader + deterministic day-N selection (unit-tested across date/timezone edges, DST). The M3 validator/sync pipeline is explicitly out of scope.
3. **Answer persistence (decide + document):** recommend `users/{uid}/soloAnswers/{yyyymmdd}` (self-only rules WITH mutation tests, M2.1 harness pattern) so solo history survives reinstall and can surface post-pairing at M3; if Firestore fights the timebox, local-first storage with a documented DEBT + migration note is the sanctioned fallback (`session-rules` §1.4 slicing).
4. **Coverage ratchet at M2 close:** Flutter gate **62 → 64** (`ci.yml` + `test-suite.md` §3 — the ratchet line exists, never lowered).
5. **ci-debt #17 (bounded ≤30 min):** stop `integration-emulator` burning ~100–140 billed macOS minutes on docs-only main pushes (e.g. `paths-ignore` or a docs-only detection job guard). If it resists the timebox, comment findings on the issue and defer — do not let it eat the session.
6. **Docs-with-code:** `architecture.md` §4 (solo mode ships, M2 complete) + §3 if a new collection lands, `implementation-plan.md` (M2 ✅ with evidence), `test-suite.md` if the harness grows, `mvp.md` only if scope nuance emerged, ADR only if genuinely weighed. Refresh `docs/operator-expected.md` at close (session-rules §3.3).

**Acceptance criteria:** a fresh unpaired user sees solo question day 1 and can answer; the same user on day 3 (clock-independent test proof) sees question 3; answers persist across an app restart (per the persistence decision); the invite nudge is visible on every solo state and routes to the share flow; pairing mid-solo (coupleId arrives) re-routes to the paired-home placeholder without losing solo data; six-cell goldens for the solo home states; rules mutation tests for any new collection; all four CI jobs green with the Flutter gate at **64**.

**External dependencies (founder):** none for the emulator-first session (see `docs/operator-expected.md`). Blaze + the Mac slice gate only the optional real-device bonus, unchanged.

**Files likely to change:** `app/lib/features/daily_question/**` or a new `app/lib/features/solo/**` (decide placement — the solo question is the daily-question ritual's unpaired sibling), `app/lib/features/profile/presentation/onboarding_gate.dart`, `app/assets/` (+ pubspec asset entry), `app/lib/core/l10n/arb/*`, `firestore.rules` + rules suite (if Firestore persistence), `.github/workflows/ci.yml` (ratchet + #17), goldens, `docs/*`, this file.

**Validation steps:** functions suite via `emulators:exec` (only if rules/functions change); full Flutter gate sequence (`dart format` → `flutter analyze` → `dart tool/rtl_lint.dart app/lib` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 64 app/coverage/lcov.info`); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main.

**Complexity:** medium — no new Functions expected; the risky bits are the day-N determinism (timezone/DST) and scoping the content decision so M3's pipeline isn't accidentally built early. **Estimated duration:** one session (1–3 h). If it overflows: land the solo screen + rotation + nudge first (the product spine); slice persistence to M2.4b per session-rules §1.4.

**Stopping conditions:** if the bundled-JSON content decision fights Flutter asset/l10n tooling beyond a 30-min timebox, ship the 7 questions as ARB strings with a documented migration note to the M3 pack format; if Firestore solo-answer rules/tests overflow, take the local-first fallback (documented DEBT + issue). If ci-debt #17 resists its 30-min timebox, defer with findings on the issue.

**Explicitly out of scope this session:** the M3 content pipeline (validator, remote sync, packs beyond the 7 solo questions); daily rollover Function; streaks; push notifications; question text in `invitePreview` (M3); any Functions deploy (Blaze bonus only); universal links; issue #15 (needs a Mac); issue #13 (Android, M6.5).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M3.1 — question packs pipeline: `content/` JSON schema + validator + bundled loading, the daily-question data spine), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
