# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after. **Session 019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss; on agent death, inventory the tree against the deliverable list before resuming.
>
> **Standing review-ordering note (Sessions 015–020, now SIX-for-six):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code; S016: 1 blocking; S017: 3 confirmed; S018: 2 blocking + 10 serious; S019: 1 blocking + 6 serious; **S020: 4 blocking + 5 serious + ~14 minor pre-code, then 2 blocking + 4 serious MORE post-implementation**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** S020's post-implementation pass found two blockers the design review could not have seen, because they lived in the gap between what the ADR promised and what the code did. **S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND — do not drop them.** S018 addendum: when two verifier lenses disagree, the governing doc decides. S019 addendum: a skeptic pass can surface a DEEPER defect while refuting a shallow one — read refutations as carefully as confirmations.
>
> **S020 addenda (five, all earned the hard way):**
> 1. **The highest-value finding class is "a guarantee the doc MAKES that the mechanism cannot carry."** Five of S020's six blockers were exactly that. One ("never a bypass") had to be **DELETED rather than fixed**, because no app-level mechanism could ever carry it against same-device re-auth — prefer deleting an over-claim to inventing a mechanism that fakes it. Another (recovery "is idempotent when already signed out") was a sentence the ADR wrote and the code silently did not implement, and it would have **permanently bricked** any device that hit it.
> 2. **Riverpod listeners fire on state CHANGE, and the sealed auth states are value-equal.** Re-entering an identical state (signing out while already signed out) notifies NOBODY. Never hang a security-critical side effect off a transition that a no-op can skip — read the settled state and act on the *state*.
> 3. **A guarantee no fake can express must be pinned by a SOURCE-SENTINEL test** (S020: `biometricOnly: true` — no fake has a device passcode to fall back to, so the source is the only place that guarantee can live).
> 4. **MUTATION-CHECK every guard you add:** neuter it, confirm exactly its own tests turn red, restore. S020 wrote a first-draft race test that passed against the very bug it was written for — the mutation check caught it, not the review.
> 5. **Native/asset config can fail SILENTLY with a green pipeline.** S020's discreet-icon `Contents.json` mixed two schema shapes; CI compiled fine, but the plausible other branch was actool emitting no `CFBundleAlternateIcons` at all — a dead headline feature that every check would have called green. When a config's failure mode is "the feature just isn't there", conform to the canonical shape exactly and put the runtime check on the operator's device list.
>
> **Standing toolchain note (M2.1–M6.1):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`; LLM tests run against `FixtureCoachProvider` + recorded fixtures ONLY. **M5.2 (ADR-017): the coach app half is live** — `CoachScreen` behind `PremiumGate`, help-sticky LATCH, `LocalFlagStore` seam over shared_preferences. **M6.1 (ADR-018): the device-privacy layer is live** — `PrivacyGuard` mounts in `MaterialApp.builder` (the ONE point above `home` AND every pushed route) and gates the WHOLE app; the PIN lives in the **Keychain** (`PinLockStore` seam → `flutter_secure_storage`, `unlocked_this_device`) and NOT in prefs, because Firebase Auth's session is also Keychain-resident and survives delete+reinstall — a prefs-resident lock would evaporate on reinstall while the session it guards survived. **Four invariants a future session must not break: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)` — it is keepAlive and seeded from the by-value boot snapshot, so invalidation replays boot state; the wipe is a state mutation. (2) Every mutating controller op re-checks the generation token after EVERY await (`ref.mounted` cannot carry it — the controller is wiped in place, never disposed). (3) `biometricOnly: true` in the local_auth adapter is load-bearing (plain `deviceOwnerAuthentication` accepts the DEVICE PASSCODE — an app-PIN side door) and is pinned by a source-sentinel test. (4) The lock screen sits ABOVE the only Navigator — NO `showDialog`/`Overlay`/`Tooltip` in it or its children; that throw would BE the lockout.** Also: `TickerMode(enabled: false)` **pauses Riverpod subscriptions** (`flutter_riverpod-3.0.3/.../consumer.dart:383,402`), so gated content is not fetched, not merely hidden — which is why every bypass negative is paired with an unlock→reveal positive control. `handleAppLifecycleStateChanged` DOES assert on invalid transitions: drive the real iOS chains (`resumed→inactive→hidden→paused` and back). **Theme gotcha (S019): the global `FilledButton` style has `minimumSize: Size.fromHeight(48)` = INFINITE min-width — a FilledButton inside a Row needs a local `minimumSize` override.** App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions instance regional: `europe-west1`). Flutter lives at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart` (M2.4); goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` (ADR-011). **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 021: M6.2 — KVKK/PDPL data rights: self-serve export + cascade delete (with partner notification) + the discreet-notification override

**FIRST, BEFORE SCOPING: check `docs/operator-expected.md` item 6.** If the founder has answered it (an LLM provider + API key), **abandon this objective and do M5.3 instead** — the live coach adapter takes precedence the moment it is unblocked (it is the only gap left in M5, and it has been waiting since Session 018). Write the M5.3 resume-prompt from ADR-016's frozen wire contract + the `UnconfiguredCoachProvider` seam, and push M6.2 to Session 022. If item 6 is still unanswered, proceed with M6.2 as specced below.

M6's second slice (`implementation-plan.md` M6; PRD §8 NFR: *"KVKK/PDPL data-subject rights (export + delete self-serve)"*). This is the last **legally-required** feature of the MVP and the one M6 accept line still open: *"delete cascade integration test (couple data gone, partner notified, entitlement handled)."* It is server-heavy — the first Functions work since M5.1 — and it lands on a settings screen that now exists (M6.1 built it as their home, deliberately).

1. **Scope precisely at session start** (session-rules §1.5) and **write ADR-019 FIRST, then adversarially review it BEFORE code** (six-for-six — see the standing review-ordering note; use its S020 addenda). Decisions that come due: **what "delete everything" MEANS for a two-person shared object** — a couple's answers are *jointly* authored, so does A's deletion destroy B's copy of a shared reveal, or only A's authored half (the honest options differ sharply and this is a legal + product call, not a mechanical one); **the partner-notification contract** (B must learn the couple ended — through what channel, with what content, and what does B's app show the next morning?); **entitlement handling** (a deleted payer with an active RevenueCat subscription — the mirror, the RC alias, and the "one purchase entitles both" rule all collide; ADR-013/015 are the governing docs); **the export format + delivery** (Function → JSON; email vs in-app download — email needs a provider and that is an operator dependency, so check before committing to it); **soft-delete vs hard-delete and the grace window** (a DV user who deletes in a panic; an accidental tap); **idempotency + partial-failure recovery** (a cascade that dies halfway must be resumable, never a half-deleted couple); **rules** (what can a deleting user still read/write mid-cascade?); and **the discreet-notification override** (M6.1's deferral — the `users/{uid}` field + the settings-Function path + `resolveDiscreet` reading it, per `functions/src/notifications/recipients.ts`'s documented seam).
2. **The firm deliverable: the cascade proven in the emulator** — a two-member couple, both with answers, streak, entitlement mirror and coach usage; A deletes; assert every couple-scoped doc is gone, B is notified, B's app renders an honest post-deletion state (not a crash, not a ghost couple), the entitlement is handled per the ADR, and the whole thing is **idempotent + resumable** (kill it mid-cascade, re-drive, converge). Plus the export: a real JSON artifact containing everything the user authored and nothing the partner did.
3. **Docs-with-code:** ADR-019; architecture §3/§4/§8 (the deletion flow + the data-rights posture); test-suite §1; implementation-plan M6.2; this file; operator-expected (an email-delivery provider may become a NEW operator item — flag it the moment the ADR decides).

**Acceptance criteria:** export + delete Functions shipped and emulator-proven; the delete-cascade integration test passes (couple data gone, partner notified, entitlement handled — the M6 accept line, verbatim); cascade is idempotent and resumable mid-failure (test-pinned); rules updated + **mutation-tested** (the repo's standard: weaken each clause, the denied op must succeed); the settings screen gains the two rows behind honest confirmation copy (deletion is irreversible — say so, and never make it a one-tap accident); the discreet-notification override wired end-to-end through `resolveDiscreet` with the settings toggle; partner-notification copy ×3 locales (native review flagged); free tier + the daily loop + **the M6.1 lock** untouched (assertion-proven); app suite green (gate 68); **functions suite green (gate 80 hard / 85 target)** — this is the first session in three to move functions coverage, so watch it; all CI jobs green.

**External dependencies (founder):** **check item 6 first (see above).** Otherwise none required to BUILD — but if the ADR chooses email delivery for the export, an email provider becomes a new operator item; prefer a design that does not create one (in-app download / a signed URL) unless the ADR argues otherwise convincingly.

**Files likely to change:** `functions/src/data-rights/**` (new), `functions/src/notifications/recipients.ts` (the override seam), `firestore.rules`, `app/lib/features/settings/**` (two new rows — the screen exists), ARB ×3, `functions/test/**`, `app/test/**`; `docs/adr/019-*`, docs per item 3.

**Validation steps:** functions gate (`npm run lint && npm run typecheck`, then the FULL suite from the repo root: `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'` — echo the exit code, never trust the pipe); app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** high — a cascade over a shared object with a legal deadline is the hardest correctness problem left in the MVP, and the "whose data is a joint answer" question is genuinely contested. **Estimated duration:** one session, but it is the likeliest overflow candidate in the plan. If it overflows, the honest split is: **delete-cascade + partner notification this session; export + the discreet-notification override next** — do NOT stretch (session-rules §1.5).

**Stopping conditions:** if the "whose data is a shared answer" question cannot be resolved from KVKK/PDPL principles + the PRD without a founder call, **pick the most protective option** (delete only what the deleting user authored; keep the partner's own authored content; make the couple object itself terminal), record the alternatives loudly in the ADR, and flag it for the founder — do NOT block the session on it. If entitlement handling turns out to need RevenueCat *account* state (not just the mirror), stop at the mirror, note the RC-REST reconciliation debt (already recorded, rides item 0 + the deploy era), and ship the rest.

**Explicitly out of scope this session:** M5.3 unless item 6 is answered (then it REPLACES this objective); store metadata/Fastlane/perf pass/beta build (M6.3, Mac-dependent); Android (M6.5); any deploy; hotline numbers (★); `coach_sessions` persistence (founder item 7); a change-PIN flow (M6.1 debt, a convenience gap not a hole); the native SceneDelegate snapshot cover (only if item 4's on-device check finds a gap).

**M6.1 handoffs to keep in view (binding):** the settings screen exists and M6.2's rows EXTEND it — do not invent a second surface. The four lock invariants in the standing toolchain note above are not negotiable; in particular, **a cascade-delete that signs the user out must go through the existing `AuthSignedOut` path** so the lock's wipe listener fires (a deleted user must not leave a lock record behind — and if you add a new sign-out-adjacent path, the lock's `wipe()` must be on it). The deletion flow must be reachable while the app is locked? **No — deliberately: it lives behind the lock like everything else.** If deletion ever needs a pre-auth entry point, that is an ADR decision, not an implementation detail.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M6.3 — store metadata + perf + beta build; or M5.3 if still unblocked-and-unbuilt), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
