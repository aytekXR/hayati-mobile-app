# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 5). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing toolchain note (M2.1/M2.2):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`) — the functions emulator never compiles TS. Full suite: `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'` from the repo root. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`).

## Objective — Session 009: M2.3 — transactional join + race rejection + partner preview screen

Third slice of M2 (`implementation-plan.md`). M2.2 landed in Session 008: `invitePreview` (zero-auth onRequest, uniform `{status, creatorDisplayName?}` surface), the real invite share screen (`createInvite` + WhatsApp-formatted TR/AR/EN share message), and `hayati://invite/<code>` parsed into `pendingInviteProvider` (state only). This session pairs the couple:

1. **`joinInvite` callable (europe-west1, auth required, App Check plumbed/enforcement OFF — M2.1 posture):** one Firestore transaction that (a) loads `invites/{code}` and rejects unknown/expired/non-pending with typed `HttpsError`s, (b) rejects **self-join** (joiner == creatorUid) and **already-paired** participants (either side has `users/{uid}.coupleId`), (c) creates `couples/{coupleId}` (`memberUids: [creatorUid, joinerUid]` frozen, `createdAt` server timestamp, initial `timezone` — decide the source and document), (d) sets `coupleId` on both `users` docs, and (e) marks the invite consumed — **decide the invite terminal-state vocabulary** (`status: 'joined'` + `coupleId` on the invite doc per the §3 comment "coupleId arrives on join") and update `architecture.md` §3. **Race rejection is the acceptance criterion:** two concurrent joins on the same code → exactly one couple, loser gets a typed error (prove at service AND callable level, mirroring M2.1's concurrency tests); creator re-issue after join must NOT resurrect the code.
2. **Rules hardening (flagged in Session 007's review, deferred to now):** `users/{uid}.coupleId` becomes authorization-meaningful — freeze it against client writes (function-only), keep `couples/` create/delete function-only, and confirm member-only read now that real couples exist. **Mutation tests for every new protecting clause** (M2.1 harness pattern, distinct `demo-*` projectIds).
3. **Partner preview screen (app-side):** invitee-facing screen consuming `pendingInviteProvider` (deep-link entry) with a manual code-entry fallback (the invitee may have only the 8-char code from WhatsApp); calls `invitePreview` over plain HTTP (zero-auth — a thin repository seam over `http`/`HttpClient`, NOT cloud_functions; emulator base URL behind the existing dart-define pattern) → shows creator name + validity (+ the M3 `questionText` slot designed but empty), join CTA that (after sign-in) calls `joinInvite` via the callable seam and routes on success. Post-join routing: `OnboardingGate` sends users with a `coupleId` to a paired-home placeholder (the M3 daily-question screen's slot).
4. **Tests:** functions suite (happy/unknown/expired/self-join/already-paired/**concurrent race**/re-issue-after-join), rules suite + mutations, app unit tests (preview repository seam, join controller, gate routing) + widget tests and six-cell goldens for the preview screen states (empty/loading/valid/expired-or-unknown/error), `pairing_emulator_test.dart` extended: **two auth users pair via code through the real Functions** (the "two emulated devices" acceptance criterion, approximated as two signed-in users in one process — document). Functions coverage ≥85/hard-fail 80; Flutter ratchet: coverage gate **62 → 64** at M2 close only if M2 closes this session (it doesn't — M2.4 remains), so 62 stays.
5. **Docs-with-code:** `architecture.md` §3 (couples doc shipped shape, invite terminal state, coupleId rules) + §4 (pairing flow status), `implementation-plan.md` M2 progress, `test-suite.md` if the harness grows, ADR only if genuinely weighed.

**Acceptance criteria:** concurrent double-join proven to create exactly one couple with the loser receiving a typed error (emulator, service + callable level); rules mutation tests prove `coupleId` client-write denial and couple member-only read; the preview screen shows a real `invitePreview` response (name/validity) from the emulator or a faked seam per the M2.2 seam precedent (decide and document); a signed-in second user joins via code end-to-end in `pairing_emulator_test.dart`; all four CI jobs green where applicable.

**External dependencies (founder):** none for the emulator-first session. **Blaze** (FOUNDER-ACTIONS #2) gates the OPTIONAL end-of-session extra: first deploy + real-device pairing test — which also needs the Mac slice (build to device), so treat on-device strictly as a bonus; do not block the session on it.

**Files likely to change:** `functions/src/invites/` (+join, maybe shared invite-state module), `functions/test/emulator/`, `firestore.rules` + rules suite, `app/lib/features/pairing/**` (preview repository/controller/screen, join controller), `app/lib/features/profile/presentation/onboarding_gate.dart`, `app/lib/core/l10n/arb/*`, goldens, `app/integration_test/pairing_emulator_test.dart`, `docs/*`, this file.

**Validation steps:** functions suite via `emulators:exec` (standing toolchain note); full Flutter gate sequence (`dart format` → `flutter analyze` → `dart tool/rtl_lint.dart app/lib` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 62 app/coverage/lcov.info`); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main (integration-emulator runs there).

**Complexity:** medium-high — the transaction + race semantics and the rules interplay are the risky core; the preview screen is familiar territory. **Estimated duration:** one session (1–3 h). If it overflows: land `joinInvite` + rules + their suites first (the server-side invariants are the milestone's spine); slice the preview screen to M2.3b per session-rules §1.4.

**Stopping conditions:** if the zero-auth HTTP seam for `invitePreview` fights the app test harness beyond a 30-min timebox, fake the repository seam in widget tests and cover the real HTTP path only in the integration test (M2.2 precedent, document in test-suite.md); if the two-user integration flow fights the single-simulator harness, prove the race/join at the functions-emulator level and keep the app-side to one user + faked second (document).

**Explicitly out of scope this session:** solo mode (M2.4); question text in preview (M3); any Functions deploy unless Blaze already done AND time remains (bonus only); universal links / associated domains (FOUNDER-ACTIONS); issue #15 (needs a Mac); issue #13 (Android, M6.5).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M2.4 — solo mode, closing M2 and ratcheting the coverage gate to 64), commit, push, verify CI via `gh run watch`, then `codegraph sync`.
