# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after. **Session 019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss; on agent death, inventory the tree against the deliverable list before resuming. **Session 022 addendum: three implementation agents ran in PARALLEL safely** because their file sets were disjoint by design and every shared file (`ci.yml`, cross-cutting docs) was reserved to the orchestrator — reserve-shared-files is the rule that makes parallel implementation safe.
>
> **Standing review-ordering note (Sessions 015–022, now EIGHT-for-eight):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code; S016: 1 blocking; S017: 3 confirmed; S018: 2 blocking + 10 serious; S019: 1 blocking + 6 serious; S020: 4 blocking + 5 serious pre-code, then 2 blocking + 4 serious post-implementation; S021: 2 blocking + 6 serious pre-code, then 2 serious + 8 minor post-implementation; **S022: 2 blocking + 2 serious pre-code, then 0 blocking/serious + 2 real minors post-implementation — the NINTH consecutive review pass to find real defects**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND. S018 addendum: when two verifier lenses disagree, the governing doc decides. S019 addendum: a skeptic pass can surface a DEEPER defect while refuting a shallow one — and (S022) a REFUTATION can carry load-bearing precision worth folding into the ADR (the flutter_tools cloud-signing proof). S020 addenda (all held again in S022): guarantee-vs-mechanism gaps are the highest-value finding class — prefer deleting an over-claim to faking a mechanism; never hang a security side effect off a value-equal state transition; source-sentinel tests for guarantees no fake can express; MUTATION-CHECK every guard; native/asset config can fail silently green. S021 addenda: concurrency tests must race DISTINCT principals; per-entity cursors don't survive a peer's mutation of your resolution path; a rules freeze-on-update without a create-forbid is a mint gap. **S022 addenda: (1) a first-time `workflow_dispatch` workflow registers only once its file lives on the DEFAULT branch — a new workflow cannot be branch-dispatched pre-merge; plan the acceptance proof post-merge. (2) The warm-boot deferral landmine class generalizes: before deferring ANY bootstrap step past first frame, check what the FIRST BUILD already does — `AuthController` seeds `AuthSignedIn` synchronously and `OnboardingGate` opens Firestore listens at frame one with zero interaction (this killed the App Check deferral, PERF-1). (3) Environment secrets are invisible to a job without `environment:` — a "missing secrets" message can lie about WHY.**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** (`functions/test/support/admin.ts`). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M5.2 (ADR-017): the coach app half is live.** **M6.1 (ADR-018): the device-privacy layer is live — the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay` in lock-screen children.** **M6.2 (ADR-019): the data-rights layer is live — wire contracts FROZEN; the seven cascade invariants in past sessions' note are binding; the deletion notice sends NO push, by recorded DV decision.** **M6.3 (ADR-020/021/022): the release lane + store metadata + cold-start posture are live:** `release.yml` is tag-triggered (`v*.*.*`) + dispatchable (post-merge `--ref main` for edits landed via PR — a first-time workflow can't branch-dispatch); its `sign-upload` job fails CLOSED at the secrets gate until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); store copy lives in `fastlane/metadata/{tr,en-US}` guarded by `tool/store_metadata_lint.dart` in the quality job — **`--allow-empty-urls` is a ratchet: drop the flag when the founder supplies privacy/support URLs**; the app-store name "Hayati" is provisional (founder trademark search, ADR-020 D1) and `CFBundleDisplayName` stays "Hayati App" pending the founder's ADR-020 D2 call; **the pre-frame bootstrap await set is PINNED by `app/test/bootstrap/entrypoint_sentinel_test.dart` — touching `main_dev.dart`/`main_prod.dart` bootstrap means editing the sentinel in the same diff** (the four awaits: Firebase → AppCheck∥Crashlytics → `configureIfKeyed` (deliberately pre-frame — warm-boot identity-sync trap, ADR-022 D3) → prefs∥lock-snapshot); the tz parse is post-frame; `BootTrace` is debug-only, stage names only; `ios-build-smoke` asserts the `{en,tr,ar}.lproj/InfoPlist.strings` actually land in the built bundle; the 200 MB size cap in `build-report` ratchets once real measurements exist. Also: `TickerMode(enabled: false)` pauses Riverpod subscriptions; `handleAppLifecycleStateChanged` asserts on invalid transitions. **Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows.** App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 023: the mvp item-12 legal bundle, buildable half — consent surface + privacy-policy/terms drafts TR/AR/EN + the DPA inventory

**FIRST, BEFORE SCOPING — three preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter from ADR-016's frozen wire contract + the `UnconfiguredCoachProvider` seam). It has priority the moment it is unblocked (waiting since Session 018).
2. **Blaze flipped (item 2)?** → do **the first-deploy slice** instead: script the first deploy of all six Functions to `hayatiapp-dev`, verify the deploy-only pieces (Cloud Scheduler trigger, Eventarc retry, `RC_WEBHOOK_TOKEN` Secret Manager binding), inherit the ADR-019 backup-retention alignment.
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
If none: proceed with the legal bundle below.

**Why this is next:** with M6.3 closed, the **22-unit MVP engineering plan is COMPLETE except founder-blocked halves** (M5.3 ← item 6; sandbox purchase ← items 0+4; TestFlight signing ← item 4; on-device checks ← item 4). The highest-priority *unblocked* work (session-rules §4) is the pre-launch legal gate that `architecture.md` §8 currently records honestly as **unbuilt**: consent screens per locale, the privacy policy TR/AR/EN, terms, and the DPA inventory (mvp item 12). It is also the natural sibling of M6.3's finding that **no privacy-policy URL exists** — the App Store submission needs a hosted policy, and the policy must exist before it can be hosted.

**Scope (Linux-session honest):** (1) **ADR-023 first** (review-ordering rule): what consent KVKK/PDPL actually requires at first run vs what is already covered by contract/consent-at-signup — the consent surface's shape (first-run gate? settings surface? per-purpose toggles?), where consent state lives (device-local vs `users/{uid}` — mind the M6.2 rules invariant: every server-owned users field needs freeze-on-update AND create-forbid), version-stamping for future policy revisions, and the export/delete interaction (consent records are personal data too — ADR-019's export projection may need a lane). (2) **The consent/legal surface in-app** per the ADR — TR/AR/EN, RTL-correct, goldens, reachable pre-auth if the ADR says so, never blocking the founder couple's existing sessions without a recorded migration story. (3) **Draft texts**: privacy policy TR/AR/EN + terms TR/AR/EN, AI-drafted in plain honest language consistent with the shipped reality (in-app export/delete, no email, ephemeral coach, discreet notifications, the ADR-019 whole-thread deletion, billing-survives-deletion) — committed under `docs/legal/`, flagged for the founder's legal/native review (a NEW operator gate; hosting additionally needs the domain choice). (4) **`docs/dpa-inventory.md`**: every processor with role/data/region (Firebase/GCP eur3, Apple, RevenueCat, the future LLM provider as a placeholder row, Mixpanel-when-built), cross-checked against architecture §8's claims. (5) Architecture §8 updated from "unbuilt" to reality.

**Acceptance criteria:** ADR-023 committed + adversarially reviewed BEFORE code (both passes — design and built diff); the consent surface shipped per the ADR with tests + goldens ×3 locales (app gate 68 stays green); rules/tests updated if any server-side consent field ships (functions gate 80 if touched); `docs/legal/` drafts ×3 locales committed with review-PENDING flags and zero contradictions with shipped app copy (spot-check against the ARB honest bounds); `docs/dpa-inventory.md` complete and consistent with architecture §8; §8 honesty-updated; all CI green.

**External dependencies (founder):** none to BUILD. The drafts' legal/native review and the hosting (domain) are founder-gated and recorded at close, not silently deferred.

**Files likely to change:** `docs/adr/023-*.md`, `app/lib/features/` (consent surface), ARBs ×3, goldens, possibly `firestore.rules` + `functions/` (only if the ADR puts consent server-side), `docs/legal/*` (new), `docs/dpa-inventory.md` (new), `docs/architecture.md` §8.

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); functions suite iff touched; push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium — the trap is legal over-claiming: the policy text must describe what the app DOES (the M6.2/M6.3 honest bounds), not boilerplate; every sentence a lawyer would strike is a sentence the founder must be able to review AGAINST the code. **Estimated duration:** one session. If it overflows: consent surface + DPA inventory this session; the draft texts next.

**Stopping conditions:** if KVKK/PDPL consent-requirements research is genuinely ambiguous on a load-bearing question (e.g. whether daily-answer processing needs explicit consent vs contract basis), record both readings in the ADR, implement the CONSERVATIVE one, and flag the founder's lawyer question in operator-expected — do not play counsel.

**Explicitly out of scope this session:** M5.3 unless item 6 answered (then it REPLACES this); any deploy (item 2); hosting/domain anything; M6.5 (founder timing); signing/TestFlight (item 4); screenshots; hotline numbers (★); `coach_sessions` (item 7); seasonal windows (issue #29) and ci-debt #36 (candidates for a later hardening session).

**M6.3 handoffs to keep in view (binding):** the entrypoint sentinel rule (bootstrap edits = sentinel edits, same diff); the `--allow-empty-urls` ratchet drops the day URLs exist — the privacy policy this session drafts is the missing content for exactly that URL; store copy is review-PENDING — do not "fix" its TR without the founder; nothing this session may add a push notification or touch the four lock invariants / seven cascade invariants.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: whichever preemption fires first — M5.3 on item 6, the first-deploy slice on Blaze, M6.5 on the founder's Android go — else the next unblocked hardening slice: seasonal windows #29 / ci-debt #36 / change-PIN flow), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
