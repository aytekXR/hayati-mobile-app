# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore` MCP tool (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. **The other claudes on this box work OTHER repos (`evrak`, `repo`(parent), `ams-pulse`, `unhooked`)** — confirm by walking cwd before treating one as a conflict, **and a leftover background `bash` from your OWN pre-`/clear` session will show up as a child of your own claude PID** (S051 saw one sleeping 15 hours; **S053 killed another that had been spinning for 25**). **S038/S053 addendum: a CONCURRENT session on ANOTHER machine/tree can merge to `main` mid-session AND consume your session number** — S053 opened to find four commits and an operator refresh all labelled "Session 052", none of which had appended to `past-prompts.md`. `ps`/`tmux` only cover THIS box, so **re-derive the session number and the queue from `git log` + `gh issue list`, never from this file's prose.** **Checkpoint-commit implementation output IMMEDIATELY**; commit design docs BEFORE launching workflows. **S037 addendum: REVIEW workflows can mutate the tree** — after every review workflow returns, `git status` must be EMPTY before you commit anything (S047, S050, S051 and S053 all checked; all clean).
>
> **Standing review-ordering note (Sessions 015–053, now TWENTY-EIGHT consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are. Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff.** Aggregate so a finding surfaces when EITHER verifier says real. **Check `agents_error`/`agents_empty_result` before trusting a verdict distribution — an empty verdict is *unverified*, and the tooling renders it as the opposite** (S041; S053 had two empty lenses and read both transcripts before believing them). **Rebase onto latest `main` before sending a diff to the review workflows** and re-check at merge time (founder directive, S042). **S053 addendum: cap your verify fan-out with a `log()` of what you DROPPED** — S053's design review dropped 16 of 23 findings to a cap and only knew because it printed them.
>
> **S053 addendum — (56) CHECK THE UNIT OF EVERY NUMBER YOU INHERIT.** #131 said "seven high-severity advisories". There were **three**, and after the fix **one**. `npm audit` counts one entry per affected *package*, so a single advisory deep in a chain reports itself plus a "depends on a vulnerable version of X" wrapper for every package above it — "12 vulnerabilities" was **2 advisories and 10 wrappers**. The session's own design brief repeated the issue's unit error before measuring it. A number you did not define the unit of is a number you have not read.
>
> **S053 addendum — (57) A VENDOR'S "FIX AVAILABLE" IS A CLAIM, AND IT WAS FALSE.** `npm audit` reported `fixAvailable: true` for all five remaining highs. The only clean `brace-expansion` is ESM-first and its CommonJS entry exports an **object**, while `minimatch@9` calls it as a function — so taking npm's advice converts a DoS advisory into a hard runtime failure inside Firestore's own dependency chain. **Measured with a control that passes**, not reasoned. The same instinct killed the "unfixable" framing too: the *moderate*'s override does work; it was declined for a different, stated reason.
>
> **S053 addendum — (58) POLICY IN YAML IS POLICY NO SELF-TEST CAN SEE — AND THE FIX FOR A FINDING CAN CARRY ITS OWN BUG.** ADR-024 D1 established this in S025 and S053 violated it anyway, putting base-ref selection in an inline shell branch; the build-diff review found it through a `workflow_dispatch` run that would have silently no-opped. **Moving it into the tool then introduced a second defect** — resolving the base before checking the lockfile exists let a missing lockfile *skip* instead of failing closed — which **the test suite caught and the reviewers never saw**. Re-run the suite after every review fix; a fix is a change, and changes are where defects come from.
>
> **S054 addendum — (59) A RENAME IS A COPY REWRITE, AND THE NEW NAME CAN COLLIDE WITH THE PRODUCT'S OWN VOCABULARY.** "ikimiz" means "the two of us" in Turkish, so in a product *about* two people a find/replace produced real nonsense — "ikimiz ikinizin arasında kalsın" mixes *our two* with *your two*; "ikimiz iki kişi için tasarlandı" reads "the two of us were designed for two people". Four sentences had to be REWRITTEN and one had the brand removed entirely. **The suffixes moved too**, because the stem now ends in a consonant (`Hayati'yi` → `ikimiz'i`, not `ikimiz'yi`). **And the Arabic case nearly falsified ADR-033 D3 with nothing going red:** a Latin wordmark in the LEAD position of an RTL string breaks the very premise on which chrome is left un-isolated — fixed by restructuring so an Arabic word leads, preserving the premise by construction rather than patching it. **Before any bulk rename, ask what the new string MEANS in every language it lands in, and what invariant assumed the old one's shape.**
>
> **S055 addendum — (60) A GATE WRITTEN IN ONE LANGUAGE GUARDS ONE LANGUAGE.** `build_site.py`'s placeholder gate matched two ENGLISH phrases, so **four of the six legal documents reported zero unfilled blanks while carrying them** — including a Turkish privacy policy reading `[KURUCU/ŞİRKET TÜZEL KİMLİĞİ — kurucu tarafından doldurulacak]`, which is precisely what the gate exists to keep off a public URL, in this product's primary market. It was blind in English too: the governing-law blank says "to be **determined**" and was flagged only incidentally because a sibling blank shared its file. And its two markers were **one** pattern — `"TO BE COMPLETED".lower()` is a substring of the other under the case-insensitive match, so every count it printed was doubled (addendum 56, in a second tool). The fix matched the **shape** all twelve blanks share in all three languages, not the words. **When you write a guard over localized content, ask which languages it can see — and the comment justifying a narrow rule ("so ordinary prose can never trip it") is exactly where the blindness will be hiding.**
>
> **S055 addendum — (61) A TEST THAT IS NOT REGISTERED IS A GREEN RUN THAT PROVES NOTHING.** Four new `build_site_test.py` suites were added and the run went green — **with the tests never executed**. That file registers tests as bare function references in a tuple inside `main()`, and the "registration" edit matched nothing. `EXIT=0`, 38 ok lines, four suites silently absent, and it would have merged. Caught by grepping the log for a string only the new tests print. **After adding a test, prove it RAN — count the assertions or grep for its output. Do not infer execution from a passing exit code.**
>
> **S055 addendum — (62) THE LOG SAID "COMPLETE IN CODE". THE PLATFORM SAID "OPEN PR".** S054's close claimed its four-part founder directive was done; PR **#145** was green, mergeable, and **still open**. Everything S055 needed sat on that branch. Addendum 45 (query the platform, not the docs) applies to **your own session log** — the most authoritative-sounding document in the repo is still a claim. **`gh pr list` before you believe any "done" you did not watch merge.** Corollary, from the same session: a **guard that catches your change is usually right that something moved, and usually wrong about the fix.** The release-lane lint correctly flagged four new secrets reaching `sign-upload` ungated; adding them to the gate would have made a missing contact secret redden a release whose binary had already shipped. The exemption was named **and its premise proved** (every exempt secret must be read only by `continue-on-error` steps) rather than the rule weakened.
>
> **Earlier addenda that keep paying (condensed):** **the verifier panel is an INPUT to judgement, not a substitute for measuring** (S051, wrong in both directions twice in one session); **a probe whose control passes is a broken probe** (S051; S053's first secret probe masked its own error text with `sed` and only the control exposed it); **your own ADR is a claim surface you will falsify with your own code — re-read the WHOLE ADR after every code change, hunting the PARAPHRASE and the NEGATION, not just the noun** (S051 ×4, S053 ×3 — and note S053's two kinds of "12" in one document, one still true and one stale); **a falsified declaration is a finding — sort the causes before accepting any of them** (S051); guarantee-vs-mechanism gaps are the highest-value class, and **a doc under-reporting what code does is hygiene, not a gap**; **N EXPERT SWEEPS CAN ALL MISS THE SAME THING — budget a completeness critic into every fan-out and give it the MODALITIES list** (S050's #131; S053's critic found the whole winning design, and **all six agents still missed Dependabot, which the platform answered in one query**); **a remainder deferred into prose is a remainder that gets lost**; **a premise that was replaced rather than measured is likely wrong again** (S049); **a new rule can make old rows vacuous — re-run every row** (S048); **a test whose fixture is derived from its subject proves nothing** (S047/S050); **MUTATION-CHECK every guard AND the test, in both directions** (S042; S053's build-diff review proved a surviving mutant by *running* it); **verify with the command CI runs, not the convenient one** (S044 — S053 checked `npm ci` in an isolated copy); **`$?` after a pipe reads the PIPE's status** (S047, S051, and **S053 again** — three sessions); **query the PLATFORM, not the docs** (S045, S053); **read the ARTEFACT, not just the source** (S047, S051); **run the session, do not assert its conclusion** (S045); **"no unblocked engineering" is a claim to RE-DERIVE every session, never to inherit**; a stale operator item is worse than no item.
>
> **Standing toolchain note:** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin`) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **FROM THE REPO ROOT** (echo the exit code — and beware that `; echo $?` after a pipe reads the *pipe's* status): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **The emulator suite binds fixed ports and is NOT safe to run concurrently — including with your own review agents** (check `ss -ltn` for 8080/9099/5001; forbid emulator runs in workflow-agent prompts, as S050/S051/S053 did). **Also forbid workflow agents from running `flutter`/`dart` at all, and from any package-manager command that WRITES** (`npm install|ci|audit fix|update|dedupe`) — S053 added the latter after realising a review agent could rewrite the very lockfile under review. **`gcloud` is NOT installed on this box and there is no ADC** (measured S053) — Scheduler/Eventarc state cannot be verified from here; say so rather than asserting it. **Run `flutter gen-l10n` in `app/` before trusting any test or analyze run that touches localized text**. Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). **Run `dart format` before every commit** — CI runs `dart format --set-exit-if-changed app/lib app/test tool content` **from the repo root**. Coverage gates: **app 68** (measured **87.30%** at the S051 close), **functions 80 hard / 85 target** (measured **97.22%** at the S053 close). Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers. Theme gotcha: global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. Goldens are Linux-canonical: **360 tracked PNGs across 24 directories** at the S051 close (`git ls-files 'app/test/**/*.png' | wc -l` — re-measure rather than quoting this).
>
> **Standing binding-invariants note:** **M6.1 (ADR-018 rev 4)** the four lock invariants; **M6.2 (ADR-019)** the seven cascade invariants, deletion notice sends NO push, export `formatVersion` 2; **ADR-023** consent/legal is BINDING — `users.consent` server-owned, the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in one diff, `docs/legal/` byte-synced to `app/assets/legal/` under a drift test, withdrawal is PROSPECTIVE by DV doctrine; **ADR-024** `tool/ci/slack_notify.sh` is the single notifier with NO vote on the build and **ALL policy in the script** (D1 — S053 re-learned this the hard way, see addendum 58); **ADR-025** the slice-0 firewall stays live and **D8's golden declaration is discipline, not a CI gate**; **ADR-026** the `seasonalWindow` vocabulary is CLOSED and gated in FIVE readers — adding a season is a five-file change, **but only FOUR of the five are parity-guarded: that is #130**; **ADR-032** release signing is fastlane `match` + MANUAL, the build NAME comes from pubspec while the build NUMBER is CI-synthesized, and `fastlane/metadata/*/name.txt` is PINNED to **İkimiz** — enforced per-PR by `tool/release_lane_lint.dart`; **ADR-033** bidi isolation is applied at the **string boundary** and **at render only** — nothing persisted, exported or shared may carry `U+2068`/`U+2069`, chrome is deliberately NOT isolated; **ADR-034 (new, S053)** advisories are gated on what a change **INTRODUCES**, never on the tree's absolute set — there is deliberately **no baseline file, no cron and no Slack routing**, base-ref selection is policy that lives in the TOOL not the YAML, and the check is fail-closed (exit 2) on its own failure.

## Objective — Session 056: **#140 — nothing in CI compares what is MERGED to what is DEPLOYED. Firestore rules sat un-deployed for 18 days behind six green milestones, and a founder bug report found it, not a gate.**

> **Sessions 054 and 055 both deferred this, and both for founder directives.** S054 shipped the rename; S055 shipped the TestFlight readiness path and the website. #140 has now been the queued objective for three sessions running. **Do not inherit that framing either — re-read #140 and re-measure first.**
>
> **S055 changed one input to this objective.** A credential path now exists that S053 could not find: the `firebase` CLI on this box is **logged in as the founder** (`aaytekinerdogan@gmail.com`) with access to `hayatiapp-prod` and `hayatiapp-dev`, and S055 used it to create a Hosting site and deploy. That is a **local** path, not a CI one, and acceptance criterion 1 is about what CI can do — but it means a local probe of the live ruleset is now possible where S053 recorded it as impossible. **Measure with it before designing.** `FIREBASE_SERVICE_ACCOUNT` is still **not** set (checked at the S055 close).

The symptom was remediated by S052 (both projects deployed from `fa990e6`; prod ruleset `3702186d`, dev `fbae0b36`). **The defect was not.** Nothing deploys rules, and nothing compares deployed-to-merged for rules *or* for Functions code.

### Why this keeps being the same failure shape — and S055 met it twice more

`ci.yml`'s `functions-rules` job proves `firestore.rules` against the emulator, honestly, on every PR. But **the emulator loads the working-tree file**. The job guards a ruleset the runtime never reads.

S055 met the same shape twice in one session, which is the argument for building the gate:

- **`build_site.py`'s placeholder gate matched two ENGLISH phrases** and reported *clean* for four of six legal documents, including a Turkish privacy policy that said "to be completed by the founder" in Turkish. A gate whose whole purpose is that a published policy must not say that, silently no-opping for the primary market.
- **Four new `build_site_test.py` suites went green without ever executing** — registered by an edit that matched nothing, in a file whose `main()` takes bare function references. `EXIT=0`, and the four suites simply absent.

**A green check that guards nothing is worse than no check, because it is also a claim.** That is the sentence #140 is about.

### Acceptance criteria

1. **Measure the gap before designing for it.** For **both** projects: what ruleset is live, and does it match `firestore.rules` on `main`? The releases API is `firebaserules.googleapis.com/v1/projects/{p}/releases`. **Use the logged-in `firebase` CLI first** (S055's finding) and say plainly which of the two questions — "what is live" and "can CI ask" — it answers. `gcloud` is still NOT on this box and there is no ADC.
2. **The comparison instrument.** A check that detects drift between the released ruleset and `main`'s `firestore.rules`. **Argue its trigger and its vote against ADR-034 and ADR-024 before building it.** The asymmetry that decided ADR-034 does **not** obviously apply: drift between merged and deployed **is** caused by our own action or omission, unlike a third party publishing an advisory. That is an argument *for* a vote. Make it explicitly, or reject it explicitly.
3. **Decide the deploy path, which is the actual fix.** A drift *detector* still leaves rules deployed by hand. `deploy-site.yml` is the repo's worked example of a dispatch-only deploy lane with a fail-closed secrets gate — follow its shape rather than reinventing it. **Prod must be dispatch-only and the founder told before it is ever fired.** Dev is a session's to exercise.
4. **Mutation check.** Prove the detector goes red on seeded drift, not merely that it runs. The credential-free half belongs in `quality` with the other self-tests — `tool/ci/build_site_test.py` and `tool/ci/npm_audit_delta_test.py` are the two freshest examples of the shape. **And verify the new tests are actually REGISTERED** — grep the run's output for a string only they print. S055 nearly shipped four tests that never ran.
5. ADR + `docs/architecture.md` §9 + `docs/test-suite.md` §2 in the same diff (rule 8). **Write the ADR LAST** (rule 45).
6. Close **#140** with the PR. If the honest slice is detector-now / deploy-path-next, **split it and say so** rather than stretching the session.

**Design-review before the code.** The question worth adversarial attention: **is a deployed-vs-merged check a guarantee, or does it just move the trust problem to whatever runs the check?** A detector that reads a credential CI holds is only as good as that credential's scope, and a detector nobody deploys behind is the same gap one level up.

### Then, in priority order

**#146 (NEW, S055) — two external beta groups, and only one gets builds.** `Friends` (auto-assigned by `release.yml`, ADR-037) and `arkadaslar` (the founder's, pre-existing). Anyone in `arkadaslar` and not in `Friends` receives nothing, with every check green — the #140 shape in TestFlight. The founder chose `Friends` as the target, so the work is: teach `print_status` to report each group's **membership** (read-only, one API call per group), then hand the founder the list. Re-inviting a real person emails them, so that half is theirs. Cheap, and it closes a gap the session that created it could not see.

**#130 — make ADR-026's "five readers" claim true by construction.** Properly TDD-shaped: the failing test *is* the fix. All five readers do reject an unknown value; what is missing is the **parity net**. `content/schema/question-pack.schema.json` is the source of truth; `validator_core.dart` and the two TS readers are parity-tested against it; **`app/lib/features/daily_question/domain/question.dart:32` is NOT**. And the test that looks like the fifth guard is **self-referential**: `app/test/features/daily_question/data/question_pack_dto_test.dart:62` iterates `knownSeasonalWindows` — the very list under test. **The failure it permits:** add a season to the schema, validator and TS parser; forget the Dart file. CI fully green, and the app throws `FormatException` at pack-load **on a real device**. Acceptance: (1) a failing test first — `seasonal_window_parity_test.dart` reads the schema, extracts the enum, asserts **set-equality** with `knownSeasonalWindows`; follow `brandkit_token_parity_test.dart`, which already reads a JSON source-of-truth from a Dart test. (2) **Widen it** — `category` and `register` have the identical comment-instead-of-guard shape; one parity test over **every enum in the schema** closes all three, and any enum deliberately not mirrored gets its reason **in the test**. (3) **MUTATION-CHECK both directions** and re-run the neighbouring DTO tests; the self-referential loop at line 62 is a prime candidate for deletion once real parity exists. (4) Correct ADR-026 D3's wording in the same commit, strikethrough-plus-dated-note style. (5) Close #130. **Design-review the plan first:** is set-equality right, or should the app's list be permitted to be a strict subset? A reader that knows fewer seasons fails *closed*; one that knows more fails *open*. Not symmetric — encode the answer deliberately.

**#129** — delete `release.yml`'s false `Gemfile.lock` comment (uncontroversial), and decide on `--frozen`. Read the issue's honest scoping first: **no doc ever claimed the release lane installs frozen**, so this is the S044 lesson applied to the producer, not a broken written guarantee. The `--frozen` half should land on a run someone is watching, which pairs it with **#121**.

**#137** — `intl`'s first-strong ranges miss Arabic Extended-A **and misclassify it as LTR**, so the bidi seam silently no-ops for it in LTR chrome. Not reachable in Turkish or Gulf Arabic. Filed because it fails **quietly**; carries a `// DEBT:` comment. Fixing it means diverging from `intl`, which needs its own guard — read the issue's three options before choosing.

**#136** — the Functions-side twin of #133: Arabic push bodies interpolate a partner name with no isolation. **Latent, not live.** Its blocker is honest: *nobody has established whether iOS/Android notification chrome honours `U+2068`/`U+2069` at all*, and ADR-033's evidence is Flutter-engine-side and does not transfer. Ride it along with the on-device checks.

**FIRST — the preemptions. Query the platform, not the docs; re-derive from issues AND PRs.**

1. **The four `ASC_REVIEW_CONTACT_*` secrets** — `gh secret list --env release`. **If they exist, item 2(c) is live and the highest-value thing a session can do is dispatch `testflight-testers.yml` with `set_review_contact=true assign_latest_build=true submit_for_review=true` and report `externalBuildState`.** That is the founder's actual goal — five people with the app on their phones — and it is now one dispatch.
2. **`gh workflow run testflight-testers.yml -f status_only=true`** — read-only, and since S055 it prints `externalBuildState` per build plus group membership. This is the honest answer to "do the testers have it".
3. **#115** — `curl -i -X POST https://revenuecatwebhook-mzym2uw5gq-ew.a.run.app -H 'Content-Type: application/json' -d '{}'`. **JSON = fixed. HTML = still broken** (HTML at the S055 close, unchanged for **seven** sessions). If fixed: ask for a RevenueCat event replay, and **#41 becomes urgent**.
4. **Prod runtime** — `firebase functions:list --project hayatiapp-prod`. **11 × `nodejs22`** at S055, re-confirmed not inherited. **Scheduler `ENABLED @ 0 * * * *` and Eventarc `RETRY_POLICY_RETRY` still could NOT be verified — no `gcloud`, no ADC.** Record the gap; do not assert it.
5. **`RC_WEBHOOK_TOKEN` on dev** — still absent at S055 (404, prod exit-0 as control), so dev runs **ten** of eleven functions. Operator item **0(c)**.
6. **Dependabot alerts** — still **disabled** at S055. Operator item **2(b)**. Both open advisories are recorded in ADR-034 as measurably unreachable; **do not let an alert badge stampede a session into the `firebase-admin@10.3.0` downgrade ADR-034 refuses.**
7. **The website** — `curl -sI https://ikimiz.beyondkaira.com/privacy`. At the S055 close the domain still A-records to the founder's **VPS** (161.97.172.146), not Firebase. The `ikimiz` Hosting site now EXISTS in `hayatiapp-prod` and the generated site is proven on a preview channel (expires **2026-08-04**). Live channel deliberately empty.
8. **Open issues and PRs** — re-derive both from scratch. Do not inherit any list, including this one. **And re-derive the session NUMBER from `git log`.**
9. **Gate 3 / Android green-light** → M6.5.

### The blocked table as it stood at the S055 close — re-check every line; do not copy it forward

| Issue | Blocked on | Why a session cannot take it alone |
|---|---|---|
| **operator 2(c)** | founder | The four `ASC_REVIEW_CONTACT_*` values are facts about the founder. Everything else in that path is built and tested |
| **operator 2(d)** | founder | Associated Domains on the App ID. **Newly blocking** — the `applinks:` entitlement (ADR-036) is not in the provisioning profile, so the next release build fails at signing. `match` is readonly by design (ADR-032) |
| **operator 2(e)** | founder | The `ikimiz` A record points at the founder's own VPS and must be repointed at Firebase; and the controller's **legal name** is a fact no session should guess into a privacy policy |
| **#115** | founder | Making a production endpoint world-reachable is a security-posture change on a live system |
| **#41** | founder | Live billing identity. Whether sandbox purchases already exist decides *clean change* vs *migration* |
| **#48** | the device | Defers to on-device observation of a transient Face ID lockout |
| **#15** | the device | Needs a native crash log from a real iPhone |
| **#136** | the device | Needs one observation nobody has made: does the notification shade honour bidi isolates? |
| **#13** | M6.5 | Android, Gate-3 gated (ADR-006) |
| **#63**, **#71** | founder | Brandkit revisions |
| **#121** | **a go-ahead** | Dispatching the release lane uploads a real binary. **And it will now FAIL until operator 2(d) is done** — pair it with #129 and mention that |

**Do NOT** grant public invoker on prod, migrate RC subscriber ids, **deploy anything to `hayatiapp-prod`'s LIVE hosting channel or its Functions**, re-bootstrap `match` certificates, run `npm audit fix --force`, downgrade `firebase-admin`, enable Dependabot on the founder's behalf, guess the founder's legal name into a legal document, or dispatch the release lane without asking.

**What is DONE (do not re-do):** the whole MVP **M1–M6.3 including M5.3**, consent/legal at **legal version 2**, CI→Slack, the UI/UX arc + redesign waves, **#74**, **#29**, **#88**, **#47**, the bundle-id rename, the first dev deploy, release-lane signing, the `match` lane (#103, #117), **Node 22 on dev and prod**, **#76**, **firebase-admin v14**, **#70**, **ADR-032** + `tool/release_lane_lint.dart` + **#99**/**#67**, **#120**, **#100**, **#133** (ADR-033), the S052 TestFlight lanes (**#139**, #142/#143), **#131** (**ADR-034**), the **ikimiz rename** (**ADR-035/036/037**, merged at the S055 open — it had been sitting unmerged on a green branch), and **ADR-038** (S055): `--set-review-contact` writing the four Beta App Review fields from secrets with an anti-leak sentinel, `--submit-for-review` gated on readiness and de-duplicated by a state read, `--status` printing `externalBuildState` and per-build group membership, the release lane's non-fatal contact write with the `nonFatalSecrets` exemption the lint now *proves*, the placeholder gate rebuilt to be language-independent, and the `ikimiz` Hosting site created and proven on a preview channel.

**Open and UNBLOCKED at the S055 close — this is the queue:** **#140** (the objective above), **#146**, **#130**, **#129**, **#137**. All five are Linux-only and need nothing from the founder. Re-derive them anyway — do not inherit this line.

On completion, follow `session-rules.md` §3/§4: append to `past-prompts.md`, regenerate this file, refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
