# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is now gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9; the other six generate brand/marketing assets and brandkit v1.0 is final).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after.** **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing. **S029 addendum: the sibling claude at `/home/aytek/repo` (the parent) was working `repo-blueprint`, not hayati — confirm by walking its child processes' cwd/args, not just its own cwd, before treating a parent-dir session as a conflict.**
>
> **Standing review-ordering note (Sessions 015–029, now SIXTEEN consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S023: 2 blocking + 8 serious pre-code; S025: 4 blocking + 6 serious + 8 minor then 12 post-diff; S026: 26 raised, 15 survived double verification; **S029: 28 raised → 5 real defects pre-code, all fixed before the first line of code; 7 → 3 post-diff, all minor/refinement, "sound to merge as-is"**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff.** Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class; when two lenses disagree, the governing doc decides; MUTATION-CHECK every guard AND the test; policy in a YAML `if:` is invisible to the suite; a safety claim in a CODE COMMENT is a guarantee surface; a stale operator item is worse than no item. **S029 addenda: (1) when a restructure gates a widget behind a new `if/else`, spell out the fate of EVERY sibling widget and spacer around it — five of S029's pre-code findings converged on one under-specified detail (an unconditional `SizedBox` + `_PartnerSlotCard` whose migration into the `else` branch was left implicit), and a naive read would have double-rendered a card or shifted 15+ non-declared goldens. (2) A design's clean discriminant can be UNACHIEVABLE — S029's "haptic only on the live reveal, never cold-open" died because the read chain genuinely passes Waiting→Revealed on cold-open too; the honest fix was to record the bound and pick the simple §6-consistent behaviour, not to fake a distinction. (3) A `pumpAndSettle` that runs a live animation UNDER a platform-channel mock does not terminate — drive frames with `pump()`/`pump(Duration)` when a test both mocks a channel and animates.**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent**. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4): the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay`/`Tooltip`/`tooltip:`/`IconButton`/text-selection field/`ScaffoldMessenger.of` in lock-screen children — and since S027 invariant 4 HAS a mechanism: `lock_screen_forbidden_api_sentinel_test.dart`, whose scan set is DERIVED from `lock_screen.dart`'s relative-import graph.** **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); `--allow-empty-urls` is a ratchet; the pre-frame bootstrap await set is PINNED by `entrypoint_sentinel_test.dart`. **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned; the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in the same diff — and M5.3 (the live LLM) is a RECORDED re-consent trigger; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier; it has NO VOTE on the build (every error path `exit 0`s AND `continue-on-error: true`); ALL policy lives in the script, never in a YAML `if:`; the secret is repository-scoped, NOT `environment: release`; ci.yml's concurrency keys push events on the COMMIT (D8). **S026–S029 (ADR-025) — the arc is SCOPED, its firewall BUILT, and slices 1+2 shipped. The firewall: the lock-screen forbidden-API sentinel, the brandkit→Dart token-parity test (bidirectional; `iconography.*` unasserted until #63), and the 96-pair frozen-sentence digest over the ★ safety + `consent*`/`legal*` strings. A slice that turns any of them red has broken something, not found a nuisance. S028's lesson is standing: a golden matrix proves SCREENS, not transient surfaces — dialogs, snackbars, and (S029) reveal MOTION mount above/outside the golden and need a widget test. S029 addendum: motion is a §6 RULE (150–300ms, ease-out) with no `hayati-tokens.json` token, so `MotionTokens` is review-enforced (like `minimumBodySize`), NOT in the parity test — #71 tracks a possible future JSON token.** Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.



## Objective — Session 030: **ADR-025 slice 3 — onboarding & pairing.** The first slice with a Class-G guarantee surface (`ProviderActions`).

**FIRST, BEFORE ANY CODE — the preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter behind ADR-016's frozen wire contract). It has priority the moment it is unblocked — the ONLY planned MVP session-unit left. **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR bumps the legal version (all three sources, same diff), names the provider in the revised notice/policy, re-gates every user, adds the provider row to `docs/dpa-inventory.md`; the frozen-sentence digest covers every `consent*`/`legal*` string, so the notice rewrite turns it red — re-stamp it deliberately, in the same diff.
2. **Blaze flipped (item 2)?** → the **first-deploy slice** (all eleven Functions to `hayatiapp-dev`; verify the Cloud Scheduler trigger, Eventarc retry and `RC_WEBHOOK_TOKEN` Secret Manager binding). **Verify Blaze FACTUALLY** — `gcloud` is not on this box, so mint a token from the firebase-tools refresh token (`~/.config/configstore/firebase-tools.json` → OAuth exchange with the public firebase CLI client) and GET `cloudbilling.googleapis.com/v1/projects/<id>/billingInfo` for **both** projects. (S028+S029 did this; both `billingEnabled:false`.)
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
4. **On-device defect reported from the ★ recipe?** → triage preempts any planned slice.
5. **"Build the dev-rig slice"?** → fold in or re-scope.
6. **Answers on #67 (muted/outline tokens), #63 (Phosphor), or #71 (motion token)?** None blocks slice 3. **#67** would let a later slice finish `ColorScheme`; **#63 = migrate** → slice 1.5, its own session and golden era; **#71** is a brandkit-JSON tidy-up, fold into whichever slice next touches motion. If answered, record and fold into the appropriate slice, not this one.
If none fire: build slice 3 below.

**Why this is next:** ADR-025 D7 orders the arc; with the product core done (slice 2), onboarding & pairing is next and is the FIRST slice carrying a **Class-G guarantee surface**. **0 and 1 may not move off the front; 8 stays last; slices 2–7 the founder may re-order — but this is the queued next.**

**Scope — 7 surfaces (ADR-025 D7 row 3), 87 goldens, Class N + G:** sign-in screen, phone sign-in, **`ProviderActions`** (the shared sign-in-buttons + legal-footer widget), profile capture, the onboarding gate, invite share, partner preview. Composition, hierarchy, spacing, motion — **not** copy, and **not** the pairing state machine or the deep-link/join semantics.

**Acceptance criteria (ADR-025 D7):** **`ProviderActions` stays ONE shared widget** and **the legal footer is present BY CONSTRUCTION on all three sign-in call sites** (D10.9 forbids splitting it — that would convert a structural guarantee into a convention); every onboarding/pairing state still renders every state it did (the widget suite is the proof); Appendix A asserted; the declared golden set re-baselined per D8 with `git status --porcelain` pasted beside the declaration; **the three slice-0 guards green and unweakened**; `flutter analyze` clean; full suites green; coverage ≥68.

**The traps, named so they are checked rather than discovered:**
- **`ProviderActions` is Class G.** Its rendered sentences (the legal footer + any `consent*`/`legal*`-adjacent copy) are a guarantee; re-lay-out and re-theme freely, but reword nothing under `consent*`/`legal*`, and never fork the widget per screen. Find its call sites first (sign-in, phone sign-in, paywall — verify the exact set) and confirm the by-construction property holds after any change.
- **The deep-link / join flow is server semantics, out of scope.** Partner preview and invite share may be re-composed, but the `_JoinActions` pop-on-success idiom, the `popUntil(isFirst)` collapse, and the invite-code plumbing are behaviour, not layout — do not "tidy" them.
- **Copy is frozen** (Class N for the rest; Class G for the footer). Any reword resets native-review status and must be recorded in `operator-expected.md` item 1 in the same diff.
- **`onSurfaceVariant`/`outline` are still unset (#67).** If this slice wants a muted tone or a divider colour, that is #67 and it STOPS — it does not invent one (slice 1's precedent).
- **Motion, if added, is a §6 rule** (like the reveal unfold): reuse `MotionTokens`, keep it review-enforced, do NOT add it to the parity test, and prove any transient motion with a widget test (no golden sees it).

**Files likely to change:** `app/lib/features/auth/presentation/**`, `app/lib/features/pairing/presentation/**`, `app/lib/features/profile/presentation/**`, `app/lib/features/onboarding/**` (verify the exact tree), the declared golden set, `docs/test-suite.md` if the golden matrix changes shape.

**Validation steps:** `cd app && ~/flutter/bin/flutter analyze && flutter test` · `~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root · **declare the golden set BEFORE `--update-goldens`, then paste `git status --porcelain -- 'app/test/**/*.png'`** · **look at the regenerated images, do not just count** · PR, watch, squash-merge · **watch the post-merge MAIN run** (`integration-emulator` is main-only; slice 3 touches app code, so it runs) · `codegraph sync`. (Emulator suite locally is optional for an app-only slice — CI's `functions-rules` per-PR + the post-merge `integration-emulator` cover it.)

**Complexity:** high — 87 goldens (the largest set in the arc) and the first Class-G surface. Fan out the golden regen and LOOK at the images.

**Stopping conditions:** if improving a surface requires a colour, a motion value or a type step the brandkit does not define, **STOP and record it** (slice 1's precedent). If the `ProviderActions` by-construction property or a state-ladder test turns red, that is a guarantee/product regression, not a test to update.

**Explicitly out of scope:** slices 4–8; any copy edit; the pairing/join state machine; deep-link semantics; #63's Phosphor migration; #67's/#71's token decisions; M5.3 unless item 6 answered; any deploy; hosting/domain; M6.5; signing/TestFlight; screenshots; hotline numbers (★); `coach_sessions`; the legal documents; any legal-version bump.

**S029 handoffs (binding):** the slice-0 guards are live — a slice that turns one red has broken something. `onSurfaceVariant`/`outline` stay unset until #67. **Motion is a §6 rule realised in `MotionTokens`, review-enforced, not parity-tested (#71).** And the standing S028+S029 lesson: a golden matrix proves screens, not transient surfaces — motion/haptic/dialogs/snackbars need widget tests. When restructuring, spell out the fate of every sibling widget and spacer (S029's five converging pre-code findings).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file (expected next: whichever preemption fires, else **ADR-025 slice 4 — commerce** (paywall, pack selection, `PremiumGate`; 30 goldens; the free-tier byte-identity + "processing banner reads as good news, never error" invariant)), refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
