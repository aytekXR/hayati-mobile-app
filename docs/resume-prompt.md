# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is now gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9; the other six generate brand/marketing assets and brandkit v1.0 is final).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after.** **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing. **S029/S030 addendum: the other claudes on this box work OTHER repos (`evrak`, `repo`(parent), `ams-pulse`, `unhooked`) — confirm by walking child processes' cwd/args before treating one as a conflict.** **S038 addendum: a CONCURRENT session on ANOTHER machine/tree can merge to `main` mid-session** — S038's own PR merged on top of `ce80908` (ADR-027, the bundle-id rename), which no local process check could have seen: `ps`/`tmux` only cover THIS box. Two consequences, both now standing practice: (a) after every merge, read `git log --oneline -5` and `git show --stat` the commits you did not write — S038 found ADR-027 that way, and with it a stale operator item; (b) session NUMBERS are per-tree and can collide (two sessions both called themselves 037) — trust dates, ADR numbers and PR numbers over the ordinal. **S037 addendum: REVIEW workflows can mutate the tree too** (a test-integrity lens legitimately writes mutants to see whether the suite catches them) — after every review workflow returns, `git status` + `git diff` must be EMPTY before you commit anything.
>
> **Standing review-ordering note (Sessions 015–037, now TWENTY-THREE consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are. Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff** (for a genuinely one-line change, a single combined pass is proportionate — S031). Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class; when two lenses disagree, the governing doc decides; MUTATION-CHECK every guard AND the test; policy in a YAML `if:` is invisible to the suite; a safety claim in a CODE COMMENT is a guarantee surface; a stale operator item is worse than no item; a golden matrix proves SCREENS, not transient surfaces. **S030 addenda:** aggregate a verified-findings pass so a finding surfaces when EITHER verifier says real (the skeptic-REFUTED / adjudicator-REAL split is the valuable one); a cross-surface "every X is Y" rule must be pattern-scoped with counter-examples carved out; a motion widget test must prove BOTH halves of an unfold. **S031 addenda:** when an audit turns up one small defect, the honest slice is that one change; a "byte-identical" acceptance is a LEAK-CHECK, not a freeze; the motion restraint discriminant is SURPRISE vs deliberately-navigated. **S036 addendum:** a small refactor still earns a single built-diff pass; when a DRY lens wants full-DRY but the issue says "keep the seam", the governing doc (the issue) decides. **S037 addenda: (10) THE ADR'S PROMISES ABOUT ITS OWN DIFF ARE GUARANTEE SURFACES.** The built-diff pass returned 5 findings, all verifier-confirmed, and TWO of the three distinct defects were the ADR asserting something was done that nothing enforced — "the comments in *both* files are corrected" (only one was) and "the suite asserts the sweep sets the flag" (no test entered that branch). When an ADR says *"X is corrected/asserted in the same diff"*, grep for X before declaring done. **(11) A mutation check must simulate the DANGEROUS mode, not merely make the guard fire.** rev 1's "break the assertion and prove a test reddens" was unachievable: on a healthy runtime, *deleting* the guard changes nothing and only *inverting* it reddens anything — which proves the wrong thing. The fix was to install the real degradation (a `resolvedOptions` spy + `vi.resetModules` so the memoised verdict recomputes under it) and assert the symptom by name: *"ramadan does not fire in September."* **(12) "No unblocked engineering" is a claim to RE-DERIVE from the whole issue list every session, never to inherit** — S036 recorded the backlog as empty; reading all nine open issues found #29 sitting in `roadmap.md`'s own named queue position. **S044 addenda: (35) VERIFY WITH THE COMMAND CI RUNS, NOT THE CONVENIENT ONE.** `npm install firebase-admin@^14` succeeded, typechecked and passed 979 tests — and `npm ci` then **refused the tree**, because `firebase-functions@7.2.5`'s peer range excludes admin v14. The local state that worked was one CI could not reproduce. `npm install` resolves permissively and rewrites the lockfile to fit; `npm ci` asserts it is already coherent. Same shape as the S042 stale-`gen/` false green. **(36) THE EMULATOR SUITE BINDS FIXED PORTS AND IS NOT SAFE TO RUN CONCURRENTLY — INCLUDING WITH YOUR OWN REVIEW AGENTS.** A re-run of an unchanged tree reported **52 failures**; the cause was a review agent running its own `emulators:exec`. Check `ss -ltn` for 8080/9099/5001 before believing a surprising red, and re-run in a clean window before attributing anything. **(37) MEASURE THE BASELINE, DO NOT REASON ABOUT IT.** Coverage looked like it had dropped after the upgrade; checking out `main`, reinstalling the OLD major and re-running the same suite proved it byte-identical (the delta was new source from a concurrent PR). A controlled A/B on the same tree costs one run and converts a suspicion into a fact. **(38) "UNDATED" IS NOT "BLOCKED".** S043 closed by licensing a stop while #107 sat unblocked-but-optional; it took a nudge to notice that optional maintenance is still work a session can do. Distinguish *nothing left I can do* from *nothing left I must do*. **(39) A DEPENDENCY MAJOR'S VERSION NUMBER UNDERSTATES IT** — firebase-admin v13→v14 moved **48 packages**, the real headline being `@google-cloud/firestore` v7→v8. Diff the lockfile and name what actually moved, including what got DROPPED, and say which moved packages your tests do not exercise. **S043 addenda: (30) "A RUNTIME UPGRADE" IS AT LEAST THREE UPGRADES — ENGINE, ICU, AND TZDATA — AND THEY MOVE INDEPENDENTLY.** ADR-030 framed Node 20→22 as an ICU risk and checked ICU carefully; **ICU was identical (78.2 on both)** and it was **tzdata** that moved (2025c→2026a), verified by running both binaries and diffing (hour `"24"` vs `"00"` at the Istanbul midnight boundary). Harmless there only because `day-key.ts` reads no hour part — but tzdata changes UTC offsets and a day key is a function of offsets, so the guard that mattered was the **day-key parity fixture**, not the ICU check. Check `process.versions.icu` AND `process.versions.tz`, and diff behaviour across the two binaries rather than reasoning about it. **(31) VALIDATE A RUNTIME BUMP ON THE ACTUAL TARGET, LOCALLY, IF YOU POSSIBLY CAN** — `nvm` had the target major, so the full suite ran on it before anything was pushed. A runtime upgrade validated only in CI is a runtime upgrade nobody has run. **(32) WHEN CHOOSING A VERSION, READ THE VENDOR'S OWN LIFECYCLE TABLE.** firebase-tools ships one; it showed nodejs22 and nodejs24 sharing a **2028-10-31** decommission date, so "newer buys more runway" was simply false and the conservative choice cost nothing. **(33) A PR BODY IS NOT THE REPO.** #76's author had reasoned correctly about ignoring a lockfile — in the PR description, which vanishes from the tree. project-rules #9 wants the compromise, and the condition that invalidates it, next to the line that needs it. **(34) A REDEPLOY CAN MOVE WHAT `ACTIVE` DOES NOT COVER** — after any deploy that touches every function, re-verify the **Cloud Scheduler** state/schedule (ADR-011) and the **Eventarc retry policy** (ADR-012) explicitly; and use the **named-exclusion** deploy command, because `--only functions` dies on `revenueCatWebhook`'s absent secret and can leave a **split-runtime backend**. **S042 addenda: (24) A GITIGNORED GENERATED ARTIFACT IS A BASELINE YOU DO NOT CONTROL.** `app/lib/core/l10n/gen/` is gitignored; the local copy was STALE, so `flutter test` rendered the *old* ARB string and only 9 of 18 goldens failed. Regenerating goldens there would have produced 18 that CI rejects. **Run `flutter gen-l10n` before trusting any test that renders localized text**, and never regenerate goldens off an unverified baseline. **(25) A BOUND JUSTIFIED BY ONE CALLER'S CONSTRAINTS IS NOT SAFE FOR ANOTHER CALLER.** The crisis post-filter reused `truncateForScan` (4,000 chars, justified in its own comment as *"double the 2,000-char legit maximum"* — a USER-input bound) on a MODEL REPLY bounded by `COACH_MAX_TOKENS` (1024), which can exceed 4,000 chars — so the tail of a long reply was never scanned by a **safety** filter. The justification travelled with the function; the constraint did not. When you reuse a bounded helper, re-derive the bound for the new caller. **(26) A CAST ON A TEST FIXTURE SILENCES THE CHECK THAT WOULD HAVE CAUGHT YOU.** A leak test used invented enum values behind `as unknown as`, so prompt construction threw INSIDE the try block and every case classified `unknown` **without the SDK ever being reached** — the assertions were scanning an error about the adapter's own arguments. Use real enum members, drop the cast, and **assert the thing was reached** (`expect(spy).toHaveBeenCalledTimes(1)`). **(27) `JSON.stringify` CANNOT SEE AN `Error`.** `message`/`stack` are non-enumerable, so `JSON.stringify({cause: err})` is `{"cause":{}}` — a leaked upstream body stayed invisible and the test green. Walk the object graph explicitly, following `cause`. **(28) REBASE ONTO LATEST `main` BEFORE SENDING A DIFF TO THE REVIEW WORKFLOWS** (founder directive, S042) — the lenses read `git diff main...HEAD`, so a `main` that moves afterwards means the reviewed diff is not the diff that merges. Re-check at merge time and say whether it moved. **(29) MUTATE YOUR OWN REPLACEMENT, NOT JUST THE ORIGINAL.** S042 rewrote a vacuous leak test, then the matrix found TWO further vacuities in the rewrite. A fix for a vacuity is exactly as likely to be vacuous as the thing it replaced. **S041 addenda: (18) A REVIEW WHOSE VERIFIERS DIED IS A REVIEW WITH NO VERDICT.** S041's built-diff pass had all TEN verifier agents die on an API session limit; the workflow returned an empty `surfaced` list and filed all seven findings under `refuted`. Taking that at face value would have discarded seven TRUE findings. **Check `agents_error`/`agents_done` before trusting a verdict distribution** — an empty verdict is *unverified*, and the tooling renders it as the opposite. Hand-adjudicate when the verifiers are gone. **(19) WHEN A DIFF CORRECTS A CLAIM, GREP THE WHOLE REPO FOR THAT CLAIM BEFORE DECLARING IT CORRECTED.** This is addendum 15 applied to *facts* rather than principles, and S041 broke it three times in one session: a false "10×-billed on this **private** repo" premise corrected in ONE place and left in NINE (six of them `ci.yml` comments, one saying "on private repos" outright, and one in the very paragraph being rewritten); three stale ADR-027 surfaces fixed while a FOURTH stood in the founder-facing checklist; and an ADR claiming "all three stay commented out" of a diff that deletes one. **(20) THE MOLD IS SOUND ONLY FOR ITS OWN KEY.** The channel-parity sentinel's global `allMatches` count works because its key appears only in app-target blocks. `CODE_SIGN_STYLE = Automatic` was the exact inverse (3× RunnerTests, 0× Runner), so the same shape would have been **green when broken, red when correct, and green again when re-broken**. Before reusing a mold, check whether the new key's distribution matches the old one's. **(21) CHANGING A GUARD INVALIDATES THE MATRIX THAT PROVED IT** — re-run it, don't reason about it. **(22) RE-DERIVE FROM THE OPEN **PR** LIST TOO, NOT ONLY ISSUES.** Addendum 12 says re-derive the backlog from every open issue; S041 found THREE stale open PRs no session had closed, one of them green *and* mergeable for four days. **(23) ADR NUMBERS COLLIDE ACROSS TREES**, exactly like session ordinals: S041 drafted 028, found the concurrent session's open PR #95 had claimed it four hours earlier, and renumbered to 029. Earliest-created wins; record the renumber in the ADR's bullets. **S038 addenda: (13) A MUTATION MATRIX IS THE DELIVERABLE FOR A GUARD, AND IT WILL CATCH YOUR OWN GREEN THEATRE.** #88's first draft killed 9 of 12 mutants: the field-set loops iterated the schema's field names *without ever putting them on the document*, so a mutant that added a schema property sailed through. A vacuous loop reads exactly like a real one — only a mutant tells them apart. Then the review found two more classes (an id-pattern test that only checked the REJECT direction, and an `additionalProperties` flip) that the 12 mutants had not tried. Final: 19/19. When you write a guard, enumerate the drift axes and mutate BOTH directions of each. **(15) THE RULE YOU JUST INVOKED APPLIES TO YOU.** S039's ADR-018 rev 5 cited this project's own *"a comment that names a retired API misleads the next reader of a security path"* to justify fixing four Dart comments — and left the ADR's OWN body, including the Decision 8 table it calls "the table reviewers check first", naming the retired property. All three review lenses found it independently. When a diff invokes a principle, grep the whole repo for that principle's other victims before declaring done. **(16) Verify a convention before proposing a change to it** — the one refuted S039 finding proposed updating the ADR README index for an amendment; the skeptic checked how rev 4 was recorded, found amendments do not re-index, and refuted it. **(17) An API shape on a security path comes from the SDK header or the vendor's docs, never from memory** — S039 read `API_DEPRECATED_WITH_REPLACEMENT("domainState.biometry.stateHash", ios(9.0,18.0))` and the exact nullability of each property before writing a line. **(14) A gap shared with the other implementation is a reason to CLOSE it, not a licence to keep it** — #88's F2 was refuted by the adjudicator partly because the Dart side has the same `additionalProperties` gap; it was fixed anyway, and the asymmetry recorded.
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent**. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4): the four lock invariants are binding** (nothing may `ref.invalidate(privacyLockControllerProvider)`; every mutating controller op re-checks the generation token after EVERY await; `biometricOnly: true` is source-sentinel-pinned; no `showDialog`/`Overlay`/`Tooltip`/`IconButton`/text-selection field/`ScaffoldMessenger.of` in lock-screen children). **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); the pre-frame bootstrap await set is PINNED by `entrypoint_sentinel_test.dart`. **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned; the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in the same diff — M5.3 (the live LLM) is a RECORDED re-consent trigger; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier; it has NO VOTE on the build; ALL policy lives in the script, never in a YAML `if:`; the secret is repository-scoped. **S026–S036 (ADR-025) — the uipro arc is COMPLETE** (slices 0–8, Sessions 027–035, plus the #74 DRY tidy in S036); the slice-0 firewall stays live for any app-touching unit: the lock-screen forbidden-API sentinel, the brandkit→Dart token-parity test, and the 96-pair frozen-sentence digest. The shared §6 unfold is `core/widgets/soft_unfold_reveal.dart`. **S037 (ADR-026) — seasonal windows are BINDING:** the window predicate takes the **dayKey being written**, never `now` (ADR-011 D2's race argument rests on `create()` being atomic — do not restore the "identical assignments" overclaim); Hijri comes from ICU `islamic-umalqura` behind an availability guard because **`Intl` resolves an unsupported calendar to `gregory` SILENTLY**; the `seasonalWindow` vocabulary is CLOSED (`ramadan`/`eid_fitr`/`eid_adha`/`new_year`) and gated in **five** readers — schema `enum`, validator check, `validateSchemaAgreement`'s `checkEnum`, the TS parser's narrowed type, the app DTO — so **adding a season is a five-file change** and `functions/test/fixtures/seasonal-window-cases.json` must gain both sides of both new edges. Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). **Run `dart format` before every commit** (S036 CI incident). Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.



## Objective — Session 045: **re-derive the queue. If it is empty of anything both unblocked and undone, show the derivation and stop — that is the result, not a failure.**

> **What changed in S044.** **#107 closed** — `firebase-admin` is **v14.2.0** and `firebase-functions` **7.3.0** (ADR-031), deployed to `hayatiapp-dev` and smoke-tested. The bump's real content was **`@google-cloud/firestore` v7 → v8**, a client major on ADR-019's cascade path, verified by a controlled A/B that came out byte-identical against v13. The `firebase-functions` move was **not optional** — its peer range excluded admin v14, and `npm ci` is what caught it.

**FIRST — the preemptions, derived factually and written down (addendum 12; addendum 22 — the open PRs too):**
1. **`RC_WEBHOOK_TOKEN` generated?** `firebase functions:secrets:access RC_WEBHOOK_TOKEN --project hayatiapp-dev` (S044: 404). → deploy the **eleventh** Function. ADR-013 reserves generating it to a session working *with* the founder; the present-secret deploy path is proven (S042), so the token is the only unknown.
2. **Anything on #103?** → the Apple signing lane. **Read `origin/ci/testflight-lane-signing` FIRST** — a concurrent session is on it with fastlane `match`, and it likely closes **#103 and #99 together**. Do not start parallel work in `release.yml` / `fastlane/Fastfile`.
3. **A crash log on #15, or anything on #48?** → triage.
4. **Gate 3 / Android green-light?** → M6.5 planning.
5. **Answers on #67 / #63 / #71?** Founder-owned. *(Note #101 landed the four colour tokens closing #67's mechanical gap; what remains is the Settings-polish judgement.)*

**Then — the honest part.**

Re-derive from **every open issue and every open PR**. As of S044's close, what remains is: **#70** (docs, conflicted — rebase it or close it with a reason: a stale conflicted PR is a decision nobody has made, and either outcome is better than leaving it); the concurrent session's branches; and everything else founder-gated (#99/#100/#103 evidence- or operator-blocked, #41/#48/#13/#63/#67/#71 founder or M6.5).

**There is no longer a tracked engineering item that is both unblocked and undone.** If that is still true when you re-derive it — and *derive* it, do not inherit this sentence (addendum 12, which this project has been wrong about before) — then **say so plainly, show the derivation, and end the session.** That is a real result. What it must not become is a manufactured slice: session-rules §4 forbids idle-improvising features while blocked, and this codebase is finished enough that inventing work would subtract from it.

**Two things that would change this instantly**, so check them before concluding: the founder answering **#103**'s two Apple checks (which turns the release lane into live work), or the concurrent signing branch merging (which may leave follow-ups worth picking up).

**Do NOT deploy `hayatiapp-prod`.** Its blockers are cleared — the runtime has no end date and the dependencies are current — but it remains a founder decision.

**What is DONE (do not re-do):** the whole MVP engineering **M1–M6.3 including M5.3 — feature-complete**, consent/legal at **legal version 2** (ADR-023/028), CI→Slack (ADR-024), the UI/UX arc (ADR-025) plus the concurrent `redesign/` waves (#94/#98/#101/#105), **#74**, **#29** (ADR-026), **#88**, **#47**, the bundle-id rename incl. Firebase Phase 2 (ADR-027), the **first backend deploy** (S040), the **release lane's signing** (ADR-029, diagnosed to Apple's provisioning → #103), **M5.3 + its deploy** (ADR-028), the **Node 22 runtime** (ADR-030, #96), **#76**, and **firebase-admin v14 + firebase-functions 7.3.0** (ADR-031, #107).

On completion, follow `session-rules.md` §3/§4: append to `past-prompts.md`, regenerate this file, refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
