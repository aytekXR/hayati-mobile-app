# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing toolchain note (M2.1–M4.2):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (Sessions 013–015: running `emulators:exec` from `functions/` breaks the exec script's `cd functions`; and never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`) or the live `answerReveal` trigger races the drive — only deliberate wire-proof e2e tests stay on `demo-hayati`. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`** (`RC_WEBHOOK_TOKEN=demo-rc-webhook-token`; the emulator loads `.env.<projectId>` dotenv files — no CI or command change needed; real env files are gitignored). **M4.2: `purchases_flutter` is pinned `^10.4.1` behind the `PurchasesRepository` seam** — the SDK drives platform channels, so ALL app tests run against `test/support/fake_purchases_repository.dart` + `purchases_fixtures.dart` (fixtures mint REAL SDK model objects; `Package`'s ctor needs a positional `PresentedOfferingContext`); the real adapter configures only when the `REVENUECAT_IOS_API_KEY` dart-define is non-empty (fail-closed `PurchasesUnavailableException` otherwise) and `Purchases.logIn(uid)` is auth-wired via `PurchasesIdentitySync` (warm-start-aware). App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id). Widget-test gotcha (M2.4): asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content: packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones: couple dayKey comes from `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites; never key couple state off the device zone (ADR-011). Coverage gates: app 66 (consider 66→68 with the M4 close — this session if M4 closes), functions 80 hard / 85 target; the app gate script runs from repo root: `dart tool/coverage_gate.dart --min 66 app/coverage/lcov.info`.

## Objective — Session 017: M4.3 — gift flow + `TRANSFER` handling; sandbox purchases IF the founder dependency landed (else the engineering half + M4-close bookkeeping)

Third M4 slice (`implementation-plan.md` M4; PRD F4 "gift-your-partner purchase flow"; ADR-013 parked `TRANSFER` here; ADR-014 built the paywall it mounts on). **Check `docs/operator-expected.md` item 0 FIRST** — the shape of this session forks on it:

- **If the RevenueCat account + App Store Connect app record now exist (operator item 0):** wire the live half — RC project config (iOS API key via the `REVENUECAT_IOS_API_KEY` dart-define, never committed), subscription products (TR/SAR/USD tiers per F4, spec'd with the founder), StoreKit-configuration sandbox purchases on the simulator if feasible without the Mac slice. **The M4 accept line — a sandbox purchase in TR + SA storefronts flipping `isPremium` — is THE deliverable**, plus the webhook config plan (deployed URL + ≥256-bit token = operator item 2, Blaze).
- **If item 0 is still absent (likely):** ship the engineering half only, and DO NOT stretch: (1) **`TRANSFER` event handling** in `revenueCatWebhook` (ADR-013 amendment: a subscription moving between RC subscribers — decide and document the projection for within-couple transfers vs cross-couple, with the same lane/total-order discipline; mocked events, emulator-proven, mutation-hardened); (2) **the gift flow's app half** against the fake seam (PRD F4: gift-your-partner purchase — decide the UX shape on the existing paywall per ADR-014's entry points; likely a gift affordance on the paywall + the RC semantics documented for the live slice); (3) **M4 close bookkeeping**: the coverage ratchet 66→68 (the M3-close note said "with the M4 close"), implementation-plan M4 status honestly marked (engineering complete, sandbox accept line pending operator item 0 + deploy — NOT silently dropped), and the next objective (likely M5.1 coach) unblocked.

1. **Scope precisely at session start** (session-rules §1.5): the gift flow's real RC semantics (RC "gifting" is not a first-class store feature — App Store family sharing / promo codes / a second purchase are the real mechanisms) may collapse item (2) into a documented decision + a small UI affordance rather than a full flow. An honest ADR-015 (or ADR-013/014 amendment) beats speculative UI.
2. **`TRANSFER` handling is the firm engineering deliverable**: ADR-013 left it a logged no-op; M4.3 decides its projection. Within-couple transfers (both uids → same mirror) are near-no-ops by construction; the dangerous case is a transfer that RESOLVES to a different couple (stale aliases, re-paired users) — the existing-user hard stop and lane model must be re-examined against it. fast-check + in-process emulator proofs, same bar as M4.1.
3. **Docs-with-code:** ADR for whatever is decided; test-suite §1; implementation-plan M4.3 (+ M4 close status); this file; operator-expected refresh.

**Acceptance criteria (absent item 0):** `TRANSFER` events project correctly (replay/out-of-order/duplicate-safe, proven in-process + property tests) with the cross-couple hazard explicitly tested; the gift decision is documented with whatever UI half is honestly buildable against the fake; functions suite green (gate 80); app suite green with the **66→68 ratchet if M4 engineering closes**; all CI jobs green; M4 status recorded honestly in implementation-plan. **(With item 0):** the above PLUS a real sandbox purchase flipping `isPremium` on the paired home via the deployed-or-emulated webhook path, TR + SA storefronts.

**External dependencies (founder):** operator item 0 (RevenueCat account + ASC app record — needs the Apple Developer enrollment promised 2026-07-08) gates the sandbox half; item 2 (Blaze) gates the webhook's deployed URL. **The session proceeds without them** (engineering half + honest M4 bookkeeping).

**Files likely to change:** `functions/src/entitlements/**` + tests (TRANSFER projection), `app/lib/features/entitlements/**` (gift affordance if decided), `docs/adr/`, docs per item 3. Rules: none expected (`subscriptions` writes stay function-only).

**Validation steps:** functions gate (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from repo root**); app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min <66 or 68> app/coverage/lcov.info` from repo root); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium-low absent item 0 (one webhook event type + a documented decision + bookkeeping — a deliberately thinner session after two dense ones); medium-high with item 0 (live store config + sandbox proof). **Estimated duration:** one session.

**Stopping conditions:** if RC's documented `TRANSFER` payload is ambiguous about alias/subscriber semantics, project the conservative reading (never mutate a mirror the resolved couple doesn't own) and record the question for the live-sandbox verification. If the gift mechanism turns out to need store features that don't exist pre-ASC-record, document that as the decision and move on — no speculative UI.

**Explicitly out of scope this session:** the coach (M5); RC-API reconciliation/backfill (scheduled with the deploy era, likely M6 hardening); webhook deploy/config (operator item 2); Android (M6.5); the private thread (M5 scope selection); seasonal selection (#29, W9); any deploy; the M4.2 identity-sync hardening note (cursor-reset-on-failure — lands WITH the live-key slice, whichever session first runs with a real `REVENUECAT_IOS_API_KEY`).

**M4.2 handoffs to keep in view (binding):** the mirror is the ONLY unlocker — `CustomerInfo` from purchase/restore is a hint, no UI flips entitled from it (ADR-014 Decision 3); every premium surface mounts on `PremiumGate` + `isPremiumProvider(coupleId:)` — never a second decision point; `Purchases.logIn(uid)`-before-purchase is auth-wired (`PurchasesIdentitySync`) and guarded in the adapter (`Purchases.isAnonymous` → `PurchaseNotIdentifiedException`); the paywall is reachable only with a `coupleId` (purchase structurally gated behind pairing — the ADR-013 gap closure; a gift flow must not reopen it); store price strings render verbatim, never re-formatted; the durable pending-purchase banner + the webhook-undeployed window is exactly what a sandbox purchase will show until item 2 lands; paywall/pack ARB copy is AI-drafted — founder native review pending (TR by the founders, AR by the Gulf reviewer; recorded in operator-expected).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M5.1 — coach v0 scope + proxy foundation, or the M4 sandbox slice if item 0 landed mid-stream), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
