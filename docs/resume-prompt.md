# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing toolchain note (M2.1–M3.2):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` — since M3.2 this is `tsc` **plus** `scripts/bundle-packs.mjs` copying `content/packs/` → `lib/content/packs/`; the byte-equality guard test fails without it) — the functions emulator never compiles TS. Full suite: `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'` from the repo root. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id, `demo-hayati`). Widget-test gotcha (M2.4): rootBundle loads never complete inside the fake-async zone — asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`; plain `test()` cases keep proving the real pubspec asset wiring. Content (M3.1): packs are authored under `content/packs/` ONLY — edit there, then `dart content/validator/validate.dart --sync` regenerates `app/assets/content/` (check mode runs in CI and fails on drift; self-tests: `dart content/validator/validate_test.dart`).

## Objective — Session 013: M3.3 — answer → mutual reveal: paired-home question UI, reveal-gated answers, server-side reveal invariant

Third slice of M3 (`implementation-plan.md`). M3.2 closed in Session 012: the rollover Function assigns each couple `couples/{cid}/days/{yyyymmdd}` (metadata-only: `questionId`, `packId`, `packVersion`, `assignedAt`; member-only read, client writes denied) deterministically per timezone bucket. This session builds the loop the couple actually lives in — answer today's question, then see your partner's answer only after yours exists:

1. **Paired-home daily question UI (app):** replace `paired_home_placeholder.dart` with the real daily-question screen. Read path: `users/{uid}.coupleId` → `couples/{cid}` (first app-side couple read — new repository seam; the doc carries `timezone`) → dayKey via a **Dart mirror of the functions `localDayKey` over the couple's STORED timezone — NEVER `soloDayKey(DateTime.now())`/the device zone** (ADR-011 binding contract; a device in another zone would read the wrong doc around midnight) → `days/{dayKey}` → resolve question text from the bundled pack by `packId`+`questionId` (`AssetQuestionPackRepository` exists). Missing day doc (pre-first-rollover, offline, deploy lag) gets an honest empty/waiting state — the server assignment is authoritative (ADR-011); client-side prediction/prefetch is OPTIONAL scope, skip unless trivial.
2. **Answers with the server-side reveal invariant (rules — THE M3 accept line):** answers live at `couples/{cid}/days/{dayKey}/answers/{uid}` — a reveal-gated per-user SUBCOLLECTION, deliberately NOT fields on the day doc (rules are document-granular; ADR-011/architecture §3 decided this in M3.2). Own answer: create/update self-only with a frozen field surface mirroring `soloAnswers` (`hasOnly`, bounded non-empty text, server-stamped timestamp, delete denied). **Partner's answer readable ONLY when the requester's own answer doc exists** (`exists()` on the sibling path) — mutation-tested in the weakened-rules harness (this is the invariant `implementation-plan.md` M3 accepts on: "partner answer unreadable pre-answer", architecture §3 line 60). Member-of-couple guard via the parent `get()` as in the M3.2 days block.
3. **Mutual reveal UX:** pre-answer the partner slot shows a locked state (never the text — enforced by rules, not just UI); after own answer lands, the partner answer streams in. Both-answered is the "revealed" state. `revealedAt`/thread scaffold: decide whether M3.3 stamps `revealedAt` (needs a Function or a rules-safe client write — day doc is client-write-denied) or defers it with the thread to M3.4/M3.3b; document the decision either way.
4. **Goldens:** question card/paired home across TR/AR/EN × LTR/RTL (+130% dynamic type per test-suite §1 — the M3 accept line "goldens: question card"): states locked / own-answered-waiting / revealed / no-day-yet.
5. **Docs-with-code:** architecture §3 (answers subcollection shape + reveal rule shipped), §4 (flow status), test-suite §1 (app + rules layers), implementation-plan (M3 progress line), this file.

**Acceptance criteria:** server-side reveal rule proven in the rules suite incl. mutation tests (own-answer-exists guard weakened → pre-answer partner read succeeds; membership guard weakened → non-member read succeeds; answer field-surface mutants per the soloAnswers pattern); answer write path proven against the emulator; paired-home question UI renders the real assigned question end-to-end against the emulator (rollover-seeded day doc); goldens land for the new screen (6-cell matrix + scale variants); dayKey parity — a Dart↔TS fixture test pins `localDayKey` byte-equality for shared instants/zones (incl. a DST boundary); app suite green with coverage ≥ the current gate (64 — the +2 milestone ratchet lands when M3 closes, test-suite §3); functions suite green (rules suite grows, functions src untouched); all CI jobs green.

**External dependencies (founder):** none (emulator-only session). Blaze/Mac items remain optional-bonus (`docs/operator-expected.md`).

**Files likely to change:** `app/lib/features/daily_question/**` (couple/day repositories, day-key mirror, paired home screen + providers), `app/lib/features/pairing/presentation/paired_home_placeholder.dart` (replaced), `app/test/**` (+goldens), `firestore.rules` (+answers block + suite), `functions/test/emulator/firestore-rules.test.ts`, `app/lib/l10n/*.arb`, docs per item 5.

**Validation steps:** app gate (`flutter analyze` → `flutter test` → coverage ≥66 → goldens intentional) + functions gate (rules suite changed: `npm run lint` → `typecheck` → `build` → full `emulators:exec` suite); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** high — first app-side couple/day read path, a new rules invariant, and a golden-heavy screen in one session. **Estimated duration:** one session (2–3 h). If it overflows: land the answers subcollection + reveal rules + repositories first (the invariant is the M3 accept line and M3.4's dependency); slice the full paired-home UI/goldens to M3.3b per session-rules §1.4.

**Stopping conditions:** if streaming the partner answer through rules-gated listeners fights the SDK (permission-denied on the pre-answer listen is EXPECTED — attach the partner listener only after own answer exists, or catch-and-lock), pin the behavior with emulator tests before UI polish. If `revealedAt` needs server writes, defer it (the both-answers-exist condition is derivable client-side) and note it for M3.4's streak logic.

**Explicitly out of scope this session:** the private thread beyond a decided-and-documented scaffold; streaks (M3.4); push notifications (M3.4); `invitePreview.questionText` (M3.4+, needs the loop verified end-to-end); client answer prediction/offline prefetch beyond the honest empty state; seasonal selection (issue #29, W9); device-IANA timezone capture at join (later M3/M4 app slice); couple-pack authoring (W9); any Functions deploy; packConfig UI; issues #15 (Mac) and #13 (Android, M6.5).

**M3.2 handoffs to keep in view (ADR-011 "binding contracts"):** the day doc is metadata-only — M3.3 must NOT add answer fields to it; couple questions come from the `solo_tr` placeholder bank until W9 (register-neutral, 7 questions, deterministic recycle on day 8+ — the founders will recognize their solo questions; accepted for dogfooding); the couple dayKey is a pure function of `couples.timezone`; the day doc's `packId`+`questionId` are globally unique, so text resolution needs no version juggling.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M3.4 — streak engine + pushes, or M3.3b if sliced), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
