# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Session 013 incident):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window; two sessions racing one working tree nearly corrupted M3.3. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing toolchain note (M2.1–M3.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`; the byte-equality guard test fails without it) — the functions emulator never compiles TS. Full suite **from the repo root** (Session 013: running `emulators:exec` from `functions/` breaks the exec script's `cd functions` and can flake the rules suite): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id, `demo-hayati`). Widget-test gotcha (M2.4): rootBundle loads never complete inside the fake-async zone — asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content (M3.1): packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones (M3.3): the couple dayKey comes from `coupleDayKey(now, couples.timezone)` (`couple_day.dart`, `package:timezone` latest_10y, bootstrap + lazy init) — Dart↔TS parity is pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites; never key couple state off the device zone (ADR-011).

## Objective — Session 014: M3.4 — streak engine + reveal-driven Functions: mutual-day record, streak with grace, notification triggers (emulator-side), closing M3

Final M3 slice (`implementation-plan.md`). M3.3 closed the loop the couple lives in: answer → server-gated mutual reveal, proven end-to-end. What's left of M3 is what the loop *produces* — the streak — and the pushes that pull the couple back in. Two design seams were deliberately parked for this session: `revealedAt` is unstamped (both-answered is client-derivable; architecture §3/§4), and nothing server-side reacts to answers yet.

1. **Reveal trigger Function (the repo's first Firestore trigger):** `onDocumentCreated` (or `Written` — decide against replay/idempotency semantics, document in an ADR) on `couples/{cid}/days/{dayKey}/answers/{uid}`. When BOTH answers exist, atomically (transaction): stamp the day doc's `revealedAt` (admin write — the M3.3 deferral lands here; day doc stays client-write-denied) and update `couples.streak`. Idempotent under duplicate trigger delivery and the two-answers-race (both creates firing "second"); mutation/race proven in-process like the M3.2 rollover suites.
2. **Streak semantics (THE M3 accept line — property tests):** `streak {count, lastMutualDate, graceTokens}` over the couple's LOCAL calendar (dayKeys in `couples.timezone`, reusing `localDayKey`): consecutive-day increment, same-day idempotence, gap resets, grace-token consumption bridging a 1-day gap (earn/refill policy: decide + document — prd.md F-streak), DST/timezone edges (the parity fixture's zones). `fast-check` property tests: grace, gaps, timezone edges (test-suite §1). Rules: `streak` must be client-frozen on `couples` (extend the M3.3 freeze pattern + mutation test).
3. **Notification triggers, emulator-provable half only:** compose-and-send seam (`messaging.send` behind an injectable port — FCM has no emulator; the SEND is mocked in-process, APNs delivery stays the Mac/deploy item, operator-expected #4): partner-answered nudge (first answer of the day → partner who hasn't), reveal push (both answered), streak-at-risk (decide: piggyback on the hourly rollover sweep at couple-local evening vs a separate schedule — document). Quiet hours (couple-local, stored zone) + discreet-text mode (generic copy, no question/answer content in payloads — prd.md privacy posture) as pure payload policy, unit-tested. `users.fcmTokens` capture is app-side platform-channel work — DEFER the app half to the on-device slice if it fights the session (the Functions read tokens that may simply not exist yet; empty = skip send, loud in summary).
4. **App: streak display on the paired home** — the revealed state shows the couple's streak (read from the live couple doc; `Couple` grows `streak`); goldens for the revealed-with-streak cell set (extend the M3.3 matrix, same harness). Keep it a display slot, not a celebration screen (M5 polish).
5. **Docs-with-code + M3 CLOSE:** ADR (trigger design + streak/grace + push policy), architecture §3 (streak shape + revealedAt now real, §4 flow status, §10 trigger cost shape), test-suite §1, implementation-plan (M3 ✅ with evidence), **coverage ratchet 64→66 in ci.yml** (the +2 milestone ratchet, test-suite §3), this file, operator-expected refresh.

**Acceptance criteria:** trigger idempotency + two-answers-race proven in-process (duplicate delivery, concurrent creates → exactly one streak increment + one `revealedAt`); streak property tests green (grace consumption, gap reset, timezone/DST edges over the parity zones); quiet-hours/discreet-mode payload policy unit-tested (no question/answer text in any discreet payload); `streak` client-write-frozen with a mutation test; paired home renders the streak in the revealed state (goldens extended); app suite green with coverage ≥66 (ratchet lands this session — M3 closes); functions suite green; all CI jobs green.

**External dependencies (founder):** none (emulator-only; APNs/real delivery stays operator-expected #4).

**Files likely to change:** `functions/src/` (first Firestore-trigger Function + streak/notification services), `functions/test/**`, `firestore.rules` (+streak freeze + suite), `app/lib/features/daily_question/**` (Couple.streak + revealed-state display), `app/test/**` (+golden cells), `.github/workflows/ci.yml` (coverage 66), `docs/adr/012-*`, docs per item 5.

**Validation steps:** functions gate (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from repo root**) + app gate (`flutter analyze` → `flutter test` → coverage ≥66 → goldens intentional); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main (includes the M3.3 `daily_question_emulator_test` first post-merge run — watch it specifically); `codegraph sync`.

**Complexity:** high — first Firestore trigger, transactional streak math, and a payload-policy surface in one session. **Estimated duration:** one session (2–3 h). If it overflows: land the trigger + streak + rules + property tests first (THE M3 accept line and the ratchet); slice the notification triggers to M3.4b per session-rules §1.4 — quiet hours/discreet mode are pure functions and move with them cleanly.

**Stopping conditions:** if the answers-trigger fights the functions emulator (trigger delivery flakiness is a known emulator soft spot), pin the SERVICE-level semantics in-process first (the M3.2 pattern: handler tests carry the acceptance; the trigger wire gets a thin e2e or is deploy-verified) and document. If grace-token policy has no prd.md anchor deep enough to decide, pick the simplest coherent rule (1 token, consumed automatically to bridge exactly one missed day, no earn-back until W9 content decides), mark it an ADR decision, move on.

**Explicitly out of scope this session:** APNs setup / on-device delivery (operator item 4); `users.fcmTokens` app-side capture IF it fights the session (defer loudly); the private thread (M4+ decision — record where it lands when regenerating this file); `invitePreview.questionText` (M3.4+ note from M3.2 — decide-or-defer at close); seasonal selection (issue #29, W9); streak celebration/animation polish (M5); Android (M6.5); any deploy.

**M3.3 handoffs to keep in view (binding):** the reveal invariant is SERVER-side — the trigger must not weaken it (it writes day-doc/couple fields, never answer docs); answers freeze once both exist (`hasAnswered` both → update denied), so the trigger sees immutable inputs; `revealedAt` lands on the DAY doc (client-write-denied; members read it via the M3.2 rules); the day doc's metadata-only posture (ADR-011) is amended by THIS session's ADR, not silently; `couples.timezone`/`createdAt` are rules-frozen — extend the same pattern to `streak`; the answers subcollection has NO pre-answer existence oracle — the partner-answered push must not become one (push payloads reveal "partner answered", which is deliberate and fine POST-first-answer; quiet-hours policy still applies).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M4.1 — paywall/entitlements start, or M3.4b if sliced), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
