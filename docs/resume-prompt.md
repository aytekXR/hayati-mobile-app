# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after. **Session 019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss; on agent death, inventory the tree against the deliverable list before resuming.
>
> **Standing review-ordering note (Sessions 015–021, now SEVEN-for-seven):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code; S016: 1 blocking; S017: 3 confirmed; S018: 2 blocking + 10 serious; S019: 1 blocking + 6 serious; S020: 4 blocking + 5 serious pre-code, then 2 blocking + 4 serious post-implementation; **S021: 2 blocking + 6 serious pre-code, then 2 serious + 8 minor post-implementation — the EIGHTH consecutive review pass to find real defects**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff** — both S020 and S021 post-implementation passes found defects the design review could not have seen. **S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND — do not drop them.** S018 addendum: when two verifier lenses disagree, the governing doc decides (S021 applied it twice, both for the adjudicator — one verdict DELETED a whole push-notification surface from the design). S019 addendum: a skeptic pass can surface a DEEPER defect while refuting a shallow one — read refutations as carefully as confirmations. S020 addenda (the five hard-earned rules: guarantee-vs-mechanism gaps are the highest-value finding class — prefer deleting an over-claim to faking a mechanism; never hang a security side effect off a value-equal state transition; source-sentinel tests for guarantees no fake can express; MUTATION-CHECK every guard — neuter it, confirm exactly its own tests turn red, restore; native/asset config can fail silently green) all held and paid again in S021.
>
> **S021 addenda (three, earned in the post-implementation pass):**
> 1. **A concurrency test must race DISTINCT principals.** S021's "concurrent double-delete" test raced A against A — a shape the per-uid cursor structurally protects — and passed while the real A-vs-B interleaving (the partner's detach destroying A's resolution path) was broken. If a mechanism is per-principal, the test that validates its concurrency claim must cross the principal boundary, and the fix must be mutation-checked (remove the mechanism, watch the exact residue appear).
> 2. **Per-entity resume cursors do not survive a PEER's mutation of your resolution path.** When another actor's transaction can destroy the pointer you resolve from (B's detach clears `users/{A}.coupleId`), that same transaction must seed YOUR cursor (`deletions/{partnerUid}`), and your resolve must be a transaction so the torn state is unreachable. "Idempotent + re-drivable" is not enough if a re-drive can no longer FIND the work.
> 3. **A rules freeze on update without a create forbid is a mint gap** whenever the create rule has no `hasOnly` (this repo's `users` create). Every server-owned users field needs BOTH clauses, each mutation-tested. (`coupleId` always had both; `coupleEnded`/`notificationPrivacy` shipped rev-2 with only the update half.)
>
> **Standing toolchain note (M2.1–M6.2):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`; LLM tests run against `FixtureCoachProvider` + recorded fixtures ONLY. **M5.2 (ADR-017): the coach app half is live** — `CoachScreen` behind `PremiumGate`, help-sticky LATCH, `LocalFlagStore` seam over shared_preferences. **M6.1 (ADR-018): the device-privacy layer is live** — `PrivacyGuard` in `MaterialApp.builder` gates the WHOLE app; the PIN lives in the **Keychain** (`PinLockStore` seam, survives reinstall like the Firebase session it guards). **Four M6.1 lock invariants (binding): (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)` — the wipe is a state mutation. (2) Every mutating controller op re-checks the generation token after EVERY await. (3) `biometricOnly: true` is load-bearing and source-sentinel-pinned. (4) No `showDialog`/`Overlay` in lock-screen children — that throw IS the lockout.** **M6.2 (ADR-019): the data-rights layer is live** — `deleteAccount`/`exportData`/`updateNotificationPrivacy` callables; wire contracts FROZEN (`{confirm:'DELETE'}→{status:'deleted'}`; export `formatVersion:1`, epoch-ms `*Ms` fields, questionId-only — the app renders text; `{discreet:bool}`). **M6.2 cascade invariants a future session must not break: (1) the `deletions/{uid}` cursor is AUTHORITATIVE on re-drive — never resolve from the live `coupleId` when a cursor exists; (2) the detach transaction seeds the PARTNER's cursor (`seededByPartner:true`) and the seed-cleanup step removes only seeded cursors — a self-written cursor is removed only by its owner's own cascade; (3) every sweep is confirm-or-throw (BulkWriter `close()` never rejects — await the op promises) and auth deletion stays LAST and idempotent (`deleteUsers`, plural); (4) the own-subtree sweep precedes the invites sweep BECAUSE the deleted profile fail-closes `createInvite` — reordering reopens the intra-cascade mint window; (5) every server-owned `users` field is frozen on update AND forbidden at create (no `hasOnly` on users create); (6) `coupleEnded` is cleared by `joinInvite`, rendered via the gate branch ABOVE pending-invite, with an EVENT-keyed device-local seen flag; (7) the deletion notice sends NO push, by recorded DV decision — do not "helpfully" add one.** Also: `TickerMode(enabled: false)` **pauses Riverpod subscriptions**, so lock-gated content is not fetched; `handleAppLifecycleStateChanged` asserts on invalid transitions — drive real iOS chains. **Theme gotcha (S019): the global `FilledButton` style has `minimumSize: Size.fromHeight(48)` = INFINITE min-width — a FilledButton inside a Row needs a local `minimumSize` override.** App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions instance regional: `europe-west1`). Flutter lives at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart` (M2.4); goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` (ADR-011). **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 022: M6.3 — store metadata (TR/EN via Fastlane) + performance pass + the closed-beta release lane

**FIRST, BEFORE SCOPING: check `docs/operator-expected.md` item 6.** If the founder has answered it (an LLM provider + API key), **abandon this objective and do M5.3 instead** — the live coach adapter takes precedence the moment it is unblocked (the only gap left in M5, waiting since Session 018). Write the M5.3 slice from ADR-016's frozen wire contract + the `UnconfiguredCoachProvider` seam, and push M6.3 to Session 023. If item 6 is still unanswered, proceed with M6.3 as specced below.

M6's third and final slice (`implementation-plan.md` M6): App Store metadata TR/EN via Fastlane, the performance pass (PRD §8: cold start <2s; the reference device is iOS-first per ADR-006 — the mid-range Android pass rides M6.5), crash-free instrumentation posture, and the closed-beta build lane. **The M6 accept line still open:** *"`release.yml` produces signed builds to TestFlight."*

**Be honest at scoping time about the Mac/enrollment boundary (session-rules §1.5):** signing, the actual TestFlight upload, and on-device perf measurement need the founder's Apple Developer enrollment (operator item 4 — still pending) + Mac + signing secrets that do not exist in CI yet. The realistic Linux-session scope is: (1) **Fastlane metadata structure** (`fastlane/metadata` per locale TR/EN — title/subtitle/description/keywords/privacy URLs, drafted in the brandkit voice, native review flagged; screenshots are Mac-era), (2) **the `release.yml` lane built and dry-runnable** (tag-triggered: integration suite → build → sign → upload, with the signing/upload steps cleanly gated on secrets existing so the lane is testable to the boundary — `ITSAppUsesNonExemptEncryption=false` lands in Info.plist per the operator-expected runbook note), (3) **the perf pass that CI can prove**: startup-cost instrumentation (a cold-start budget trace), build-size report, deferred-init audit (nothing heavy before first frame; the boot snapshot read is already sync by design — ADR-018), and any found wins applied, (4) **crash-free posture verified** (Crashlytics dev-off/prod-on already shipped M1.3 — audit + document, don't rebuild). If the ADR-scale decisions turn out thin (this slice is mostly configuration + copy), a lightweight ADR or a documented-decisions section in the docs pass is acceptable — but any *irreversible* store-facing choice (app name, bundle display name, category, the name-under-the-discreet-icon question from operator item 2's M6.1 note) gets written down BEFORE it is baked into metadata, and the review-ordering rule applies to whatever design doc this session produces.

**Acceptance criteria:** `fastlane/metadata` TR/EN complete + validated (fastlane precheck-style lint where runnable without credentials); `release.yml` exists, is syntax-valid, runs its pre-signing stages green on a branch dispatch, and fails CLOSED with a clear message at the signing boundary when secrets are absent (never a silent skip that looks green); Info.plist carries the export-compliance key; the cold-start budget is measured in CI terms (instrumented trace or startup profile in the integration job) with the findings + any fixes documented; store copy ×2 locales drafted and flagged for native review; app + functions suites stay green (gates 68 / 80); all CI jobs green. **Store metadata copy joins operator item 1's native-review gate; the signed-build + TestFlight upload halves are recorded as operator-blocked (item 4 enrollment + signing secrets) — not silently deferred.**

**External dependencies (founder):** none required to BUILD the above. The signed-build/TestFlight half of the M6 accept line is **operator-blocked** (item 4: Apple Developer enrollment, promised 2026-07-08, still pending; then certificates/profiles per the TestFlight runbook). Flag it plainly at close; do NOT stretch the session waiting on it.

**Files likely to change:** `fastlane/**` (metadata TR/EN, Fastfile lanes), `.github/workflows/release.yml`, `app/ios/Runner/Info.plist` (export compliance), possibly `app/lib/main_*.dart`/bootstrap (deferred-init wins), `docs/` per docs-with-code; NO functions changes expected (spot lint/typecheck only if anything under `functions/` moves).

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); functions suite only if touched; `gh workflow run` the release lane on the branch to prove the pre-signing stages + the honest secrets-absent failure; push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium — configuration-heavy, but two traps: a release lane that silently no-ops at the signing boundary (must fail closed, loudly), and store copy that bakes in irreversible naming decisions without the founder (write them down first). **Estimated duration:** one session. If it overflows, the honest split is: metadata + Info.plist + the release lane this session; the perf pass next.

**Stopping conditions:** if the perf instrumentation cannot produce a meaningful cold-start number in CI (simulator-only measurement can be noise), record the honest bound — instrument, capture what CI can, and put the real-device measurement on operator item 4's on-device checklist rather than faking a pass. If store-naming decisions (display name vs the discreet icon's immutable label — operator-expected item 2 from M6.1) cannot be resolved without the founder, draft BOTH variants in metadata comments, pick the conservative one, and flag it loudly — do not block.

**Explicitly out of scope this session:** M5.3 unless item 6 is answered (then it REPLACES this objective); any deploy (Blaze — item 2); actual signing/upload (operator-blocked, item 4); screenshots (need the Mac/simulator polish era); Android (M6.5); hotline numbers (★); `coach_sessions` (item 7); the RC-REST reconciliation (item 0 + deploy era).

**M6.2 handoffs to keep in view (binding):** the seven cascade invariants in the standing toolchain note; the data-rights wire contracts are FROZEN; the deletion notice deliberately has NO push — do not add one while wiring anything notification-shaped; store metadata must not contradict the honest lock/discreet-icon copy (the icon image changes, the App Store name is a separate, founder-owned decision — operator item 2's note).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M6.5 Android enablement, or M5.3 if unblocked, or the first-deploy slice if the founder flips Blaze — at that point the 22-unit MVP engineering plan is COMPLETE except founder-blocked halves), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
