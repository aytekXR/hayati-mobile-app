# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is now gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9; the other six generate brand/marketing assets and brandkit v1.0 is final).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after** (S025 did exactly this with an inherited `.claude/settings.json` model pin — never "fix" a config you did not author). **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing.
>
> **Standing review-ordering note (Sessions 015–026, now FOURTEEN-for-fourteen pre-code / FIFTEEN consecutive passes):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S023: 2 blocking + 8 serious pre-code; S024: 1 blocking; S025: 4 blocking + 6 serious + 8 minor pre-code then 12 more post-diff; **S026: 26 raised, 1 blocking + 7 serious + 7 minor survived DOUBLE verification**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class (prefer deleting an over-claim to faking a mechanism); when two lenses disagree, the governing doc decides; **a skeptic pass can surface a DEEPER defect while confirming a shallow one** (S026: confirming that the digest's key set was unnamed, the adjudicator found that `D10.1` promised `legal*` protection the digest's own scope did not deliver); **MUTATION-CHECK every guard — and the TEST too**; policy that lives in a YAML `if:` is invisible to the test suite; a safety claim in a CODE COMMENT is a guarantee surface; a stale operator item is worse than no item. **S026 addenda: (1) a source sentinel must scan every SPELLING of the thing it forbids — `Tooltip` the class and `tooltip:` the parameter are the same crash, and `IconButton` builds one internally. (2) When a reviewer reports a count that contradicts yours, find out WHY before accepting it — S026's "145 files" was `__pycache__` written by the session's own probe runs, and the correct number was the original. (3) Five review lenses still missed a defect the author's own verification caught (the Phosphor divergence) — the review is a net, not a proof.**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent**. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4): the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay`/`Tooltip`/`tooltip:`/`IconButton`/text-selection field/`ScaffoldMessenger.of` in lock-screen children — and **since S027 invariant 4 finally HAS a mechanism**: `lock_screen_forbidden_api_sentinel_test.dart`, whose scan set is DERIVED from `lock_screen.dart`'s relative-import graph, so a shared widget mounted there is guarded automatically.** **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); **`--allow-empty-urls` is a ratchet**; the pre-frame bootstrap await set is PINNED by `entrypoint_sentinel_test.dart`. **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned; the **three-way legal-version source-sentinel** means a legal-text revision bumps ALL THREE in the same diff — and **M5.3 (the live LLM) is a RECORDED re-consent trigger**; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier; **it has NO VOTE on the build** (every error path `exit 0`s AND `continue-on-error: true` — both halves required); **ALL policy lives in the script, never in a YAML `if:`**; the secret is **repository-scoped, NOT `environment: release`**; **ci.yml's concurrency keys push events on the COMMIT (D8)** — do not "simplify" it back to ref-keying. **S026/S027/S028 (ADR-025) — the arc is SCOPED, its firewall is BUILT, and slice 1 shipped. S028's lesson is now standing: a golden matrix proves SCREENS, not transient surfaces — dialogs and snackbars mount above the screen and NO golden captures them, which is how a flat-on-flat dialog and a cream snackbar survived from M1.4 with a 303-file net in place. When a slice's fix lands on a transient surface, it needs a widget test, not a golden. The firewall: the lock-screen forbidden-API sentinel, the brandkit→Dart token-parity test (bidirectional; `iconography.*` deliberately unasserted until #63 resolves) and the 96-pair frozen-sentence digest over the ★ safety + `consent*`/`legal*` strings. A slice that turns any of them red has broken something, not found a nuisance.** Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.



## Objective — Session 029: **ADR-025 slice 2 — the product core.** The highest-value slice of the arc.

**FIRST, BEFORE ANY CODE — the preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter behind ADR-016's frozen wire contract). It has priority the moment it is unblocked — waiting since Session 018, the ONLY planned MVP session-unit left. **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR bumps the legal version (all three sources, same diff), names the provider in the revised notice/policy, re-gates every user, and adds the provider row to `docs/dpa-inventory.md` with the no-training requirement + its own KVKK SCC leg. **S027 note: the frozen-sentence digest covers every `consent*`/`legal*` string, so the notice rewrite turns it red — that is the digest working; re-stamp it deliberately, in the same diff as the version bump.**
2. **Blaze flipped (item 2)?** → the **first-deploy slice** (all eleven Functions to `hayatiapp-dev`; verify the Cloud Scheduler trigger, Eventarc retry and `RC_WEBHOOK_TOKEN` Secret Manager binding; inherit the ADR-019 backup-retention alignment). **Verify Blaze FACTUALLY** — `gcloud` is not on this box, so mint a token from the firebase-tools refresh token and GET `cloudbilling.googleapis.com/v1/projects/<id>/billingInfo` for **both** projects.
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
4. **On-device defect reported from the ★ recipe?** → triage preempts any planned slice.
5. **"Build the dev-rig slice"?** → fold in or re-scope.
6. **Answers on #67 (muted-text / outline tokens) or #63 (Phosphor)?** Neither blocks slice 2. **#67** would let a later slice finish `ColorScheme`; if answered, fold the two tokens + the parity-test extension into whichever slice next needs them, not into this one. **#63 = migrate** → slice 1.5, its own session and its own golden era.
If none fire: build slice 2 below.

**Why this is next:** ADR-025 D7 puts the product core immediately after the theme floor, and brandkit §9.3 is explicit — **"the reveal is the product; spend polish budget there."** It is also the biggest single block of golden coverage in the arc (66 of 301).

**Scope — 12 surfaces, all Class N:** `SoloHomeScreen`, `PairedHomeScreen`, and their sub-widgets: the question card / `_PairedQuestionView`, the answer entry `TextField`, `_PartnerSlotCard`, `_AnswerCard`, `_StreakRow`, `_PacksTile`, `_CoachTile`, `_InviteNudgeCard`, `_SoloCompletedView`, `SettingsGearOverlay`. Composition, hierarchy, spacing and motion — **not** copy, and **not** the reveal's server semantics.

**Acceptance criteria:** the reveal moment measurably improved (state it in the PR — what changed and why it reads better); every home state still renders every state it rendered before (the widget suite's state ladder is the proof, not the goldens); Appendix A asserted; the declared golden set re-baselined per D8 with `git status --porcelain` pasted beside the declaration; **the three slice-0 guards green and unweakened**; `flutter analyze` clean; full suites green; coverage ≥68.

**The traps, named so they are checked rather than discovered:**
- **The free-tier probes.** The coach tile and packs tile live INSIDE `PremiumGate`'s unlocked subtree, and there are tests asserting a free couple's goldens are byte-identical and that no `PaywallScreen` route is ever pushed from the answer flow. A restructure that hoists a tile out of the gate breaks a *product* guarantee, not a layout.
- **`surfaceContainerHighest` is now the whole container family** (slice 1). The home cards already read it explicitly; do not "modernise" them onto a different slot expecting a tonal step — the brandkit defines one raised tone, and slice 1 pinned all five slots to it.
- **Copy is frozen** (Class N): restructure freely, reword nothing. Any reword resets that key's native-review status and must be recorded in the same diff.
- **`onSurfaceVariant`/`outline` are still unset** (#67). If this slice finds itself wanting a muted tone, that is #67 and it STOPS — it does not invent one. Slice 1 tried exactly that and produced a measurable regression.

**Files likely to change:** `app/lib/features/daily_question/presentation/**`, the declared golden set, `docs/test-suite.md` if the golden matrix changes shape.

**Validation steps:** `cd app && flutter analyze && flutter test` · the emulator suite from the repo root · `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` · **declare the golden set BEFORE `--update-goldens`, then paste `git status --porcelain -- 'app/test/**/*.png'`** · PR, watch, squash-merge · **watch the post-merge MAIN run** (`integration-emulator` is main-only and this slice touches code, so it will actually run) · `codegraph sync`.

**Complexity:** high — the largest golden set in the arc and the surface the product is judged on. **Look at the regenerated images, do not just count them** (that is what caught slice 1's regression). A slice that improves a metric and worsens the screen has failed.

**Stopping conditions:** if improving the reveal requires a colour, a motion token or a type step the brandkit does not define, **STOP and record it** — slice 1's experience is the precedent: an invented value read as plausible and measurably worsened an interactive control. If a free-tier probe or a state-ladder test turns red, that is a product regression, not a test to update.

**Explicitly out of scope:** slices 3–8; any copy edit; #63's Phosphor migration; #67's token decision; M5.3 unless item 6 answered; any deploy; hosting/domain; M6.5; signing/TestFlight; screenshots; hotline numbers (★); `coach_sessions`; the legal documents; any legal-version bump.

**S028 handoffs (binding):** the slice-0 guards are live — **a slice that turns one red has broken something, not found a nuisance.** `onSurfaceVariant`/`outline` stay unset until #67 is answered. And slice 1's lesson generalises: **a golden matrix proves screens, not transient surfaces** — if a fix lands on a dialog, a snackbar, a sheet or a tooltip, it needs a widget test, because no golden will ever see it.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file (expected next: whichever preemption fires, else **ADR-025 slice 3 — onboarding & pairing**, 87 goldens, and the one that must keep `ProviderActions` a single shared widget), refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
