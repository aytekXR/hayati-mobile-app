# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is now gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9; the other six generate brand/marketing assets and brandkit v1.0 is final).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after** (S025 did exactly this with an inherited `.claude/settings.json` model pin — never "fix" a config you did not author). **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing.
>
> **Standing review-ordering note (Sessions 015–026, now FOURTEEN-for-fourteen pre-code / FIFTEEN consecutive passes):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S023: 2 blocking + 8 serious pre-code; S024: 1 blocking; S025: 4 blocking + 6 serious + 8 minor pre-code then 12 more post-diff; **S026: 26 raised, 1 blocking + 7 serious + 7 minor survived DOUBLE verification**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class (prefer deleting an over-claim to faking a mechanism); when two lenses disagree, the governing doc decides; **a skeptic pass can surface a DEEPER defect while confirming a shallow one** (S026: confirming that the digest's key set was unnamed, the adjudicator found that `D10.1` promised `legal*` protection the digest's own scope did not deliver); **MUTATION-CHECK every guard — and the TEST too**; policy that lives in a YAML `if:` is invisible to the test suite; a safety claim in a CODE COMMENT is a guarantee surface; a stale operator item is worse than no item. **S026 addenda: (1) a source sentinel must scan every SPELLING of the thing it forbids — `Tooltip` the class and `tooltip:` the parameter are the same crash, and `IconButton` builds one internally. (2) When a reviewer reports a count that contradicts yours, find out WHY before accepting it — S026's "145 files" was `__pycache__` written by the session's own probe runs, and the correct number was the original. (3) Five review lenses still missed a defect the author's own verification caught (the Phosphor divergence) — the review is a net, not a proof.**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent**. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4): the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay` in lock-screen children — and (S026) invariant 4 is the ONE of the four with NO mechanism, which is exactly what issue #61 fixes.** **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); **`--allow-empty-urls` is a ratchet**; the pre-frame bootstrap await set is PINNED by `entrypoint_sentinel_test.dart`. **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned; the **three-way legal-version source-sentinel** means a legal-text revision bumps ALL THREE in the same diff — and **M5.3 (the live LLM) is a RECORDED re-consent trigger**; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier; **it has NO VOTE on the build** (every error path `exit 0`s AND `continue-on-error: true` — both halves required); **ALL policy lives in the script, never in a YAML `if:`**; the secret is **repository-scoped, NOT `environment: release`**; **ci.yml's concurrency keys push events on the COMMIT (D8)** — do not "simplify" it back to ref-keying. **S026 (ADR-025) — the UI/UX arc is SCOPED and its firewall is binding before any pixel moves.** Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 027: **ADR-025 slice 0 — build the invariant firewall.** Three guards, mutation-checked. No pixels move.

**FIRST, BEFORE ANY CODE — the preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter behind ADR-016's frozen wire contract, replacing the fail-closed `UnconfiguredCoachProvider`). It has priority the moment it is unblocked (waiting since Session 018 — the ONLY planned MVP session-unit left). **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR must bump the legal version (all three sources, same diff), name the provider in the revised notice/policy, re-gate every user, and add the provider row to `docs/dpa-inventory.md` with the no-training contractual requirement + its own KVKK SCC leg.
2. **Blaze flipped (item 2)?** → do **the first-deploy slice** instead: script the first deploy of all **eleven** Functions to `hayatiapp-dev`, verify the deploy-only pieces (Cloud Scheduler trigger, Eventarc retry, `RC_WEBHOOK_TOKEN` Secret Manager binding), inherit the ADR-019 backup-retention alignment. **Verify Blaze FACTUALLY**, never from the checklist's memory — S026's method, since `gcloud` is not installed on this box: mint a token from the firebase-tools refresh token in `~/.config/configstore/firebase-tools.json` and GET `https://cloudbilling.googleapis.com/v1/projects/<id>/billingInfo`, checking `billingEnabled` on **both** projects. Deploy-ordering note (ADR-023 D4): the Functions legal-version constant deploys before or with any app binary that raises the gate's expectation.
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
4. **Has the founder reported results from the ★ direct-install on-device test?** A failing M6.1 lock check, a broken deep-link delivery, or a phone-auth native crash log (#15 on real hardware) **preempts** — triaging a reported on-device defect beats any planned slice.
5. **Has the founder said "build the dev-rig slice"?** (`lib/main_demo.dart` on the integration tests' `demo-hayati` bootstrap + an on-demand day-doc seeder, so the **join** and the **couple daily loop** run on the phone rig pre-Blaze.) If yes, fold it in or re-scope to it.
6. **Has the founder answered issue #63 (Phosphor vs Material icons)?** If they chose **migrate**, that becomes slice 1.5 and is scheduled AFTER slice 0 and slice 1 — it does not preempt, but record the answer. If they chose **amend the brandkit**, this session may fold the brandkit §5 amendment into its diff (it is docs-only and touches no pixels).
If none fire: build slice 0 below.

**Why this is next:** ADR-025 (committed, rev 2, adversarially reviewed) makes slice 0 the **precondition for every other slice** — "until these exist, no slice may proceed." Two of its three guards close *pre-existing* holes that are worth fixing on their own merits regardless of the refactor: **issue #61** (the lock screen's no-dialog constraint — ADR-018 invariant 4, the only one of the four with no mechanism, where a violation crashes the app on the lock screen and *the crash is the lockout*) and **issue #62** (brandkit→Dart token drift — "the brandkit decides" currently rests on an unchecked hand-copy). This is the highest-value unblocked work in the repo: it is safety work wearing a refactor's clothes.

**Scope — exactly three guards plus their mutation checks. ADR-025 D5 specifies all three in implementable detail; follow it, do not re-derive it.**
1. **The lock-screen forbidden-API source sentinel** (#61). Explicit two-file scan set (`lock_screen.dart`, `widgets/pin_keypad.dart`), comment lines stripped first (the header comment *names* the forbidden calls in prose — the `no_invalidate_sentinel_test.dart` precedent), the full ADR-018 D3 list **including `tooltip:` in its lowercase parameter form, `IconButton` outright, and the text-selection family** (`SelectableText`/`TextField`/`TextFormField`/`EditableText`), plus a **sentinel-of-the-sentinel** asserting the scan set still holds both paths and both files exist. Add the `// SENTINEL SCAN SET` comment to both scanned files.
2. **The brandkit token-parity test** (#62). The JSON↔Dart mapping table in ADR-025 D5.ii is the spec, **including its four deliberate non-assertions** (`minimumBodySize`, `dynamicTypeMax`, `iconography.*`, `rules[]`) and the derived-spacing rule (`x2..x8` asserted as exact multiples of `spacing.grid`, so a hand-edit of `x3` still fails).
3. **The frozen-sentence digest.** SHA-256 over `UTF-8(sorted("<locale>.<key>=<value>\n"))` for the five named ★ keys + every `consent*` + every `legal*` key × 3 locales — **96 pairs today**, verified. Assert the five ★ keys still exist by name.

**Acceptance criteria:** all three guards land and are **each mutation-checked by hand** (neuter → confirm exactly its own test turns red → restore) — and for the sentinel, **once per forbidden string, including the `tooltip:` parameter form**, because a mutation that only inserts `showDialog` proves the sentinel scans, not that its list is complete (the S025 lesson: mutation-check the TEST, not just the guard). `flutter analyze` clean · full app + functions suites green · coverage gate ≥68 satisfied · **ZERO golden PNGs changed** (slice 0 renders nothing differently — this is itself the check that slice 0 stayed in scope) · the three issues (#61, #62) closed by the PR · `docs/test-suite.md`'s "pending" wording on the slice-0 guards flipped to shipped in the same diff (rule #8).

**External dependencies (founder):** none. Issue #63 (Phosphor) is a founder decision but **does not block** — the token-parity test deliberately does not assert `iconography.*`.

**Files likely to change:** `app/test/features/privacy_lock/presentation/lock_screen_forbidden_api_sentinel_test.dart` (new), `app/test/core/design_system/brandkit_token_parity_test.dart` (new), `app/test/core/l10n/frozen_sentence_digest_test.dart` (new), `app/lib/features/privacy_lock/presentation/lock_screen.dart` + `widgets/pin_keypad.dart` (the `// SENTINEL SCAN SET` comments **only** — no behavioural change, no pixel change), `docs/test-suite.md`, `docs/adr/025-*` (status note only if a guard's spec needs a recorded correction — an ADR is immutable once accepted, so a real change is rev 3 with the reason).

**Validation steps:** `cd app && flutter analyze && flutter test` · the full emulator suite from the repo root · `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` · **`git status -- 'app/test/**/*.png'` must be empty** · push a PR and watch it (`gh pr checks --watch`) · squash-merge · **then watch the post-merge MAIN run** (session-rules §3.5 — since ADR-024 D8 the close commit no longer cancels it, but it concludes after your attention moves on; the Slack notifier will report it **only once the founder sets the webhook**, operator item 5) · `codegraph sync`.

**Complexity:** medium. The trap is writing a guard that passes its own test while the thing it guards stays broken — which is exactly the defect class the S026 review found *inside the ADR*. The second trap is scope: this session adds tests and two comments. **If you find yourself editing a widget's layout, you have left slice 0.**

**Stopping conditions:** if the token-parity test cannot be written against the real files without inventing a mapping the ADR did not specify, **STOP and record it** — ADR-025 D5.ii claims the mapping is fully specified, and if it is not, that is a rev-3 correction, not a field decision. Likewise if the brandkit JSON and the Dart constants turn out to **disagree today**: that would make slice 0 a non-no-op and it is a finding, not something to quietly "fix" in either direction (the brandkit is the constitution — but which side drifted is the founder's call if the divergence is visual).

**Explicitly out of scope this session:** every one of slices 1–8 — **no theme change, no `ColorScheme` slot, no component sub-theme, no widget-tree edit, no copy edit, no golden re-baseline**; M5.3 unless item 6 answered (then it REPLACES this); any deploy (item 2); hosting/domain; M6.5; signing/TestFlight (item 4); screenshots; hotline numbers (★); `coach_sessions` (item 7); editing the six review-PENDING legal documents; any legal-version bump; the Phosphor migration (#63 — founder's call, and it is slice 1.5 at the earliest).

**S026 handoffs to keep in view (binding):** ADR-025's **D10 twelve prohibitions** and the **F/G/N/S surface classes** govern every later slice — read them before slice 1, not during it. `.claude/skills/` is gitignored and **only `ui-ux-pro-max` may be invoked here** (D9). **Slice 1 must prove the lock goldens BYTE-IDENTICAL** — `hayatiTheme` is built above `PrivacyGuard`, so an "app-wide re-baseline" would otherwise launder a Class F change past its own rule. And the honest gaps ADR-025 recorded rather than faked: the broad native-review gate has no mechanism, D8's golden declaration is review discipline and not a CI gate, and D5.i's scan set is hand-maintained — a sentinel-of-the-sentinel can stop it shrinking but cannot know what it should grow to include.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: whichever preemption fires first — M5.3 on item 6, the first-deploy slice on Blaze, M6.5 on the founder's Android go — else **ADR-025 slice 1, the Material default floor**), refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
