# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing toolchain note (M2.1–M2.4):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`) — the functions emulator never compiles TS. Full suite: `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'` from the repo root. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id, `demo-hayati`). Widget-test gotcha (M2.4): rootBundle loads never complete inside the fake-async zone — asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`; plain `test()` cases keep proving the real pubspec asset wiring.

## Objective — Session 011: M3.1 — question-packs pipeline: schema enforcement + validator + generalized bundled loading (the daily-question data spine)

First slice of M3 (`implementation-plan.md`). M2 closed in Session 010: the solo home ships day-N questions from three bundled schema-shaped packs (`app/assets/content/solo_{tr,ar,en}.json`) through a `SoloQuestionPackRepository` seam, with `content/schema/question-pack.schema.json` still a declared stub and `content/validator/validate.dart` a placeholder that exits 1. This session turns the content contract into enforced reality — the data spine the M3.2 rollover Function and question surfaces stand on:

1. **Validator (real, replacing the stub):** `content/validator/validate.dart` enforces the schema (all fields, patterns, enums, depth bounds) plus the checks JSON Schema can't express: question-id uniqueness ACROSS packs, `packId`↔filename↔`locale` consistency, register vocabulary, `reviewedBy` presence flagged for shippable packs (warning tier pre-launch per ADR-007/W9 — the solo packs legitimately carry `PENDING…`). Validates BOTH `content/packs/` and the bundled `app/assets/content/` copies. Unit-tested (pure core, thin IO shell) per `test-suite.md` §1 "pack validator".
2. **CI wiring:** a cheap ubuntu step in the `quality` job runs the validator on every push/PR (docs `test-suite.md` §2 + `ci.yml` comment); a red pack blocks merge.
3. **Authoring home unification (ADR-009 promise):** authoring moves back under `content/packs/` with a deterministic sync into `app/assets/content/` (script or validator `--sync` mode — decide + document; CI must fail if the two drift). Delete `content/packs/en.example.json` in favor of real packs, or keep it explicitly non-shippable — decide + document.
4. **Generalize the pack model for M3 (rename/promote, not rebuild):** the M2.4 `SoloQuestion*` model/DTO/loader become the daily-question feature's general pack spine (e.g. `QuestionPack` + `seasonalWindow` surfaced + register carried), keeping the solo path working unchanged (solo packs are just packs with 7 questions). No couple-pack CONTENT authoring this session beyond what tests need (the 400/300/300 bank is founder/content-ops work, W9).
5. **Docs-with-code:** `architecture.md` §2/§3 (content pipeline real), `content/README.md` (stub language removed), `test-suite.md` §1/§2, `implementation-plan.md` (M3 progress line), ADR only if a decision is genuinely weighed (the sync-direction decision may warrant ADR-010).

**Acceptance criteria:** `dart run content/validator/validate.dart` (exact invocation decided in-session) exits 0 on the shipped packs and non-zero — with a field-precise message — for each violation class (schema field, duplicate id across packs, locale/filename mismatch, drifted app copy); the validator runs in the ubuntu `quality` job and a deliberately broken pack turns CI red; solo mode still works end-to-end (existing 591-test suite green, solo goldens unchanged); pack loading is served by the generalized model with the solo path as a thin specialization; Flutter gate stays ≥64; all CI jobs green.

**External dependencies (founder):** none (emulator-free session — validator + app-side only). The Blaze/Mac items remain optional-bonus (see `docs/operator-expected.md`).

**Files likely to change:** `content/validator/validate.dart` (+ new `content/validator/` tests — decide runner: `dart test` needs a pubspec; a self-checking script or a small `tool/`-style test is the timebox-friendly path), `content/schema/question-pack.schema.json` (de-stub the description), `content/packs/**`, `app/assets/content/**` (sync output), `.github/workflows/ci.yml` (one ubuntu step), `app/lib/features/daily_question/{domain,data}/**` (model generalization + renames), matching tests, `docs/*`, this file.

**Validation steps:** full Flutter gate sequence (`dart format` → `flutter analyze` → `dart tool/rtl_lint.dart app/lib` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 64 app/coverage/lcov.info`); validator self-tests; functions suite only if rules/functions change (not expected); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main.

**Complexity:** medium-low — pure Dart tooling + refactor-with-tests; the risky bit is scoping the model generalization so it stays a rename-plus-surface, not a speculative M3.2 design. **Estimated duration:** one session (1–3 h). If it overflows: land validator + CI wiring first (the enforcement spine); slice the model generalization to M3.1b per session-rules §1.4.

**Stopping conditions:** if the validator-as-standalone-Dart-script fights tooling (no pubspec for `content/`) beyond a 30-min timebox, move it under `tool/` (same pattern as `coverage_gate.dart` — already proven to run from the repo root in CI) and document. If the generalization ripples wider than `features/daily_question/` + tests, stop at the seam and note the remainder.

**Explicitly out of scope this session:** the rollover Function and timezone buckets (M3.2); remote pack sync/Remote Config; couple-pack content authoring at volume (W9 content-ops); register-aware pack resolution UI; streaks; push notifications; `invitePreview.questionText` (arrives with the daily-question loop, M3.2+); any Functions deploy; issues #15 (Mac) and #13 (Android, M6.5).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M3.2 — daily rollover Function: timezone buckets, deterministic register/seasonal selection, `couples/{cid}/days/{yyyymmdd}` writes + rules), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
