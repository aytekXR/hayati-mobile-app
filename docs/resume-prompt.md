# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore` MCP tool (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder — **but do not hand-roll that walk from `/proc/<pid>/stat` field 4: `comm` contains spaces, and S056's version printed 20,000 characters of garbage before it was killed. Use `ps -eo pid,ppid,args` and read the tree.** **The other claudes on this box work OTHER repos (`evrak`, `repo`(parent), `ams-pulse`, `unhooked`, `ai-videos`, `yanki-mvp`)** — confirm by walking cwd before treating one as a conflict, **and a leftover background `bash` from your OWN pre-`/clear` session will show up as a child of your own claude PID** (S051 saw one sleeping 15 hours; **S053 killed another that had been spinning for 25; S056 killed TWO `until [ "$(gh …)" ]` poll loops that had been hammering `gh` for ~2 days**). **S038/S053 addendum: a CONCURRENT session on ANOTHER machine/tree can merge to `main` mid-session AND consume your session number.** `ps`/`tmux` only cover THIS box, so **re-derive the session number and the queue from `git log` + `gh issue list`, never from this file's prose.** **Checkpoint-commit implementation output IMMEDIATELY** — S056 opened on **38 files and ~1,400 insertions** of finished, tested work sitting uncommitted in a working directory, which is the same risk one `git checkout .` away from total loss. **S037 addendum: REVIEW workflows can mutate the tree** — after every review workflow returns, `git status` must be EMPTY before you commit anything.
>
> **Standing review-ordering note (Sessions 015–053, TWENTY-EIGHT consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are. Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff.** Aggregate so a finding surfaces when EITHER verifier says real. **Check `agents_error`/`agents_empty_result` before trusting a verdict distribution — an empty verdict is *unverified*, and the tooling renders it as the opposite** (S041; S053 read two empty transcripts before believing them). **Rebase onto latest `main` before sending a diff to the review workflows** and re-check at merge time (founder directive, S042). **S053 addendum: cap your verify fan-out with a `log()` of what you DROPPED.** *S056 note: this session ran no review workflows — it inherited a finished slice and its job was to verify, commit and ship it. Verification-of-inherited-work is a different task from review-of-your-own-design; say which one you are doing.*
>
> **S056 addendum — (64) A STALE FACT INSIDE AN INSTRUCTION IS FAR WORSE THAN A STALE FACT IN PROSE.** `operator-expected.md` item **2(c)** was a 🔴 four-line recipe telling the founder to submit **build 110** to Beta App Review. The gap it named was still exactly right (four contact fields). The **build** was catastrophically wrong: `git merge-base --is-ancestor 6d1f736 fa990e6` exits **1**, so build 110 predates ADR-036 *and* ADR-039 — it **is** the build with the endless loading screen and the unclickable `hayati://` link. Following the page would have spent a 24–48 h Apple review handing five real people the exact build behind both bug reports. The number was correct when written and was never re-derived. **Addendum 45 (query the platform, not the docs) applies hardest where a doc contains a COMMAND: prose that is stale merely misleads, but an instruction that is stale gets EXECUTED. Re-derive every identifier inside every runnable block you leave behind — and prefer "the newest VALID build" to a number.**
>
> **S056 addendum — (65) AN EMPTY RESULT FROM A TOOL IS UNVERIFIED, NOT NEGATIVE — AND THAT NOW HAS THREE INSTRUMENTS.** The S055 addendum-61 check (prove your new tests actually RAN) was run against CI and returned **0 matching lines**, which reads exactly like "the four suites did not execute". They had. `gh run view <run> --job <id> --log` silently returns nothing; `gh api repos/…/actions/jobs/<id>/logs` returned all four assertions. Same shape as S041's empty verdicts and S051's masked probe error. **When a check returns nothing, prove the CHECK works before believing its answer — a control that passes, or a second instrument.**
>
> **S056 addendum — (66) VERIFYING WHAT YOU DID *NOT* CHANGE IS PART OF CHANGING SOMETHING.** ADR-040 removed the universal-links entitlement, so a tapped invite now opens the browser instead of the app. The obvious way that ships a lie is share copy promising a tap that no longer happens — and the copy was never in the diff, so no reviewer of the diff would ever look at it. Checked: all three locales already read *"get the app, then enter the code"* with the link trailing, so nothing moved. **After any change that alters what the product DOES, enumerate the strings and docs that DESCRIBE it and read them. A guarantee can be falsified by a file you never opened.**
>
> **S056 addendum — (67) THE GLOB YOU REASONED ABOUT vs. THE DEPLOY YOU MEASURED.** `firebase.json`'s ignore list contains `"**/.*"`, and `.well-known/` starts with a dot — the well-known Hosting footgun that silently drops an AASA. It would have been easy to "fix" it from first principles, and equally easy to assume it was fine. Neither: deploy, then `curl`. `found 4 files`, AASA served **200 `application/json`**. **Glob semantics are a vendor implementation detail, so only the vendor can answer** (addendum 63's rule, in a second vendor).
>
> **S056 addendum — (69) `continue-on-error` IS NOT THE BUG. AN UNREAD FAILURE IS. AND "THE SCRIPT RAISES" IS A DIFFERENT FAILURE FROM "THE SCRIPT NEVER STARTED".** ADR-037 promised that every release build reaches the external `Friends` group automatically. **It never once did.** Build 112's release was the first run to reach that step at all (build 110's predates the ADR) and it died on `ModuleNotFoundError: No module named 'jwt'` — `sign-upload` had no `actions/setup-python`, so bare `pip install` and `python3` were **different interpreters** on `macos-15`; the install printed a success notice, the import did not exist, and `continue-on-error` carried the whole release green while **no build was attached to anything.** Two compounding lessons. **First:** the non-blocking posture was correct (Apple's queue is a third party's schedule) but a tolerated failure still needs a **reader**, and this one had none — measured only because Apple was asked directly and answered `groups: founders`. **Second, and sharper:** ADR-038 D4 had **predicted this exact sentence** — *"silently repealing ADR-037's guarantee … while `continue-on-error` kept the release green"* — for the wrong cause, **one layer too high.** Every guard there assumed the tool *runs*, and its hermetic test imports `jwt` from the dev box's own environment, so it could not have caught this in either direction (addendum 63, one level down the stack). **When you harden a step, enumerate the failures BELOW your abstraction: unimportable dependency, wrong interpreter, missing binary, PATH order. Prefer `python3 -m pip` over `pip` always — it makes installer and runner the same by CONSTRUCTION — and assert the import before any long wait.**
>
> **S056 addendum — (68) NAME THE DEFERRAL, AND NAME WHY IT KEEPS WINNING.** #140 has now been deferred **four sessions running** (S054, S055, S056 ×1 objective + this file), every time for something *visibly* on fire. #140 is a gate against something *silently* wrong. **That asymmetry is #140's own subject matter, which makes the deferral pattern evidence for the issue rather than an excuse against it.** A queue item that loses to every emergency needs its priority argued explicitly, not re-inherited.
>
> **Earlier addenda that keep paying (condensed):** **the verifier panel is an INPUT to judgement, not a substitute for measuring** (S051, wrong in both directions twice in one session); **a probe whose control passes is a broken probe** (S051, S053); **your own ADR is a claim surface you will falsify with your own code — re-read the WHOLE ADR after every code change, hunting the PARAPHRASE and the NEGATION** (S051 ×4, S053 ×3); **a falsified declaration is a finding — sort the causes before accepting any of them** (S051); **N EXPERT SWEEPS CAN ALL MISS THE SAME THING — budget a completeness critic into every fan-out** (S050, S053); **a remainder deferred into prose is a remainder that gets lost**; **a premise that was replaced rather than measured is likely wrong again** (S049); **a new rule can make old rows vacuous — re-run every row** (S048); **a test whose fixture is derived from its subject proves nothing** (S047/S050); **MUTATION-CHECK every guard AND the test, in both directions** (S042, S053); **verify with the command CI runs, not the convenient one** (S044, S053, **S056** — `dart format`, `flutter analyze`, `rtl_lint`, `flutter test --coverage`, `coverage_gate`, `build_site_test.py` all run locally exactly as `ci.yml` invokes them, from the same working directories); **`$?` after a pipe reads the PIPE's status** (S047, S051, S053 — **use `${PIPESTATUS[0]}`**); **query the PLATFORM, not the docs** (S045, S053, **S056 — and see addendum 64**); **read the ARTEFACT, not just the source** (S047, S051); **run the session, do not assert its conclusion** (S045); **a RENAME is a copy rewrite that can collide with the product's own vocabulary** (S054); **a GATE WRITTEN IN ONE LANGUAGE GUARDS ONE LANGUAGE** (S055); **a TEST THAT IS NOT REGISTERED IS A GREEN RUN THAT PROVES NOTHING** (S055 — and S056 proved registration by grepping the CI log, see addendum 65); **only the VENDOR can refute a vendor API shape** (S055); **"no unblocked engineering" is a claim to RE-DERIVE every session**; a stale operator item is worse than no item.
>
> **Standing toolchain note:** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin`) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **FROM THE REPO ROOT** (echo the exit code — and beware that `; echo $?` after a pipe reads the *pipe's* status): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **The emulator suite binds fixed ports and is NOT safe to run concurrently — including with your own review agents** (check `ss -ltn` for 8080/9099/5001; forbid emulator runs in workflow-agent prompts). *S056 found two orphaned `functions/node_modules/.bin/firebase-functions` node processes from an earlier session, bound to no port — harmless, but kill them before an emulator run.* **Also forbid workflow agents from running `flutter`/`dart` at all, and from any package-manager command that WRITES** (`npm install|ci|audit fix|update|dedupe`). **`gcloud` is NOT installed on this box and there is no ADC** (re-confirmed S056) — Scheduler/Eventarc state cannot be verified from here; say so rather than asserting it. **The `firebase` CLI IS logged in as the founder** (`aaytekinerdogan@gmail.com`) with access to `hayatiapp-prod`/`hayatiapp-dev` — S056 used it to deploy live Hosting. That is a LOCAL path; `FIREBASE_SERVICE_ACCOUNT` is still **unset** (re-checked S056), so CI cannot deploy. **Run `flutter gen-l10n` in `app/` before trusting any test or analyze run that touches localized text**. Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). **Run `dart format` before every commit** — CI runs `dart format --set-exit-if-changed app/lib app/test tool content` **from the repo root**. Coverage gates: **app 68** (measured **87.42%** at the S056 close), **functions 80 hard / 85 target** (measured **97.22%** at the S053 close). Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers. Theme gotcha: global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. **`hayatiTheme` is now MEMOIZED (ADR-039 D7)** — it returns the same `ThemeData` instance per language code, so do not assume a fresh object. Goldens are Linux-canonical: **360 tracked PNGs across 24 directories**, re-measured at the S056 close (`git ls-files 'app/test/**/*.png' | wc -l`).
>
> **Standing binding-invariants note:** **M6.1 (ADR-018 rev 4)** the four lock invariants; **M6.2 (ADR-019)** the seven cascade invariants, deletion notice sends NO push, export `formatVersion` 2; **ADR-023** consent/legal is BINDING — `users.consent` server-owned, the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in one diff, `docs/legal/` byte-synced to `app/assets/legal/` under a drift test, withdrawal is PROSPECTIVE by DV doctrine; **ADR-024** `tool/ci/slack_notify.sh` is the single notifier with NO vote on the build and **ALL policy in the script** (D1); **ADR-025** the slice-0 firewall stays live and **D8's golden declaration is discipline, not a CI gate**; **ADR-026** the `seasonalWindow` vocabulary is CLOSED and gated in FIVE readers — adding a season is a five-file change, **but only FOUR of the five are parity-guarded: that is #130**; **ADR-032** release signing is fastlane `match` + MANUAL, the build NAME comes from pubspec while the build NUMBER is CI-synthesized (`100 + GITHUB_RUN_NUMBER`), and `fastlane/metadata/*/name.txt` is PINNED to **İkimiz** — enforced per-PR by `tool/release_lane_lint.dart`; **ADR-033** bidi isolation is applied at the **string boundary** and **at render only** — nothing persisted, exported or shared may carry `U+2068`/`U+2069`, chrome is deliberately NOT isolated; **ADR-034** advisories are gated on what a change **INTRODUCES**, never on the tree's absolute set — no baseline file, no cron, no Slack routing, base-ref selection is policy that lives in the TOOL not the YAML, fail-closed (exit 2) on its own failure; **ADR-039 (new, S056)** the boot is FAIL-OPEN and always ends in a frame — ADR-022's four-await sentinel still binds and a `try` adds no `await`; **every blocking wait on the launch→paired path is BOUNDED** (`kSlowLoadThreshold` 8s, `kInteractiveSignInTimeout` 2min, `kInvitePreviewTimeout` 15s) and every blocking screen carries an exit; the invite-link host set `kInviteLinkHosts` is **CLOSED** (membership, not path shape, is what makes a URL an invite) and the emitted host is one boolean `kInviteLinkUsesCustomDomain`; `--invite-only` publishes **no legal document at all**, which satisfies the placeholder gate by construction and is deliberately NOT channel-gated; **ADR-040 (new, S056)** the `associated-domains` entitlement is **deliberately ABSENT** — restoration steps live inside `app/ios/Runner/Runner.entitlements`, the AASA is already served and correct, and the app still PARSES all three hosts so links sent meanwhile start working the moment a claiming build installs.

## Objective — Session 057: **#140 — nothing in CI compares what is MERGED to what is DEPLOYED. Firestore rules sat un-deployed for 18 days behind six green milestones, and a founder bug report found it, not a gate.**

> ### ⚠️ FIRST: a founder handoff is pending and SUPERSEDES this objective if it has arrived.
>
> At the S056 close the founder said: *"I will give phone names and emails later."* Those are the **five friends** for the `Friends` TestFlight group, and possibly also the four `ASC_REVIEW_CONTACT_*` values (a phone, a name, an email — the same shape). **If that list is now in hand, the session is: set the four secrets, dispatch the review-contact write + newest-build assignment + submission, and add the testers.** That is a founder directive with five real people behind it and it outranks everything below. `docs/operator-expected.md` item **2(c)** is the recipe — read the **S056 correction box at the top of it first**, because the pre-S056 text aims at the wrong build.
>
> **#140 has now been deferred FOUR sessions running** (S054, S055, S056 — each time for a founder directive, each time correctly). See addendum 68: the reason it keeps losing is that it guards a *silent* failure while everything that beats it is *visibly* on fire — which is the issue's own argument, not a counter-argument. **Re-read #140 and re-measure before designing. Do not inherit this framing either.**

The symptom was remediated by S052 (both projects deployed from `fa990e6`; prod ruleset `3702186d`, dev `fbae0b36`). **The defect was not.** Nothing deploys rules, and nothing compares deployed-to-merged for rules *or* for Functions code.

`ci.yml`'s `functions-rules` job proves `firestore.rules` against the emulator, honestly, on every PR. But **the emulator loads the working-tree file.** The job guards a ruleset the runtime never reads. **A green check that guards nothing is worse than no check, because it is also a claim.**

### Acceptance criteria

1. **Measure the gap before designing for it.** For **both** projects: what ruleset is live, and does it match `firestore.rules` on `main`? The releases API is `firebaserules.googleapis.com/v1/projects/{p}/releases`. **Use the logged-in `firebase` CLI first** and say plainly which of the two questions — "what is live" and "can CI ask" — it answers. `gcloud` is still NOT on this box and there is no ADC. **`FIREBASE_SERVICE_ACCOUNT` is still unset**, which is now a *measured* constraint on criterion 2 rather than a guess: S056 confirmed it by deploying Hosting from the local CLI precisely because CI could not.
2. **The comparison instrument.** A check that detects drift between the released ruleset and `main`'s `firestore.rules`. **Argue its trigger and its vote against ADR-034 and ADR-024 before building it.** The asymmetry that decided ADR-034 does **not** obviously apply: drift between merged and deployed **is** caused by our own action or omission, unlike a third party publishing an advisory. That is an argument *for* a vote. Make it explicitly, or reject it explicitly.
3. **Decide the deploy path, which is the actual fix.** A drift *detector* still leaves rules deployed by hand. `deploy-site.yml` is the repo's worked example of a dispatch-only deploy lane with a fail-closed secrets gate — follow its shape rather than reinventing it. **Prod must be dispatch-only and the founder told before it is ever fired.** Dev is a session's to exercise.
4. **Mutation check.** Prove the detector goes red on seeded drift, not merely that it runs. The credential-free half belongs in `quality` with the other self-tests — `tool/ci/build_site_test.py` and `tool/ci/npm_audit_delta_test.py` are the two freshest examples of the shape. **And verify the new tests are actually REGISTERED — then verify your VERIFICATION works** (addendum 65: `gh run view --job --log` returns nothing at all; use `gh api repos/…/actions/jobs/<id>/logs`).
5. ADR + `docs/architecture.md` §9 + `docs/test-suite.md` §2 in the same diff (rule 8). **Write the ADR LAST** (rule 45).
6. Close **#140** with the PR. If the honest slice is detector-now / deploy-path-next, **split it and say so** rather than stretching the session.

**Design-review before the code.** The question worth adversarial attention: **is a deployed-vs-merged check a guarantee, or does it just move the trust problem to whatever runs the check?** A detector that reads a credential CI holds is only as good as that credential's scope, and a detector nobody deploys behind is the same gap one level up.

### Then, in priority order

**#146 — two external beta groups, and only one gets builds.** `Friends` (auto-assigned by `release.yml`, ADR-037) and `arkadaslar` (the founder's, pre-existing). Anyone in `arkadaslar` and not in `Friends` receives nothing, with every check green — the #140 shape in TestFlight. S056 re-confirmed from Apple that all three groups exist (`founders` internal, `arkadaslar` external, `Friends` external ← target) but `--status` prints **build→groups**, not **group→testers**, so the founder still cannot see who is where. The work: teach `print_status` to report each group's **membership** (read-only, one API call per group), then hand the founder the list. Re-inviting a real person emails them, so that half is theirs. **Pairs naturally with the founder handoff above.**

**#130 — make ADR-026's "five readers" claim true by construction.** Properly TDD-shaped: the failing test *is* the fix. All five readers do reject an unknown value; what is missing is the **parity net**. `content/schema/question-pack.schema.json` is the source of truth; `validator_core.dart` and the two TS readers are parity-tested against it; **`app/lib/features/daily_question/domain/question.dart:32` is NOT**. And the test that looks like the fifth guard is **self-referential**: `question_pack_dto_test.dart:62` iterates `knownSeasonalWindows` — the very list under test. **The failure it permits:** add a season to the schema, validator and TS parser; forget the Dart file. CI fully green, and the app throws `FormatException` at pack-load **on a real device**. Acceptance: (1) a failing test first — `seasonal_window_parity_test.dart` reads the schema, extracts the enum, asserts **set-equality** with `knownSeasonalWindows`; follow `brandkit_token_parity_test.dart`. (2) **Widen it** — `category` and `register` have the identical comment-instead-of-guard shape; one parity test over **every enum in the schema** closes all three, and any enum deliberately not mirrored gets its reason **in the test**. (3) **MUTATION-CHECK both directions** and re-run the neighbouring DTO tests. (4) Correct ADR-026 D3's wording in the same commit, strikethrough-plus-dated-note style. (5) Close #130. **Design-review the plan first:** is set-equality right, or should the app's list be permitted to be a strict subset? A reader that knows fewer seasons fails *closed*; one that knows more fails *open*. Not symmetric — encode the answer deliberately.

**#129** — delete `release.yml`'s false `Gemfile.lock` comment (uncontroversial), and decide on `--frozen`. Read the issue's honest scoping first: **no doc ever claimed the release lane installs frozen**, so this is the S044 lesson applied to the producer. The `--frozen` half should land on a run someone is watching, which pairs it with **#121**. *S056 note: #121's "it will FAIL until operator 2(d) is done" premise is now DEAD — ADR-040 removed the entitlement, and S056 dispatched the release lane successfully without it. Re-derive #121 rather than inheriting that blocker.*

**#137** — `intl`'s first-strong ranges miss Arabic Extended-A **and misclassify it as LTR**, so the bidi seam silently no-ops for it in LTR chrome. Not reachable in Turkish or Gulf Arabic. Filed because it fails **quietly**; carries a `// DEBT:` comment. Fixing it means diverging from `intl`, which needs its own guard — read the issue's three options before choosing.

**#136** — the Functions-side twin of #133: Arabic push bodies interpolate a partner name with no isolation. **Latent, not live.** Its blocker is honest: *nobody has established whether iOS/Android notification chrome honours `U+2068`/`U+2069` at all*, and ADR-033's evidence is Flutter-engine-side and does not transfer. Ride it along with the on-device checks.

**Universal links (ADR-040 restoration)** — not an issue yet, deliberately. The moment the founder reports whether **Associated Domains** appears in the portal capability list for `com.beyondkaira.hayati` (operator 2(d)), re-adding the entitlement is a two-line change plus a release. The AASA is already live and verified. File it as an issue if the founder answers and nobody acts.

**FIRST — the preemptions. Query the platform, not the docs; re-derive from issues AND PRs.**

1. **The four `ASC_REVIEW_CONTACT_*` secrets** — `gh secret list --env release`. **Absent at the S056 close** (the `release` env holds only `ASC_API_KEY_P8`, `ASC_ISSUER_ID`, `ASC_KEY_ID`). If they now exist, the founder handoff has landed — see the box at the top.
2. **`gh workflow run testflight-testers.yml -f status_only=true`** — read-only, and the honest answer to "do the testers have it". **At the S056 close: build 110 `external=READY_FOR_BETA_SUBMISSION` / `internal=IN_BETA_TESTING`, groups `founders, Friends`; readiness reported all four contact fields MISSING.** A build can read `VALID` forever and reach nobody. **Check the NEWEST build, not 110** (addendum 64).
3. **The release lane** — **`build 112` shipped at the S056 close** and is `processing=VALID`, `groups: founders, Friends`. Two things about how it got there are worth carrying forward: (a) the **first** dispatch (`30501640068`) died in `integration` on `auth_emulator_test` with *"Connecting to the VM Service timed out"* after a ~10-minute SwiftPM fetch — an infra flake, proven by main's own `integration-emulator` running the same five suites green on `3e248aa` 30 minutes earlier, and cleared by a re-dispatch; (b) the **Friends assignment did not happen automatically** — see addendum 69. **After any release, run `-f status_only=true` and read `groups:`. Never infer delivery from a green release.**
4. **The website** — `curl -so /dev/null -w '%{http_code}\n' https://ikimiz.web.app/i/9U4VUVRV`. **200 at the S056 close — it is LIVE**, deployed from the local CLI on founder authorization, invite surface only (`/privacy` and `/terms` deliberately 404, nothing links to them). AASA served **200 `application/json`**. `ikimiz.beyondkaira.com` still A-records to the founder's **VPS** and still fails TLS — operator 2(e)(i), and now purely cosmetic rather than blocking.
5. **#115** — `curl -i -X POST https://revenuecatwebhook-mzym2uw5gq-ew.a.run.app -H 'Content-Type: application/json' -d '{}'`. **JSON = fixed. HTML = still broken** (HTML at the S055 close, unchanged for **seven** sessions; not re-probed at S056). If fixed: ask for a RevenueCat event replay, and **#41 becomes urgent**.
6. **Prod runtime** — `firebase functions:list --project hayatiapp-prod`. **11 × `nodejs22`** at S055. **Scheduler `ENABLED @ 0 * * * *` and Eventarc `RETRY_POLICY_RETRY` still could NOT be verified — no `gcloud`, no ADC.** Record the gap; do not assert it.
7. **`RC_WEBHOOK_TOKEN` on dev** — absent at S055 (404, prod exit-0 as control), so dev runs **ten** of eleven functions. Operator item **0(c)**.
8. **Dependabot alerts** — still **disabled** at S055. Operator item **2(b)**. Both open advisories are recorded in ADR-034 as measurably unreachable; **do not let an alert badge stampede a session into the `firebase-admin@10.3.0` downgrade ADR-034 refuses.**
9. **Open issues and PRs** — re-derive both from scratch. Do not inherit any list, including this one. **And re-derive the session NUMBER from `git log`.**
10. **Gate 3 / Android green-light** → M6.5.

### The blocked table as it stood at the S056 close — re-check every line; do not copy it forward

| Issue | Blocked on | Why a session cannot take it alone |
|---|---|---|
| **operator 2(c)** | founder | The four `ASC_REVIEW_CONTACT_*` values are facts about the founder. Everything else in that path is built, tested, and now pointed at the right build. **This is the only thing between five friends and a working app** |
| **the tester list** | founder | Five names/emails, promised at the S056 close. Adding a tester emails a real person |
| **operator 2(d)** | founder | Associated Domains on the App ID. **No longer blocking (ADR-040)** — downgraded 🔴→🟡. It now buys tap-to-open-the-app instead of tap-to-open-the-page |
| **operator 2(e)(i)** | founder | The `ikimiz` A record points at the founder's own VPS. **No longer blocking** — invites are served from `ikimiz.web.app`, which needs no DNS record |
| **operator 2(e)(ii)** | founder | The controller's **legal name** is a fact no session should guess into a privacy policy. **No longer blocks invites** (`--invite-only`), but still blocks `/privacy` and `/terms`, and therefore the App Store listing |
| **#115** | founder | Making a production endpoint world-reachable is a security-posture change on a live system |
| **#41** | founder | Live billing identity. Whether sandbox purchases already exist decides *clean change* vs *migration* |
| **#48** | the device | Defers to on-device observation of a transient Face ID lockout |
| **#15** | the device | Needs a native crash log from a real iPhone |
| **#136** | the device | Needs one observation nobody has made: does the notification shade honour bidi isolates? |
| **#13** | M6.5 | Android, Gate-3 gated (ADR-006) |
| **#63**, **#71** | founder | Brandkit revisions |
| **#121** | **re-derive** | Its stated blocker (2(d)) is dead — ADR-040 removed the entitlement and S056 dispatched the lane. Pair with #129 |

**Do NOT** grant public invoker on prod, migrate RC subscriber ids, deploy anything to `hayatiapp-prod`'s **Functions**, re-bootstrap `match` certificates, run `npm audit fix --force`, downgrade `firebase-admin`, enable Dependabot on the founder's behalf, guess the founder's legal name into a legal document, add a real person as a TestFlight tester without the founder's list, or dispatch the release lane without asking. *S056 note: live **Hosting** deploys are no longer on this list — the founder authorized one in-session and it is done. Re-ask for the next one; one authorization is not standing consent.*

**What is DONE (do not re-do):** the whole MVP **M1–M6.3 including M5.3**, consent/legal at **legal version 2**, CI→Slack, the UI/UX arc + redesign waves, **#74**, **#29**, **#88**, **#47**, the bundle-id rename, the first dev deploy, release-lane signing, the `match` lane (#103, #117), **Node 22 on dev and prod**, **#76**, **firebase-admin v14**, **#70**, **ADR-032** + `tool/release_lane_lint.dart` + **#99**/**#67**, **#120**, **#100**, **#133** (ADR-033), the S052 TestFlight lanes (**#139**, #142/#143), **#131** (**ADR-034**), the **ikimiz rename** (**ADR-035/036/037**), **ADR-038** (the review-contact write, gated submission, `externalBuildState` in `--status`), and — new at S056 — **ADR-039** (fail-open boot + `BootFailureApp`, `SlowLoadEscape`, the three bounded waits, the closed invite-host set on `ikimiz.web.app`, `--invite-only`, memoized `hayatiTheme`) and **ADR-040** (ship without universal links), **the live invite website**, and **a release dispatched with both fixes in it**.

**Open and UNBLOCKED at the S056 close — this is the queue:** **#140** (the objective above), **#146**, **#130**, **#129**, **#137**. All five are Linux-only and need nothing from the founder. Re-derive them anyway — do not inherit this line.

On completion, follow `session-rules.md` §3/§4: append to `past-prompts.md`, regenerate this file, refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
