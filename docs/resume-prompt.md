# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after. **Session 019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss; on agent death, inventory the tree against the deliverable list before resuming. **Session 022 addendum: three implementation agents ran in PARALLEL safely** because their file sets were disjoint by design and every shared file (`ci.yml`, cross-cutting docs) was reserved to the orchestrator — reserve-shared-files is the rule that makes parallel implementation safe. **Session 023 addendum: an implementation agent's cross-file coordination warning was FALSE** (it inferred two more integration suites needed consent seeding without reading them) — verify agent coordination claims by direct inspection before acting; five minutes of grep beat an unnecessary edit to main-only suites.
>
> **Standing review-ordering note (Sessions 015–023, now TEN-for-ten pre-code / ELEVEN consecutive passes):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code; S016: 1 blocking; S017: 3 confirmed; S018: 2 blocking + 10 serious; S019: 1 blocking + 6 serious; S020: 4+5 pre-code then 2+4 post; S021: 2+6 then 2+8; S022: 2+2 then 0+2; **S023: 2 blocking + 8 serious pre-code, then 0 blocking + 3 serious + 3 minor post-implementation — all eleven passes found real defects**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND. S018 addendum: when two verifier lenses disagree, the governing doc decides. S019 addendum: a skeptic pass can surface a DEEPER defect while refuting a shallow one — and (S022) a REFUTATION can carry load-bearing precision worth folding into the ADR. S020 addenda (held again in S023): guarantee-vs-mechanism gaps are the highest-value finding class — prefer deleting an over-claim to faking a mechanism (S023 corollary: OR build the cheap mechanism AND record the bound it cannot carry — the rules consent predicate did both); never hang a security side effect off a value-equal state transition; source-sentinel tests for guarantees no fake can express; MUTATION-CHECK every guard; native/asset config can fail silently green. S021 addenda: concurrency tests must race DISTINCT principals; a rules freeze-on-update without a create-forbid is a mint gap. S022 addenda: a first-time `workflow_dispatch` workflow registers only on the DEFAULT branch; check what the FIRST BUILD already does before deferring bootstrap steps; environment secrets are invisible without `environment:`. **S023 addenda: (1) a client-claimed version number in a wire contract is a partial-deploy brick waiting to fire — let the SERVER stamp its own constant and pin cross-tree version agreement with a source-sentinel reading all three trees. (2) A safety-net UI state can re-create the exact trap it guards against (the stale-state escape latch) — test the AFFORDANCES of every terminal state, not just its copy. (3) A predicate added to rules can silently subsume an older guard's mutant (same-doc `get()` vs `exists()`) — re-check every existing mutant's two-leggedness when touching adjacent clauses. (4) Legal copy is a guarantee surface: review it sentence-by-sentence against the code exactly like store copy (the "stores coach messages" over-claim sat on the consent screen itself).**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** (`functions/test/support/admin.ts`) **and (S023) must seed writer consent** — the ADR-023 D4a rules predicate denies consentless answer writes (rules-context tests seed `users.consent`; the callable path uses `recordConsent`). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M5.2 (ADR-017): the coach app half is live.** **M6.1 (ADR-018): the device-privacy layer is live — the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay` in lock-screen children.** **M6.2 (ADR-019): the data-rights layer is live — wire contracts FROZEN (export `formatVersion` is 2 since S023); the seven cascade invariants are binding; the deletion notice sends NO push.** **M6.3 (ADR-020/021/022): the release lane + store metadata + cold-start posture are live** — `release.yml` tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the `release` environment (operator item 4); store copy in `fastlane/metadata/{tr,en-US}` under `tool/store_metadata_lint.dart`; **`--allow-empty-urls` is a ratchet: drop the flag when the founder hosts the (now-drafted) privacy policy**; the pre-frame bootstrap await set is PINNED by `app/test/bootstrap/entrypoint_sentinel_test.dart` (bootstrap edits = sentinel edits, same diff). **S023 (ADR-023): the consent/legal layer is live and BINDING: (1) `users.consent` is server-owned — the `recordConsent` callable is the sole writer and STAMPS ITS OWN VERSION (never accept a client-claimed legal version); (2) the three-way legal-version source-sentinel (`app/lib/features/legal/domain/legal_version.dart` == functions `CURRENT_LEGAL_VERSION` == `docs/legal/README.md` `version:`) means a legal-text revision bumps ALL THREE in the same diff — and M5.3 (the live LLM) is a RECORDED re-consent trigger (new recipient + cross-border leg ⇒ bump + re-gate); (3) `docs/legal/` is the authoring home, byte-synced to `app/assets/legal/` under a drift test — edit both or CI reds; (4) the D4a rules consent predicate on answer writes is presence-only and version-blind (the bound is recorded — do not claim gate/rules parity); (5) the six legal documents are review-PENDING (founder legal/native gate) — do not "fix" their TR/AR without the founder; (6) consent withdrawal is PROSPECTIVE by DV doctrine — never auto-erase on withdraw.** Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart` (the legal documents use the injected-bundle `shippedLegalBundle()` variant); goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 024: the hardening sweep — ci-debt #36 + the change-PIN flow + the privacy manifest (#55) + the Node-24 CI bump (#39)

**FIRST, BEFORE SCOPING — three preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter from ADR-016's frozen wire contract + the `UnconfiguredCoachProvider` seam). It has priority the moment it is unblocked (waiting since Session 018). **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR must bump the legal version (all three sources, same diff), name the provider in the revised notice/policy, re-gate every user, and add the provider row to `docs/dpa-inventory.md` with the no-training contractual requirement + its own KVKK SCC leg.
2. **Blaze flipped (item 2)?** → do **the first-deploy slice** instead: script the first deploy of all seven Functions (now incl. `recordConsent`) to `hayatiapp-dev`, verify the deploy-only pieces (Cloud Scheduler trigger, Eventarc retry, `RC_WEBHOOK_TOKEN` Secret Manager binding), inherit the ADR-019 backup-retention alignment. Deploy-ordering note (ADR-023 D4): the Functions legal-version constant deploys before or with any app binary that raises the gate's expectation.
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
If none: proceed with the hardening sweep below.

**Why this is next:** with mvp item 12's buildable half closed (Session 023), **every line of MVP engineering that can be built without founder input is done** — M5.3 is the only planned unit left and it waits on item 6 alone. The highest-value unblocked work (session-rules §4) is the recorded-debt sweep: **ci-debt #36** (the quarantined M3.3 reveal round-trip integration test — deterministic listener-denial race, at the stabilization threshold), **the change-PIN flow** (M6.1's recorded convenience gap: today PIN change = turn off, turn on; both verify first, so it is a gap not a hole), **issue #55** (the app-level `PrivacyInfo.xcprivacy` — a live App-Store submission-rejection cause, adjudicated out of S023 as scope creep and issue-tracked; small, buildable, `ios-build-smoke`-assertable), and **issue #39** (CI actions on the deprecated Node-20 runtime — an annotation on every run; a version-bump sweep). Four small, recorded, unblocked items = one coherent hardening session. (Seasonal windows #29 is deliberately NOT bundled — the Hijri mapping is a real design task deserving its own ADR/session.)

**Scope:** (1) #36: de-quarantine the reveal round-trip test by fixing the mid-test auth-switch listener race (the issue carries the failing-log analysis; if the fix is structural rather than test-side, ADR-mini or issue-update first). (2) The change-PIN flow per ADR-018's invariants (verify-first, then set — all four lock invariants binding; no new lock-controller surface without re-auditing them; goldens + attempt-bounding untouched). (3) #55 per the recorded content: UserDefaults `CA92.1`, Purchase History = Analytics + App Functionality (linked), the Sensitive-Info judgment call RECORDED not silently resolved, pbxproj Resources wiring (the S022 InfoPlist.strings precedent), `ios-build-smoke` bundle assertion. (4) #39: bump the deprecated action versions; per-PR CI must stay green. NO new ADR unless a slice turns structural (the change-PIN flow may warrant a short ADR-018 amendment note instead).

**Acceptance criteria:** #36 de-quarantined and green in the integration-emulator job (post-merge proof — it is main-only) or honestly re-quarantined with a deeper root-cause recorded; change-PIN shipped with tests + goldens ×3 locales, all four ADR-018 invariants audited intact (app gate 68 green); #55 manifest in the built bundle (ios-build-smoke assertion green) with the judgment calls recorded in the issue/operator-expected; #39 annotations gone on a green run; both adversarial review passes IF any slice grows an ADR (else one post-diff review pass over the sweep); all CI green.

**External dependencies (founder):** none to BUILD. The manifest's label judgment calls get RECORDED for the submission-era founder/lawyer review, not resolved.

**Files likely to change:** `app/integration_test/reveal_round_trip_*` (+ possibly the listener wiring it races), `app/lib/features/settings|privacy_lock/**` (change-PIN), ARBs ×3 + goldens, `app/ios/Runner/PrivacyInfo.xcprivacy` (new) + `project.pbxproj` + `.github/workflows/ci.yml` (ios-build-smoke assertion + action bumps), `docs/test-suite.md`, issues #36/#39/#55, `docs/adr/018-*` (amendment note if change-PIN alters any recorded surface).

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); functions suite iff touched; push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main (the #36 proof lives in the main-only integration-emulator job — plan the acceptance post-merge, the S022 lesson); `codegraph sync`.

**Complexity:** low-medium — four small slices; the trap is the change-PIN flow quietly widening the lock controller's surface (re-audit the four invariants) and the pbxproj edit failing silently green (the ios-build-smoke assertion is the net). **Estimated duration:** one session. If it overflows: #36 + change-PIN this session; #55 + #39 next.

**Stopping conditions:** if #36's race turns out to live in production listener code (not the test), STOP the de-quarantine slice, record the root cause in the issue, and scope a proper fix session — do not patch a production race under a hardening label.

**Explicitly out of scope this session:** M5.3 unless item 6 answered (then it REPLACES this); any deploy (item 2); hosting/domain; M6.5; signing/TestFlight (item 4); screenshots; hotline numbers (★); `coach_sessions` (item 7); seasonal windows (#29 — its own session); editing the six review-PENDING legal documents (founder gate); any legal-version bump (no legal-text change is in scope).

**S023 handoffs to keep in view (binding):** the six ADR-023 invariants in the standing toolchain note (server-stamped version; three-source same-diff bump; docs↔assets drift; the version-blind predicate bound; review-PENDING legal texts; prospective withdrawal); the entrypoint sentinel rule (bootstrap edits = sentinel edits); the `--allow-empty-urls` ratchet drops the day the founder hosts the drafted policy; nothing this session may add a push notification or touch the four lock invariants beyond the audited change-PIN flow / seven cascade invariants.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: whichever preemption fires first — M5.3 on item 6, the first-deploy slice on Blaze, M6.5 on the founder's Android go — else seasonal windows #29 as the next unblocked slice), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
