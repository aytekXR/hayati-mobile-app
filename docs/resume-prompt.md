# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after. **Session 019 addendum: an implementation agent died mid-run on a session-limit API error — checkpoint-commit-immediately meant zero loss (its finished artifacts were on disk and its final suite run had already completed green); on agent death, inventory the tree against the deliverable list before resuming.**
>
> **Standing review-ordering note (Sessions 015–019, now FIVE-for-five):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code finds; S016: 1 blocking; S017: 3 confirmed; S018: 2 blocking + 10 serious; S019: 1 blocking + 6 serious — all four lenses independently converged on the blocking find: a safety guarantee whose mechanism could not carry it). **S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND — do not drop them.** S018 addendum: when two verifier lenses disagree, the governing doc decides. S019 addendum: a skeptic pass can surface a DEEPER defect while refuting a shallow one — read refutations as carefully as confirmations (the mid-send sign-out repopulation hole was found this way).
>
> **Standing toolchain note (M2.1–M5.2):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam** — app tests run against `test/support/fake_purchases_repository.dart`; the real adapter configures only when `REVENUECAT_IOS_API_KEY` is non-empty (fail-closed). **M5.1: the coach wire contract is FROZEN in ADR-016** (`failed-precondition`/`not-premium`, `resource-exhausted`/`cap-daily`|`cap-monthly`|`rate-limited`, `permission-denied` = non-member only, `unavailable` = provider/infra, response `{kind:'reply'|'help', category?, text, remaining?}`); deployed default = fail-closed `UnconfiguredCoachProvider`; LLM tests run against `FixtureCoachProvider` + recorded fixtures ONLY. **M5.2 (ADR-017): the app half is live** — `CoachScreen` behind `PremiumGate`, help-sticky LATCH on `kind:'help'` (client-enforced, reset-only exit), code-first `mapCoachFailure` taxonomy, keepAlive transcript family `(uid, coupleId, personaId)` with an OWNER guard (late exchanges drop when the owner signed out — the S019 review race), sign-out family invalidation at the app root, the disclaimer single-homed in the ARBs (`coach*` keys; digit-run guard test pins the no-phone-number rule; key-set parity ×3 locales), `LocalFlagStore` seam over shared_preferences (`core/storage/` — the app's first local store), persona scaffolds in `functions/src/coach/persona-prompts.ts` (★-gated safety preamble; self-names track the ARB chip labels — TR "Perisi"/AR "ملهم"). **Theme gotcha (S019): the global `FilledButton` style has `minimumSize: Size.fromHeight(48)` = INFINITE min-width — a FilledButton inside a Row needs a local `minimumSize` override.** App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions instance regional: `europe-west1`). Flutter lives at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart` (M2.4); goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` (ADR-011). **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 020: M6.1 — the device-privacy layer: PIN/biometric lock + discreet alternate icon + coach snapshot obscuring

First M6 slice (`implementation-plan.md` M6; PRD F6). **M5.3 (the live provider adapter) is BLOCKED on operator item 6** (LLM provider decision + API key — now DUE, flagged in `operator-expected.md`); per session-rules §4 this session takes the highest-priority UNBLOCKED task. The device-privacy layer is that task: it is pure app-side engineering, it is THE precondition the DV threat model has been waiting on (ADR-016/017 both defer to it), and it owns three standing deferrals — ADR-017's OS-app-switcher snapshot exposure, PRD F6's discreet mode, and the M6 accept line "lock bypass attempts covered by widget tests".

1. **Scope precisely at session start** (session-rules §1.5) and **write ADR-018 FIRST, then adversarially review it BEFORE code** (five-for-five; see the standing review-ordering note). Decisions that come due: **the lock model** (PIN vs biometric-with-PIN-fallback; where the PIN lives — `flutter_secure_storage`/Keychain vs hashed-in-prefs is a security posture decision; lock timing — on launch AND on return-from-background after a grace window; what locks — the whole app behind the gate vs sensitive surfaces only); **the lock UX** (attempt limits/backoff; forgotten-PIN recovery = sign-out-and-back-in is the honest option — data is server-side; never a bypass); **discreet mode** (PRD F6: alternate launcher icon iOS + generic notification strings — the strings half shipped M3.4; the icon needs the brandkit discreet asset + a platform-channel seam, device-verified later like APNs; where the toggle lives); **snapshot obscuring** (the ADR-017 deferral: obscure the app-switcher snapshot at least while the coach surface — ideally any content surface — is on screen; iOS platform-channel work behind a seam, emulator-untestable half flagged); **the settings surface** (these toggles need a home — the minimal settings screen enters here; keep it one screen, no scope creep into M6.2's export/delete).
2. **The firm deliverable: the lock end-to-end in widget tests** — enabling the lock from settings, the lock screen gating the app on cold start and on background-return past the grace window, wrong-PIN attempts bounded with honest copy, biometric behind a faked seam (platform channels never run under `flutter test` — the M2.2/M4.2 seam precedent), bypass attempts pinned red (navigation, deep links, and route pushes must all land BEHIND the lock), discreet-icon toggle + snapshot-obscuring hooks behind seams with their device-verified halves loudly deferred to operator item 4.
3. **Docs-with-code:** ADR-018; architecture §2/§4/§8; test-suite §1; implementation-plan M6.1; this file; operator-expected (device-verification checklist grows: alternate icon render + snapshot obscuring + biometric prompt on real hardware).

**Acceptance criteria:** lock + settings screens shipped with goldens (six-cell {TR,AR,EN}×{LTR,RTL} + scale-130 for lock + settings); lock-bypass widget suite (cold start, background-return, deep-link entry, pushed-route entry — all gated); PIN storage decision implemented per ADR-018 with no secret in prefs plaintext; wrong-attempt bounding + recovery path tested; discreet-icon + snapshot seams shipped with fakes and their on-device halves flagged in operator-expected; free tier and the daily loop untouched (no lock unless enabled — assertion-proven); app suite green (gate 68) incl. `dart run build_runner build`; functions suite untouched-green (gate 80); all CI jobs green.

**External dependencies (founder):** **none required.** Item 6 (provider + key) unblocks M5.3 whenever answered — if it lands mid-session, do NOT scope-creep it in; note it for the next session. The device-verified halves (icon render, biometric prompt, snapshot behavior) join operator item 4's on-device checklist.

**Files likely to change:** `app/lib/features/privacy_lock/**` + `app/lib/features/settings/**` (new features), `app/lib/core/storage/` (secure-storage seam alongside `LocalFlagStore`), `app/lib/app.dart` (the lock gate mounts at the root), iOS Runner (alternate-icon asset registration + snapshot hook — CI-safe, `--no-codesign`), ARB ×3, goldens; `docs/adr/018-*`, docs per item 3.

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); functions untouched (spot-check `npm run lint && npm run typecheck` only if anything under `functions/` moved); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium-high — the logic is simple but the surface is security-posture-sensitive (PIN storage, bypass surface) and two seams are device-verified-later. **Estimated duration:** one session. If it overflows, the honest split is: lock + settings this session, discreet icon + snapshot obscuring next — do NOT stretch (session-rules §1.5).

**Stopping conditions:** if the PIN-storage posture needs founder input (it should not — Keychain via `flutter_secure_storage` is the defensible default; record alternatives in the ADR), pick the conservative default and record it. If the iOS alternate-icon registration turns out to require Xcode-side assets that break the CI `--no-codesign` build, ship the seam + settings toggle with the icon-half deferred loudly to the Mac slice and file the debt.

**Explicitly out of scope this session:** M5.3 (founder-blocked, item 6); KVKK export + cascade delete + partner notification (M6.2); store metadata/Fastlane/perf pass/beta build (M6.3, Mac-dependent); Android (M6.5); any deploy; hotline numbers (★); `coach_sessions` persistence (founder decision).

**M5.2 handoffs to keep in view (binding):** the coach surface must land BEHIND the lock like everything else (the lock gate wraps the root, not per-feature); ADR-017's snapshot-obscuring deferral is explicitly this session's to close (or loudly re-defer with a reason); `LocalFlagStore` is the precedent for device-scoped flags (the lock-enabled flag and discreet toggle likely join it — but the PIN itself does NOT live there); the app-root listener idiom (`app.dart`) is where lock-state watchers mount; the theme's `FilledButton` infinite-min-width gotcha applies to any button-in-a-Row.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M6.2 — KVKK/PDPL export + cascade delete; or M5.3 if the founder has answered item 6 — the live adapter takes precedence when unblocked), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
