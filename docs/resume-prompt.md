# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after** (S025 did exactly this with an inherited `.claude/settings.json` model pin — never "fix" a config you did not author). **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing (it happened; the Content-Type mutant survived a 2-minute timeout and was caught only by the explicit diff).
>
> **Standing review-ordering note (Sessions 015–025, now THIRTEEN-for-thirteen pre-code / FOURTEEN consecutive passes):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S023: 2 blocking + 8 serious pre-code; S024: 1 blocking pre-code; **S025: 4 blocking + 6 serious + 8 minor pre-code (24 of 27 findings survived), then 1 blocking + 1 serious + 5 minor post-implementation — ALL 12 post-diff findings survived double verification**). Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the ADR, once on the built diff.** Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class (prefer deleting an over-claim to faking a mechanism); when two lenses disagree, the governing doc decides; a skeptic pass can surface a DEEPER defect while refuting a shallow one; **MUTATION-CHECK every guard** — and (S025) *mutation-check the TEST too*: two S025 guards passed their own tests because the tests never executed the code path that held the secret (an anti-leak sentinel that only ran in dry-run, which exits before the POST). **S025 addenda: (1) policy that lives in a YAML `if:` is invisible to the test suite and therefore protected by nothing — put it where a test can see it. (2) A safety claim in a CODE COMMENT is a guarantee surface too (two S025 comments over-claimed and were corrected). (3) `jq --arg` makes a value safe as JSON and safe from the shell — NOT safe from the renderer that displays it. (4) A stale operator item is worse than no item: §5 had been telling the founder to push a leaked credential for ~20 sessions.**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent** (the ADR-023 D4a rules predicate denies consentless answer writes). **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4 = change-PIN): the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)` (source-sentinel-pinned since S024); (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay` in lock-screen children.** **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); **`--allow-empty-urls` is a ratchet — drop it when the founder hosts the drafted privacy policy**; the pre-frame bootstrap await set is PINNED by `app/test/bootstrap/entrypoint_sentinel_test.dart` (bootstrap edits = sentinel edits, same diff). **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned (`recordConsent` is the sole writer and STAMPS ITS OWN VERSION); the **three-way legal-version source-sentinel** (app `legal_version.dart` == functions `CURRENT_LEGAL_VERSION` == `docs/legal/README.md` `version:`) means a legal-text revision bumps ALL THREE in the same diff — and **M5.3 (the live LLM) is a RECORDED re-consent trigger**; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; the D4a rules predicate is presence-only and version-blind (bound recorded); the six legal documents are review-PENDING (founder gate); withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier, run by a terminal `slack-notify` job in BOTH workflows; **it is a side-channel with NO VOTE on the build** — every error path `exit 0`s AND the job carries `continue-on-error: true` (both halves are required); **ALL policy lives in the script, never in a YAML `if:`** (a YAML condition is invisible to the self-test); the secret is **repository-scoped, NOT `environment: release`**; `quality` now runs `shellcheck tool/ci/*.sh` + the 16-case suite; **ci.yml's concurrency keys push events on the COMMIT (D8)** — do not "simplify" it back to ref-keying, that is what was cancelling the post-merge `integration-emulator` verdict. Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin`. Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart` (legal documents use the injected-bundle `shippedLegalBundle()` variant); goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.

## Objective — Session 026: the **UI/UX Pro Max ("uipro") refactor — the SCOPING ADR** (ADR-025). Design only. No pixels move this session.

**FIRST, BEFORE SCOPING — the preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter behind ADR-016's frozen wire contract, replacing the fail-closed `UnconfiguredCoachProvider`). It has priority the moment it is unblocked (waiting since Session 018 — it is the ONLY planned MVP session-unit left). **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR must bump the legal version (all three sources, same diff), name the provider in the revised notice/policy, re-gate every user, and add the provider row to `docs/dpa-inventory.md` with the no-training contractual requirement + its own KVKK SCC leg.
2. **Blaze flipped (item 2)?** → do **the first-deploy slice** instead: script the first deploy of all **eleven** Functions (the five loop/invite units + `revenueCatWebhook` + `coachProxy` + the four data-rights callables incl. `recordConsent`) to `hayatiapp-dev`, verify the deploy-only pieces (Cloud Scheduler trigger, Eventarc retry, `RC_WEBHOOK_TOKEN` Secret Manager binding), inherit the ADR-019 backup-retention alignment. **Verify Blaze FACTUALLY** (the S023 method: Cloud Billing REST API `billingEnabled`), never from the checklist's memory. Deploy-ordering note (ADR-023 D4): the Functions legal-version constant deploys before or with any app binary that raises the gate's expectation.
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
4. **Has the founder reported results from the ★ direct-install on-device test?** A failing M6.1 lock check, a broken deep-link delivery, or a phone-auth native crash log (#15 on real hardware) **preempts** — triaging a reported on-device defect beats any planned slice.
5. **Has the founder said "build the dev-rig slice"?** (`lib/main_demo.dart` on the integration tests' `demo-hayati` bootstrap + an on-demand day-doc seeder, so the **join** and the **couple daily loop** — the two honest bounds of the on-device recipe — run on the phone rig pre-Blaze.) If yes, fold it in or re-scope to it.
If none fire: proceed with the uipro scoping ADR below.

**Why this is next:** the founder installed the **UI/UX Pro Max** skill tooling (`uipro` CLI v2.11.0, global on the dev box — verified present) and directed a full UI/UX refactor of the app (directive 2026-07-14, recorded in `roadmap.md`). The roadmap's own sequencing puts it **behind the standing preemptions and AHEAD of the AI-chosen backlog** (e.g. seasonal windows #29). It is explicitly recorded as needing **its own scoping ADR before any pixels move**, and it is sized as a **multi-session arc** — so the correct next unit is the ADR, not a refactor sprint. Everything else unblocked is smaller: #29 (seasonal windows — its own design task, still queued behind this), #47/#48 (M6.1 lock debt — both explicitly want real-device judgment, operator item 4), #41 (RC identity threat model — blocked on operator item 0 by its own terms).

**Scope (design only):** (1) Run `uipro init` in the repo (it is NOT project-installed yet) and record honestly what the tool actually produces — do not describe capabilities you have not run. (2) **Surface inventory**: every app screen/state, cross-referenced to its goldens and its invariant status. (3) **The skill-output → brandkit mapping**: brandkit v1.0 is the visual constitution — the skill *proposes*, the brandkit's tokens/assets *decide*; the refactor is expressed **through** brandkit tokens, it does not replace them. (4) **Session slicing**: how the arc is cut into sessions, with an acceptance line per slice. (5) **The invariant firewall** — spell out, per surface, what a refactor may NOT touch: the lock screen's no-dialog/no-`Overlay` constraint (ADR-018 D3) and the four lock invariants, the privacy shield, the consent screen's copy (a *guarantee* surface, ADR-023 — its sentences were adversarially matched to the code), the crisis/coach copy under the ★ safety gate, and any copy under the native-review gate (operator item 1) which a refactor session may **not** silently reword. (6) The goldens acceptance harness: ×3 locales + RTL, regenerated **intentionally** per refactored screen, never blind-accepted (W4).

**Acceptance criteria:** ADR-025 committed **before any UI code**, then adversarially design-reviewed (both passes if code lands; at minimum the pre-code pass — this session is expected to be ADR-only); `uipro init` actually run with its real output recorded (including its failure modes / what it cannot do); a surface inventory that matches the real code (verify with CodeGraph, not memory); an explicit, per-surface invariant firewall; a session-slicing plan with acceptance lines; `roadmap.md` updated from "needs a scoping ADR" to "scoped, see ADR-025". **No pixels move this session** — if a slice looks trivially safe, it still waits for its own session.

**External dependencies (founder):** none to design. The native-review-gated copy (operator item 1) and the safety-gated crisis copy (★) constrain what any later refactor session may touch — record, do not resolve.

**Files likely to change:** `docs/adr/025-*` (new) + `docs/adr/README.md`, `docs/roadmap.md`, possibly `docs/frontend-brandkit.md` (only if the mapping exposes a genuine gap — do NOT rewrite the brandkit), `docs/test-suite.md` (the goldens-per-slice acceptance harness), whatever `uipro init` legitimately writes into the repo (record it; if it wants to write into source trees, decide deliberately and say so in the ADR).

**Validation steps:** if the session stays ADR-only, CI is a docs run — still push a PR and watch it; `gh pr checks --watch`; squash-merge; **then watch the post-merge MAIN run** (session-rules §3.5 — since ADR-024 D8 the close commit no longer cancels it, but it still concludes after your attention moves on; the new Slack notifier will report it **only once the founder sets the webhook**, operator item 5); `codegraph sync`.

**Complexity:** medium — the trap is writing an ADR that *describes a tool it never ran*, or one whose invariant firewall is a list of good intentions rather than a per-surface, test-backed boundary. The second trap is scope: this is a scoping ADR, and a session that starts "just fixing one screen" has already failed it.

**Stopping conditions:** if `uipro init` turns out to require network/credentials/a Mac, or produces nothing usable on this box, **STOP and record that honestly in the ADR** (the tool's real capability is a load-bearing input to the whole arc) — then re-scope this session to the next unblocked slice (#29 seasonal windows) rather than inventing a refactor plan around a tool that cannot run.

**Explicitly out of scope this session:** M5.3 unless item 6 answered (then it REPLACES this); any deploy (item 2); hosting/domain; M6.5; signing/TestFlight (item 4); screenshots; hotline numbers (★); `coach_sessions` (item 7); editing the six review-PENDING legal documents (founder gate); any legal-version bump; **any UI/UX code change at all**.

**S025 handoffs to keep in view (binding):** the ADR-024 invariants in the standing toolchain note (the notifier has no vote — both halves; policy lives in the script; the secret is repo-scoped; **do not revert D8's commit-keyed concurrency**); **operator item 5 is a live security action** (the leaked webhook, unrotated since Session 005) — until the founder rotates + sets it, the notifier is silent BY DESIGN and that silence is honest, not a bug; and the S025 review lessons in the review-ordering note (mutation-check the *test*, not just the guard; a code comment is a guarantee surface; a stale operator item is worse than none).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: whichever preemption fires first — M5.3 on item 6, the first-deploy slice on Blaze, M6.5 on the founder's Android go — else the FIRST uipro refactor slice per ADR-025's own slicing plan), refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
