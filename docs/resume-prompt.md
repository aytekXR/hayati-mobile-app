# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing toolchain note (M2.1–M4.1):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (Session 013/014/015: running `emulators:exec` from `functions/` breaks the exec script's `cd functions`; and never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`) or the live `answerReveal` trigger races the drive — only deliberate wire-proof e2e tests stay on `demo-hayati`. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`** (`RC_WEBHOOK_TOKEN=demo-rc-webhook-token`; the emulator loads `.env.<projectId>` dotenv files — no CI or command change needed; real env files are gitignored). App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id). Widget-test gotcha (M2.4): asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content: packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones: couple dayKey comes from `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites; never key couple state off the device zone (ADR-011). Coverage gates: app 66 (M3 ratchet — consider 66→68 with the M4 close, not mid-M4), functions 80 hard / 85 target; the app gate script runs from repo root: `dart tool/coverage_gate.dart --min 66 app/coverage/lcov.info`.

## Objective — Session 016: M4.2 — paywall UI + free-tier gating: annual-first paywall per locale, `purchases_flutter` behind a seam, gating flips behind `isPremium`

Second M4 slice (`implementation-plan.md` M4; PRD F4). M4.1 built the server truth (`subscriptions/{coupleId}` mirror via `revenueCatWebhook`, ADR-013) and the app decision point (`isPremium`); this session builds the surface that sells and the flips that gate. **Two halves with different dependencies:** the UI/seam half is fully buildable with mocked offerings (no founder input); the live-sandbox half needs the founder's RevenueCat account + App Store Connect app record (operator-expected item — check status at session start; if absent, ship the mocked half and record the sandbox slice for M4.3, which needs it anyway for gifts).

1. **`PurchasesRepository` seam** (`app/lib/features/entitlements/`): `purchases_flutter` behind the house throw-until-overridden provider pattern — configure/logIn(uid), offerings fetch, purchase(package), restore. The SDK drives platform channels (untestable in the flutter-test VM, the M2.2 `cloud_functions` precedent) so ALL tests run against a fake; the real adapter is wired at bootstrap and exercised on-device later. `Purchases.logIn(firebaseUid)` before any purchase is LOAD-BEARING for ADR-013's identity resolution — wire it to the auth state, test the call-order contract at the fake level.
2. **Paywall screen** (PRD F4: annual-first presentation, 7-day trial messaging, one-purchase-covers-both framing): offerings-driven price display (TRY/SAR/USD localized via the store's own price strings — never hardcode), loading/error/offline honest states, entitled state (already-premium renders manage/restore, not buy). Entry points: a reusable premium-gate widget/route helper + a paywall route; NO paywall interstitials on the daily loop (the free tier keeps daily question + streak whole, F4).
3. **Free-tier gating flips (THE M4.2 accept line):** gate what exists TODAY — pack/register selection beyond the default bank is the only real premium surface until M5 (coach is premium-only at M5 per F5; quizzes/spice/timeline are v1.5). Concretely: the future packConfig selection surface gates behind `isPremium` (locked presentation + paywall route), and the gate helper is the single reusable seam every later premium feature (coach M5) mounts on. Widget-test the flip both ways; prove the free tier (daily question + streak + solo) is untouched by assertion, not hope.
4. **Goldens:** paywall × {TR, AR, EN} × {LTR, RTL} + scale-130 variants (PRD F4 is a P0 screen; test-suite §1 matrix) with deterministic mocked offerings; locked-state cells for the gated surface.
5. **Live sandbox (founder-dependent half):** if the RC account + ASC app record exist at session start — RC project wiring (API keys via the existing config seam pattern, never committed), products (TR/SAR/USD tiers per F4), StoreKit-config sandbox purchase on the simulator if feasible without the Mac slice (else record: first sandbox purchase = operator smoke with M4.3). The RC **webhook config** (URL + the ≥256-bit token, ADR-013) stays a deploy-time item (needs Blaze; operator item 2).
6. **Docs-with-code:** ADR-014 if the offerings/paywall architecture makes real decisions (else fold into architecture §4), test-suite §1 (paywall goldens + fake-SDK seam), implementation-plan M4.2 progress, this file, operator-expected refresh.

**Acceptance criteria:** paywall renders annual-first with trial copy from mocked offerings in all six golden cells + 130% variants; entitled/free/loading/error paywall states widget-tested; the premium gate flips a gated surface between locked and unlocked purely on `isPremium` (both directions tested) while daily question + streak + solo stay assertion-proven free; `Purchases.logIn(uid)`-before-purchase contract pinned at the fake level; restore path present; no store price ever hardcoded; app suite green (coverage ≥66); functions suite untouched-green; all CI jobs green.

**External dependencies (founder):** the live-sandbox half only — RevenueCat account (+ its ≥256-bit webhook token when the webhook is configured at deploy) and the App Store Connect app record (needs Apple Developer enrollment, promised 2026-07-08). **The session proceeds without them** (mocked offerings); check `operator-expected.md` at start.

**Files likely to change:** `app/lib/features/entitlements/**` (purchases seam + paywall + gate helper), `app/lib/features/daily_question/**` or the settings/home entry point (gate mount), `app/test/**` (+goldens), `app/pubspec.yaml` (`purchases_flutter`), docs per item 6. Functions side: none expected.

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 66 app/coverage/lcov.info` from repo root) + functions gate untouched (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from repo root**); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium — one SDK seam, one screen with goldens, one gate helper; the subtlety is offerings-model fidelity (build the fake from `purchases_flutter`'s real types so the live wiring lands without reshaping) and keeping the paywall honest offline. **Estimated duration:** one session (2–3 h). If it overflows: the paywall screen + goldens + gate helper are the coherent slice; live RC wiring moves to M4.3 (which owns sandbox purchases anyway).

**Stopping conditions:** if `purchases_flutter`'s current major has breaking API drift from training knowledge, pin the version that matches the documented API and record an upgrade note — do not fight the SDK this session. If the ASC record/RC account are absent (likely), that is NOT a blocker — ship the mocked half, flag the operator item again, move on. If store-localized price formatting for TRY/SAR proves unmockable cleanly, render the fake's display strings verbatim and record the fidelity gap for the sandbox smoke.

**Explicitly out of scope this session:** the gift flow + `TRANSFER` handling (M4.3); sandbox purchases as a proven accept line (M4.3 per implementation-plan — TR + SA storefronts); RC-API reconciliation/backfill (recorded M4.2+ in ADR-013 — schedule it when the webhook is deploy-live, likely M6 hardening); any Function change; webhook deploy/config (Blaze, operator item 2); coach caps (M5); Android (M6.5); the private thread (M5 scope selection); seasonal selection (#29, W9); any deploy.

**M4.1 handoffs to keep in view (binding):** `revenueCatWebhook` is the ONLY writer of `subscriptions/{coupleId}` (rules deny all client writes — the app NEVER writes entitlement state, the SDK's customerInfo is a hint at best, the mirror is truth); ADR-013's `entitled`-is-never-sufficient rule — every consumer pairs it with the `expiresAt` future-check (the `isPremium` provider already does; don't bypass it); `Purchases.logIn(firebaseUid)` before purchase is what makes `app_user_id` resolve (anonymous purchases skip loudly and the mirror stays free); the one-`premium`-entitlement simplification gets revisited when real RC products exist — if product config forces granularity, that's an ADR-013 amendment, not a silent drift.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M4.3 — gift flow + sandbox purchases, or M4 close), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
