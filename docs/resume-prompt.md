# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore` MCP tool (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. **S029/S030/S047 addendum: the other claudes on this box work OTHER repos (`evrak`, `repo`(parent), `ams-pulse`, `unhooked`)** — confirm by walking cwd before treating one as a conflict. **S038 addendum: a CONCURRENT session on ANOTHER machine/tree can merge to `main` mid-session** — `ps`/`tmux` only cover THIS box, so after every merge read `git log --oneline -5` and `git show --stat` the commits you did not write. **Session numbers are per-tree and collide** — S047 found the ordinal 046 already taken in `past-prompts.md` by the concurrent operator track and took 047. Trust dates, ADR numbers and PR numbers over the ordinal. **Checkpoint-commit implementation output IMMEDIATELY**; commit design docs BEFORE launching workflows. **S037 addendum: REVIEW workflows can mutate the tree** — after every review workflow returns, `git status` + `git diff` must be EMPTY before you commit anything (S047 checked; they were).
>
> **Standing review-ordering note (Sessions 015–047, now TWENTY-FIVE consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are. Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff.** Aggregate so a finding surfaces when EITHER verifier says real. **Check `agents_error`/`agents_done` before trusting a verdict distribution — an empty verdict is *unverified*, and the tooling renders it as the opposite** (S041). **Rebase onto latest `main` before sending a diff to the review workflows** and re-check at merge time (founder directive, S042).
>
> **S049 addendum — (48) A PREMISE THAT WAS REPLACED RATHER THAN MEASURED IS LIKELY WRONG AGAIN.** ADR-029 D6 correctly retired *"macOS minutes bill at 10× on this private repo"* and offered *"macOS runners queue far longer than ubuntu"* in its place. **Measured at S049 over 24 macOS jobs: queue time is 0.1 min — about six seconds — median AND max.** The substitute was as false as the original, S047 repeated it verbatim while auditing other stale claims, and a THIRD copy sat on another gate neither session touched. Whatever produced the first guess produces the second: when you retire a load-bearing claim, **measure the replacement or mark it unmeasured** — do not reason your way to a new one. (The measurement itself was one `gh api .../jobs` sweep. It had been open for four sessions.)
>
> **S048 addendum — (47) A NEW RULE CAN MAKE OLD ROWS VACUOUS.** S048 added a fifth rule to the release-lane lint, and four *pre-existing* mutation rows silently began returning an INPUT error (exit 64) instead of the violation they assert — so they passed for the wrong reason, invisibly. Found by re-running the whole matrix, not by reading the new rule. This is S042 addendum 29 generalised: after adding a rule, **re-run every row**, not only the new ones. Corollary from the same session: when mutating a version-constraint check, mutate **both halves** of the operator — making it always-true is caught by seven rows, while incrementing the *wrong segment* (which silently admits a MAJOR bump, the case a lockfile most exists to catch) was caught by **exactly one**.
>
> **S047 addenda — three of them are about reviewing your OWN work:**
> **(43) A GUARD CAN CONTAIN A BRANCH THAT IS VACUOUS *BY CONSTRUCTION*, AND YOUR OWN MUTATION ROW CAN MISS IT.** S047's release-lane lint checked "the gate demands no secret the job ignores" by measuring consumption over the *whole* job — but a fail-closed gate must bind every secret it tests, so its own `env:` block always supplied them and `checked ⊆ consumed` held unconditionally. The branch could never fire. The mutation row that "proved" it added `missing+=("X")` *without* an env entry — **a shape no human writes**, which is exactly why it passed. When a mutant has to be unrealistic to make a rule fire, the rule is probably measuring the wrong set. Fix: exclude the gate; re-mutate; confirm the old line kills precisely the new assertions.
> **(44) THE "SAFER" EDIT TO A WORKING PATH IS STILL A BLIND EDIT.** Switching the `.p8` step to decode a base64 secret looked strictly safer than requiring a second raw one. But **`openssl base64 -d -A` silently emits nothing and exits 0 on newline-bearing input**, while fastlane decodes the same secret with Ruby's `Base64.decode64`, which **ignores** newlines — so a line-wrapped secret works in fastlane and breaks only in your step, where the sanity check you added "to be safe" is what reddens a previously-working release. **Test the tool against a realistic artefact rather than reasoning about its flags** (`openssl genpkey -algorithm EC` produces a real PKCS#8 key in one line — its header is `BEGIN PRIVATE KEY`, *not* SEC1's `BEGIN EC PRIVATE KEY`).
> **(45) WRITE THE ADR LAST, NOT IN THE MIDDLE.** S047's ADR-032 landed between two implementation commits and its Decision 4 was **false two commits later** — it claimed a six-secret gate, an untouched `.p8` step and a still-required secret, all of which the next commit changed. W6 says "same commit as the change"; in a multi-commit branch that means the ADR is the *last* commit. This is addendum 10 (an ADR's promises about its own diff are guarantee surfaces) with its cause identified.
> **(46) READ THE ARTEFACT, NOT JUST THE SOURCE.** S047 reasoned from the Fastfile that `deliver` had been pushing a stale app name; the actual CI log said `deliver` **had never run at all** — the lane aborted on missing credentials inside a `continue-on-error: true` step that reported *success*. `gh api repos/:owner/:repo/actions/jobs/<id>/logs` is one call and it overturned the diagnosis. A green job is not evidence its steps ran.
>
> **Earlier addenda that keep paying (condensed):** guarantee-vs-mechanism gaps are the highest-value class; when two lenses disagree, the governing doc decides; MUTATION-CHECK every guard AND the test, and **mutate your own replacement, not just the original** (S042); **changing a guard invalidates the matrix that proved it — re-run, don't reason** (S041); a safety claim in a CODE COMMENT is a guarantee surface; **when a diff corrects a claim, grep the WHOLE repo for that claim** (S041 addendum 19 — S047 broke it again: its own newly-written `architecture.md` paragraph still named the retired three-secret gate); **"no unblocked engineering" is a claim to RE-DERIVE from the whole issue AND PR list every session, never to inherit** (S036/S041); **a bound justified by one caller's constraints is not safe for another caller** (S042 addendum 25 — the direct cause of S047's headline defect); **verify with the command CI runs, not the convenient one** (S044); **a deployed, `ACTIVE`, healthy-looking function can still be unreachable — the tell is HTML versus JSON** (S045 addendum 40); **query the PLATFORM, not the docs** (S045 addendum 41); **run the session, do not assert its conclusion** (S045 addendum 42); **"undated" is not "blocked"** (S044 addendum 38); a stale operator item is worse than no item.
>
> **Standing toolchain note:** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin`) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **FROM THE REPO ROOT** (echo the exit code — and beware that `; echo $?` after a pipe reads the *pipe's* status, which is how S047 briefly recorded a false green over `Script exited with code 2`): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **The emulator suite binds fixed ports and is NOT safe to run concurrently — including with your own review agents** (check `ss -ltn` for 8080/9099/5001 before believing a surprising red). **Run `flutter gen-l10n` in `app/` before trusting any test or analyze run that touches localized text** — `app/lib/core/l10n/gen/` is gitignored and the local copy goes stale (S042 addendum 24; S047 hit it again: 58 phantom analyze errors, zero after regen). Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). **Run `dart format` before every commit.** Coverage gates: **app 68** (`~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`. Theme gotcha: global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. Goldens are Linux-canonical.
>
> **Standing binding-invariants note:** **M6.1 (ADR-018 rev 4)** the four lock invariants; **M6.2 (ADR-019)** the seven cascade invariants, deletion notice sends NO push, export `formatVersion` 2; **ADR-023** consent/legal is BINDING — `users.consent` server-owned, the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in one diff, `docs/legal/` byte-synced to `app/assets/legal/` under a drift test, withdrawal is PROSPECTIVE by DV doctrine; **ADR-024** `tool/ci/slack_notify.sh` is the single notifier with NO vote on the build and ALL policy in the script; **ADR-025** the slice-0 firewall stays live (lock-screen forbidden-API sentinel, brandkit→Dart token parity, the 96-pair frozen-sentence digest); **ADR-026** the `seasonalWindow` vocabulary is CLOSED and gated in FIVE readers — adding a season is a five-file change; **ADR-032 (NEW)** release signing is fastlane `match` + MANUAL, the build NAME comes from pubspec while the build NUMBER is CI-synthesized, `store_metadata` uses the narrow ASC-only credential check, and `fastlane/metadata/*/name.txt` is PINNED to **İkimiz** because `deliver(force: true)` would otherwise rename the live App Store listing — all four enforced per-PR by `tool/release_lane_lint.dart` (56 mutation checks).

## Objective — Session 050: **The engineering queue is empty of unblocked work. Re-derive it to confirm that — and if it holds, present the derivation and stop.**

> **This is not a licence to skip the session.** S044 reached "nothing left to do" *in conversation* and never executed S045 — which then found a production defect that would have taken the founder's money silently (#115). S047 was handed what looked like documentation tidying and found a release-lane step that **had never once run** in its life. S049 was handed a four-session-old "someone should measure this" item and found that the premise which replaced a false premise was **also false, in three places**. **Every one of those was found by executing.** Addendum 42 is the standing rule.

**FIRST — the preemptions. Query the platform, not the docs (addendum 41); re-derive from issues AND PRs (addenda 12, 22).**

1. **#115** — `curl -i -X POST https://revenuecatwebhook-mzym2uw5gq-ew.a.run.app -H 'Content-Type: application/json' -d '{}'`. **JSON = fixed. HTML = still broken** (HTML at the S049 close). If fixed: ask for a RevenueCat event replay, and **#41 becomes urgent** — real entitlement state starts accumulating, and its remedy is a migration rather than a clean change once purchases exist.
2. **Prod runtime** — `firebase functions:list --project hayatiapp-prod`. Still 11 × `nodejs20` at S049. If it moved: re-verify Scheduler `ENABLED @ 0 * * * *`, Eventarc `RETRY_POLICY_RETRY`, and **whether the redeploy restored the webhook's public invoker**. HTML *after* a fresh deploy means something is actively removing that binding — worse than #115 itself, because it would re-break after every deploy.
3. **`RC_WEBHOOK_TOKEN` on dev** — still absent; the eleventh function is ADR-013 work to do *with* the founder.
4. **Open issues and PRs** — re-derive both from scratch. Do not inherit the list below.
5. **Gate 3 / Android green-light** → M6.5.

**THEN — the derivation as it stood at the S049 close. Re-check every line; do not copy it forward.**

| Issue | Blocked on | Why a session cannot take it alone |
|---|---|---|
| **#115** | founder | Making a production endpoint world-reachable is a security-posture change on a live system, and a session cannot read the webhook token to confirm it matches RevenueCat |
| **#41** | founder | Live billing identity. Whether sandbox purchases already exist decides *clean change* vs *migration*, and only the founder can establish it |
| **#48** | the device | Its own text defers to on-device observation of a transient Face ID lockout |
| **#15** | the device | Needs a native crash log from a real iPhone (Xcode → Devices → Console) |
| **#13** | M6.5 | Android, Gate-3 gated (ADR-006) |
| **#63**, **#71** | founder | Brandkit revisions — Phosphor-vs-Material, and a motion token. ADR-025 keeps these out of refactor slices by design |
| **#121** | **a go-ahead, not a decision** | Confirming the orphaned `.p8` step means dispatching the release lane, which **builds and uploads a real binary to the founder's TestFlight**. Outward-facing on their App Store Connect account — ask first |

**#121 is the one to offer.** It is small, the design is already written in the issue, and it is the last piece of ADR-032's honesty bound: the step is very likely dead under manual signing, and only a real run can prove it. **Ask whether to burn a release run**, and if yes: delete the step, dispatch `release.yml`, then either close it green or record *what* consulted xcodebuild's auto-discovery path under manual signing — a genuinely useful fact either way. **Read addendum 44 first**: the previous edit to that step nearly reddened a working lane, and the guard added "for safety" is what would have done it.

**If the derivation still comes out empty, that is the session's output.** Present it as a table like the one above, with what changed since S049 and what did not, and stop. Do not invent work to fill the session — `session-rules.md` §4 is explicit that idle-improvising features is how OUT-list items sneak in. But do not assert emptiness without deriving it either: three consecutive sessions have found real defects inside objectives that looked like nothing.

**Do NOT** grant public invoker on prod, migrate RC subscriber ids, redeploy prod, re-bootstrap `match` certificates, or dispatch the release lane without asking. Each is a live-system or outward-facing action the founder owns.

**What is DONE (do not re-do):** the whole MVP **M1–M6.3 including M5.3**, consent/legal at **legal version 2**, CI→Slack, the UI/UX arc + the redesign waves, **#74**, **#29**, **#88**, **#47**, the bundle-id rename, the first dev deploy, release-lane signing, the `match` lane (#103, #117), **Node 22**, **#76**, **firebase-admin v14**, **#70**, **ADR-032** + `tool/release_lane_lint.dart` + the stale-docs reconciliation + **#99**/**#67** (S047), **#120** — the CI-generated, frozen-verified `Gemfile.lock` and lint rule 5 (S048), and **#100** — the CI cost posture, measured and decided (S049).

On completion, follow `session-rules.md` §3/§4: append to `past-prompts.md`, regenerate this file, refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
