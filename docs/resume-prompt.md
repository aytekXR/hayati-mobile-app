# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing toolchain note (M2.1–M3.4):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (Session 013/014: running `emulators:exec` from `functions/` breaks the exec script's `cd functions`): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`) or the live `answerReveal` trigger races the drive — only deliberate wire-proof e2e tests stay on `demo-hayati`. App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`; the functions emulator serves ONLY its `--project` id). Widget-test gotcha (M2.4): asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content: packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones: couple dayKey comes from `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites; never key couple state off the device zone (ADR-011). Coverage gates: app 66 (M3 ratchet), functions 80 hard / 85 target.

## Objective — Session 015: M4.1 — entitlements foundation: RevenueCat webhook Function → couple entitlement mirror, subscriptions rules, app entitlement read seam

First M4 slice (`implementation-plan.md` M4; ADR-003 pre-decided RevenueCat). Emulator-only by design: RC webhook events are mocked from RevenueCat's documented webhook schema — no RC account, no store products, no device needed. The paywall UI (M4.2) and live sandbox purchases (M4.3) come later; this session builds the server truth they sit on.

1. **`revenueCatWebhook` Function (onRequest, `europe-west1`):** authenticated via the RC `Authorization` header token (secret injected via env/config seam, emulator-testable); parses RC webhook events (INITIAL_PURCHASE, RENEWAL, CANCELLATION, EXPIRATION, PRODUCT_CHANGE at minimum; unknown types = logged no-op, never 5xx) and mirrors the entitlement onto `subscriptions/{coupleId}` (architecture §3 shape — source of truth = RC, the mirror is a projection). The couple resolves from the RC `app_user_id` (decide the mapping — uid → `users/{uid}.coupleId` lookup is the obvious shape; document in the ADR). **Idempotent + out-of-order-safe:** replayed and stale events must not regress the mirror (RC event ids/timestamps — decide the guard, prove with tests, mutation/replay proven in-process like the M3.4 latch).
2. **Entitlement semantics (THE M4 unit-test accept lines):** one purchase entitles BOTH partners (the mirror is couple-scoped by construction — prove the resolution path from either member's purchase); expiry/cancellation downgrades BOTH. Grace-period/billing-retry states: map conservatively (entitled until hard expiry), document. Pure decision core (`entitlementFromEvents` or similar) unit-tested separately from the HTTP shell — the M2.2/M3.2 pure-core pattern.
3. **Rules:** `subscriptions/{coupleId}` member-only READ, ALL client writes denied (function-only, admin SDK — the M3.2 `days` pattern) + mutation tests both ways.
4. **App: entitlement read seam** — `EntitlementRepository` watching `subscriptions/{coupleId}` behind the usual provider seam (throw-until-overridden), a domain `CoupleEntitlement` read model (entitled/free + product tier + expiry), and the free-tier gating DECISION POINT as a provider (`isPremiumProvider` or similar) — no UI gating flips this session (nothing to gate until the paywall exists); widget-test the provider states. No `purchases_flutter` SDK yet (M4.2+, needs RC keys).
5. **Docs-with-code:** ADR-013 (webhook auth + event model + app_user_id→couple resolution + out-of-order guard + conservative grace mapping), architecture §3 (subscriptions shape now real) / §4 (entitlements flow status) / §8 if the webhook adds a security surface note, test-suite §1, implementation-plan M4 progress, this file, operator-expected refresh (new near-term item: create the RevenueCat account + App Store Connect app record once Apple enrollment lands — needed by M4.2, not by this session).

**Acceptance criteria:** webhook rejects bad/missing auth token (401) and malformed bodies (4xx, never 5xx-loops); replay + out-of-order event tests prove the mirror never regresses; one-purchase-entitles-both and expiry-downgrades-both proven at the service level; `subscriptions` rules member-read/function-write-only with mutation tests; app entitlement provider renders entitled/free/loading/error states in tests; functions suite green (coverage ≥80 hard gate); app suite green (coverage ≥66); all CI jobs green.

**External dependencies (founder):** none for this session (mocked RC events). Heads-up recorded in operator-expected: RC account + ASC app record become blocking at M4.2.

**Files likely to change:** `functions/src/entitlements/**` (new), `functions/test/**`, `firestore.rules` (+subscriptions block + suite), `app/lib/features/entitlements/**` (new feature dir), `app/test/**`, `docs/adr/013-*`, docs per item 5.

**Validation steps:** functions gate (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from repo root**) + app gate (`flutter analyze` → `flutter test` → coverage ≥66) ; push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium — one HTTP Function with a pure decision core, one rules block, one app seam; the subtlety is the out-of-order/replay guard and the app_user_id→couple resolution. **Estimated duration:** one session (2–3 h). If it overflows: land the webhook + mirror + rules first (the M4 unit-test accept lines); the app seam slices cleanly to M4.1b.

**Stopping conditions:** if RC's webhook schema has ambiguities that matter (entitlement vs product granularity), pin the simplest couple-scoped mapping (one `premium` entitlement id), mark it an ADR decision, move on — the paywall session revisits with real RC config. If the `app_user_id` mapping fights (e.g. anonymous-purchase edge), record the constraint, mirror only resolvable events, and count unresolvable ones loudly.

**Explicitly out of scope this session:** paywall UI/goldens (M4.2); live `purchases_flutter`/RC SDK wiring and sandbox purchases (M4.2/M4.3 — need RC account + ASC products + Mac); gift flow (M4.3); free-tier gating UI flips (M4.2, alongside the paywall); the private thread (**recorded decision, Session 014: parked to M5 scope selection** — core-loop UX polish, revisit when M5 starts or if dogfooding demand pulls it earlier); `invitePreview.questionText` (**recorded decision, Session 014: deferred** — couple questions exist only post-join, so the preview has no real question to show; revisit with W9 couple packs); seasonal selection (issue #29, W9); Android (M6.5); any deploy.

**M3.4 handoffs to keep in view (binding):** `answerReveal` is the ONLY writer of `couples.streak` and day-doc `revealedAt` — nothing new may write either (rules freeze streak; the day doc is client-write-denied); the reveal invariant remains server-side and untouched; push payloads must NEVER carry question/answer content (payload policy is the single compose path — route any new push through `composePush`); `users.fcmTokens` is still unwritten (app capture = on-device slice); the at-risk pass shares the rollover sweep's single couples read — a new sweep consumer must thread the same buckets, not re-query.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M4.2 — paywall UI + free-tier gating), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
