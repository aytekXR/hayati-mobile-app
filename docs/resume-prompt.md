# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing review-ordering note (Sessions 015–018, now FOUR-for-four):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code finds; S016: 1 blocking; S017: 3 confirmed incl. a false paying-couple downgrade; S018: 2 blocking + 10 serious, incl. a TR-diacritic under-trigger in the CRISIS filter and a DV-relevant counter leak). **S017 addendum: if a review workflow dies mid-run, adjudicate the unverified findings BY HAND — do not drop them.** S018 addendum: when two verifier lenses disagree, the governing doc decides (the rate-limiter dispute was settled by `architecture.md` §1's explicit mandate).
>
> **Standing toolchain note (M2.1–M5.1):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (Sessions 013–015: running `emulators:exec` from `functions/` breaks the exec script's `cd functions`; and never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`) or the live `answerReveal` trigger races the drive. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`** (`RC_WEBHOOK_TOKEN=demo-rc-webhook-token`; the emulator loads `.env.<projectId>` dotenv files — no CI or command change needed; real env files are gitignored). **M4.2: `purchases_flutter` is pinned `^10.4.1` behind the `PurchasesRepository` seam** — ALL app tests run against `test/support/fake_purchases_repository.dart` + `purchases_fixtures.dart`; the real adapter configures only when the `REVENUECAT_IOS_API_KEY` dart-define is non-empty (fail-closed otherwise). **M5.1: the coach's wire contract is FROZEN in ADR-016** (`failed-precondition`/`not-premium`, `resource-exhausted`/`cap-daily`|`cap-monthly`|`rate-limited`, `permission-denied` = non-member only, `unavailable` = provider/infra, response `{kind:'reply'|'help', category?, text, remaining?}`) and the deployed default provider is the fail-closed `UnconfiguredCoachProvider` — LLM tests run against `FixtureCoachProvider` + the recorded `functions/test/fixtures/coach-provider-fixtures.json` ONLY, never a live model (test-suite §4). App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`). Flutter lives at `~/flutter/bin` (not on the default PATH). Widget-test gotcha (M2.4): asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content: packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones: couple dayKey comes from `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites (ADR-011). **Coverage gates: app 68** (the gate script runs from the repo root: `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info`), **functions 80 hard / 85 target**.

## Objective — Session 019: M5.2 — the coach chat UI on the proven spine + persona scaffolds + the private-thread scope decision

Second M5 slice (`implementation-plan.md` M5; PRD F5). M5.1 shipped the safety spine and the `coachProxy` server seam — emulator-proven, fail-closed, zero live calls. This slice mounts the couple-facing chat experience on it, drafts the persona system-prompt scaffolds, and CLAIMS the private-thread scope decision that has been parked since M4.

1. **Scope precisely at session start** (session-rules §1.5) and **write ADR-017 FIRST, then adversarially review it BEFORE code** (four-for-four; see the standing review-ordering note). Decisions that come due: **the chat surface** (per-persona threads vs one coach surface with a persona switcher; where it mounts — behind the paired-home, premium-gated via `PremiumGate` + `isPremiumProvider` per ADR-014); **window construction** (the client owns the context window sent to `coachProxy` — size, trimming, and the ADR-016-pinned drop-or-retain decision for crisis turns: a retained crisis turn re-routes every call to the help path — decide and document the UX); **conversation persistence** (`coach_sessions` with TTL-30d per architecture §3, or ephemeral in-memory-only this slice — this IS the private-thread scope decision; if persistence ships, rules + mutation tests come with it, and note NO native TTL policy exists yet in this repo); **persona system prompts** (Coach / Date Genie / Gift Genie content scaffolds, register-aware per brandkit — server-side in the provider path, TR/AR/EN, founder native review flagged like all copy); **the quota surface** (render `remaining` from the response hint; if a live watch on `coachUsage` is used, the client must replicate the lazy-reset key comparison via `coupleDayKey` — ADR-016 documents the staleness trap).
2. **The firm deliverable: the chat UI end-to-end against the emulator** — a premium couple member converses (fixture replies), sees the disclaimer before first use, hits the help path rendered with distinct non-persona treatment (`kind:'help'` — never a chat bubble that looks like the persona), sees honest states (unavailable / cap-daily vs cap-monthly distinct / rate-limited / not-premium → paywall via the frozen `failed-precondition` contract), and the free tier sees ZERO coach surface (PremiumGate + the free-tier-untouched assertion suite extended).
3. **Docs-with-code:** ADR-017; architecture §2/§4 (+§3 if persistence ships); test-suite §1; implementation-plan M5.2; this file; operator-expected (persona copy joins the native-review scope; provider decision item 6 becomes ACTIVE if the founder answered).

**Acceptance criteria:** coach chat screen shipped behind `PremiumGate` with goldens (six-cell {TR,AR,EN}×{LTR,RTL} + scale-130 for the chat + disclaimer + help-path states); the help path renders visually distinct from persona replies and is pinned by widget tests; every frozen wire outcome has typed app-side mapping + UX (`not-premium` → paywall push, `cap-daily`/`cap-monthly` distinct copy, `rate-limited`, `unavailable` honest state); the crisis-turn window decision implemented + tested; free tier assertion-proven untouched (no coach surface, no paywall interstitials in the daily loop); disclaimer shown before first coach use (per PRD F5) and dismissal persisted; persona scaffolds drafted TR/AR/EN with native review flagged; app suite green (gate 68) incl. `dart run build_runner build`; functions suite untouched-green (gate 80) unless persistence ships (then its rules/tests ride along); all CI jobs green.

**External dependencies (founder):** **none required.** If the founder has answered item 6 (provider choice + key), the live adapter MAY be scoped in as a stretch — but the accept lines above must hold hermetically (fixtures) regardless; do NOT block on the key. The crisis-content native review (★ item) blocks only on-device coach use, not this slice.

**Files likely to change:** `app/lib/features/coach/**` (new feature: domain/data/presentation), `app/lib/core/l10n/arb/*` (coach + disclaimer + error copy), goldens; `functions/src/coach/` only if persona scaffolds/persistence land server-side; `docs/adr/017-*`, docs per item 3.

**Validation steps:** app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); functions gate if touched (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from the repo root**, echo `EXIT_CODE`); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium — the server contract is frozen and proven, so this is primarily a Flutter feature slice; the subtle parts are the crisis-turn window UX, the persistence scope decision, and register-aware persona copy. **Estimated duration:** one session. If it overflows, the honest split is: chat UI + wire mapping this session, persistence + persona-copy polish next — do NOT stretch (session-rules §1.5).

**Stopping conditions:** if the persistence decision needs founder input (TTL/retention posture is a privacy stance), ship ephemeral-only, record the options in `operator-expected.md`, and proceed — the chat UI does not depend on it. If persona copy quality in AR is not confidently draftable, ship EN/TR + a minimal AR scaffold and flag the AR gap in the native-review item (over-honest beats over-confident).

**Explicitly out of scope this session:** the live provider adapter unless the founder key exists AND the hermetic accept lines are already green (stretch only); any deploy; hotline numbers (founder-gated ★ item); M4's sandbox slice (item 0); Android (M6.5); seasonal selection (#29, W9); `coach_msg` analytics emission (pre-launch M6 item — but its shape is bound to `logCoachEvent` by ADR-016).

**M5.1 handoffs to keep in view (binding):** the wire contract is FROZEN (see the toolchain note) — the app maps outcomes by CODE first (`details.reason` may not survive the platform channel; the existing `mapFunctionsFailure` buries `resource-exhausted` in the generic bucket, so the coach repository needs its OWN taxonomy — ADR-016 review finding); `remaining` is an optional point-in-time hint; the help path (`kind:'help'`) must never render as a persona bubble; the crisis pre-scan means a crisis turn retained in the window routes EVERY subsequent call to help — the window decision is M5.2's, pinned in ADR-016; every premium surface mounts on `PremiumGate` + `isPremiumProvider(coupleId:)` (ADR-014); the entitlement mirror + `isPremiumMirror` remain the only premium truth (ADR-013 D5).

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M5.3 — live provider adapter + coach hardening, or the first slice of M6 if item 6 is still unanswered), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
