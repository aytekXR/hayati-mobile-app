# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5 (whose *timing* is a founder decision informed by Gate 3 — it is not the automatic next slice).
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored). **S026 addendum: `.claude/skills/` is now gitignored the same way** — a fresh machine runs `uipro init -a claude` once if it wants the corpus, and **only the `ui-ux-pro-max` skill may be invoked in this repo** (ADR-025 D9; the other six generate brand/marketing assets and brandkit v1.0 is final).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. **Identify your OWN claude PID first** (walk `$$` up the ppid chain) or you will report yourself as the intruder. If a genuine other session is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. **Inherited uncommitted work: checkpoint-commit VERBATIM first, verify after.** **S019 addendum: an implementation agent died mid-run on a session-limit API error** — checkpoint-commit-immediately meant zero loss. **S022 addendum: parallel implementation agents are safe only when their file sets are disjoint BY DESIGN and every shared file is reserved to the orchestrator.** **S023 addendum: an implementation agent's cross-file coordination warning was FALSE** — verify agent coordination claims by direct inspection before acting. **S025 addendum: a background mutation-check that times out can leave a MUTANT in the working tree** — always diff against the pre-mutation backup and restore before committing. **S029 addendum: the sibling claude at `/home/aytek/repo` (the parent) was working `repo-blueprint`, not hayati — confirm by walking its child processes' cwd/args, not just its own cwd, before treating a parent-dir session as a conflict.** **S030 addendum: the same holds — the four other claudes were on `evrak`/`repo`(parent)/`ams-pulse`/`unhooked`; none on hayati.**
>
> **Standing review-ordering note (Sessions 015–031, now EIGHTEEN consecutive pre-code passes):** write the ADR (or the slice design), commit it, then adversarially design-review it BEFORE writing code — that is where the defects are. Use 4–5 lenses × 2 independent verifiers (a refuting skeptic + a governing-docs adjudicator), **and run the review TWICE: once on the design, once on the built diff** (for a genuinely one-line change, a single combined pass is proportionate — S031). Addenda that keep paying: guarantee-vs-mechanism gaps are the highest-value class; when two lenses disagree, the governing doc decides; MUTATION-CHECK every guard AND the test; policy in a YAML `if:` is invisible to the suite; a safety claim in a CODE COMMENT is a guarantee surface; a stale operator item is worse than no item; a golden matrix proves SCREENS, not transient surfaces (dialogs/snackbars/motion/haptic need a widget test). **S030 addenda: (1) the aggregation of a verified-findings pass must surface a finding when EITHER verifier flags it real — a split where the refuting skeptic says REFUTED but the governing-docs adjudicator says REAL is the valuable one and must not be buried by keying only off the skeptic (three of S030's pre-code findings were such splits and all three were worth applying). (2) A cross-surface "every X is Y" spacing rule must be pattern-scoped, not universal — S030's "every primary→secondary gap is x4" had a live counter-example in ProviderActions' homogeneous x3 provider stack; the fix is a carve-out, recorded so a future Class-G session does not "normalise" that x3 and churn undeclared goldens. (3) A motion widget test must prove BOTH halves of a §6 unfold — an opacity-only test leaves the "gentle rise" unproven and misses a sign-inversion or zero-slide bug; assert the child's on-screen position delta ≈ `MotionTokens.revealSlide` and settle-to-zero. (4) `SafeArea` inside `Scaffold.bottomNavigationBar` needs no worry about top status-bar dead space — Scaffold strips top padding from that slot (`removePadding(removeTop:true)`) — but pin `top:false` anyway to make it explicit rather than implicit.** **S031 addenda: (5) when a slice's audit turns up NOTHING but one small defect, the honest slice is that one change — do not manufacture churn to fill the D7 golden count (slice 4 = 1 change / 9 goldens, and that was correct). (6) A "must stay byte-identical" acceptance is a LEAK-CHECK, not an absolute freeze — a deliberate, declared, reviewed improvement to that very surface is permitted under the D8 declare-and-verify discipline; verify no OTHER probe moved. (7) The motion restraint discriminant is SURPRISE vs deliberately-navigated: a `SoftUnfoldReveal` earns its place on a surprise reveal (S030's "who invited you"), NOT on a confirmation the user chose to open (S031's "You're Premium" got none).**
>
> **Standing toolchain note (M2.1–M6.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build`); the functions emulator never compiles TS. Full suite **from the repo root** (echo the exit code): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs use the trigger-isolated `demo-hayati-notrigger` project** and **(S023) must seed writer consent**. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`.** **M4.2: `purchases_flutter` pinned `^10.4.1` behind the `PurchasesRepository` seam.** **M5.1: the coach wire contract is FROZEN in ADR-016**; deployed default = fail-closed `UnconfiguredCoachProvider`. **M6.1 (ADR-018, rev 4): the four lock invariants are binding: (1) NOTHING may `ref.invalidate(privacyLockControllerProvider)`; (2) every mutating controller op re-checks the generation token after EVERY await; (3) `biometricOnly: true` is source-sentinel-pinned; (4) no `showDialog`/`Overlay`/`Tooltip`/`tooltip:`/`IconButton`/text-selection field/`ScaffoldMessenger.of` in lock-screen children — mechanism: `lock_screen_forbidden_api_sentinel_test.dart`, scan set DERIVED from `lock_screen.dart`'s relative-import graph.** **M6.2 (ADR-019): the seven cascade invariants are binding; the deletion notice sends NO push; export `formatVersion` is 2.** **M6.3 (ADR-020/021/022):** `release.yml` is tag-triggered + dispatchable; `sign-upload` fails CLOSED until the three `ASC_*` secrets exist in the **`release` environment** (operator item 4); `--allow-empty-urls` is a ratchet; the pre-frame bootstrap await set is PINNED by `entrypoint_sentinel_test.dart`. **S023 (ADR-023) — the consent/legal layer is BINDING:** `users.consent` is server-owned; the three-way legal-version source-sentinel means a legal-text revision bumps ALL THREE in the same diff — M5.3 (the live LLM) is a RECORDED re-consent trigger; `docs/legal/` is byte-synced to `app/assets/legal/` under a drift test; withdrawal is PROSPECTIVE by DV doctrine. **S025 (ADR-024) — CI → Slack:** `tool/ci/slack_notify.sh` is the single notifier; it has NO VOTE on the build (every error path `exit 0`s AND `continue-on-error: true`); ALL policy lives in the script, never in a YAML `if:`; the secret is repository-scoped, NOT `environment: release`; ci.yml's concurrency keys push events on the COMMIT (D8). **S026–S031 (ADR-025) — the arc is SCOPED, its firewall BUILT, and slices 1+2+3+4 shipped (slice 4 commerce = ONE change: the gated pitch h1→h2; the surfaces were already brandkit-compliant). The firewall: the lock-screen forbidden-API sentinel, the brandkit→Dart token-parity test (bidirectional; `iconography.*` unasserted until #63), and the 96-pair frozen-sentence digest over the ★ safety + `consent*`/`legal*` strings. A slice that turns any of them red has broken something, not found a nuisance. Motion is a §6 RULE realised in `MotionTokens` (150–300ms, ease-out), review-enforced, NOT parity-tested (#71); slices 2 (daily reveal) and 3 (pairing activation) both use it. The shared §6 unfold now lives in `core/widgets/soft_unfold_reveal.dart`; slice-2's file-local `_RevealUnfold` is a DRY follow-up (#74).** Theme gotcha (S019): global `FilledButton` has infinite min-width — override `minimumSize` inside Rows. App-side: `.g.dart` committed — `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines `USE_AUTH_EMULATOR`/`USE_FIRESTORE_EMULATOR`/`USE_FUNCTIONS_EMULATOR` (functions regional: `europe-west1`). Flutter at `~/flutter/bin` (`dart` is `~/flutter/bin/dart`, not on PATH). Widget-test gotchas: asset-backed providers use `test/support/static_asset_bundle.dart`; goldens are Linux-canonical. Content: packs authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync`. Timezones: couple dayKey = `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json`. **Coverage gates: app 68** (`~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root), **functions 80 hard / 85 target**.



## Objective — Session 032: **ADR-025 slice 5 — coach.** Chat, disclaimer, help path. Class **G + ★** — the highest-stakes slice for the frozen-sentence digest.

**FIRST, BEFORE ANY CODE — the preemption checks, in this order:**
1. **`docs/operator-expected.md` item 6** (LLM provider + API key): answered → **abandon this objective and do M5.3** (the live coach adapter behind ADR-016's frozen wire contract). It has priority the moment it is unblocked — the ONLY planned MVP session-unit left. **Binding S023 handoff: M5.3 is a recorded re-consent trigger** — its ADR bumps the legal version (all three sources, same diff), names the provider in the revised notice/policy, re-gates every user, adds the provider row to `docs/dpa-inventory.md`; the frozen-sentence digest covers every `consent*`/`legal*` string, so the notice rewrite turns it red — re-stamp it deliberately, in the same diff.
2. **Blaze flipped (item 2)?** → the **first-deploy slice** (all eleven Functions to `hayatiapp-dev`; verify the Cloud Scheduler trigger, Eventarc retry and `RC_WEBHOOK_TOKEN` Secret Manager binding). **Verify Blaze FACTUALLY** — `gcloud` is not on this box, so mint a token from the firebase-tools refresh token (`~/.config/configstore/firebase-tools.json` → OAuth exchange with the public firebase CLI client) and GET `cloudbilling.googleapis.com/v1/projects/<id>/billingInfo` for **both** projects. (S028–S030 did this; all `billingEnabled:false`.)
3. **Founder green-lights Android timing (Gate 3 / ADR-006)?** → M6.5 planning session.
4. **On-device defect reported from the ★ recipe?** → triage preempts any planned slice.
5. **"Build the dev-rig slice"?** → fold in or re-scope.
6. **Answers on #67 (muted/outline tokens), #63 (Phosphor), #71 (motion token), or #74 (DRY the unfold)?** **#67 is the one to watch for slice 5** — a chat surface is where a muted timestamp / secondary caption / divider tone is most tempting; the brandkit defines none, so if the coach chat wants one it STOPS (slice 1's precedent). **#63 = migrate** → slice 1.5, its own session and golden era; **#71/#74** are tidy-ups, fold into whichever slice next touches motion. If answered, record and fold into the appropriate slice, not this one.
If none fire: build slice 5 below.

**Why this is next:** ADR-025 D7 orders the arc; with commerce done (slice 4), coach is the queued next. **0 and 1 may not move off the front; 8 stays last; slices 5–7 the founder may re-order — but this is the queued next.**

**Scope — 3 surfaces (ADR-025 D7 row 5), 27 goldens, Class G + ★:** the coach **chat**, the **disclaimer**, the **help path** — all in `coach_screen.dart` (states `conversation`, `disclaimer`, `help_path`, each × sixCells + a scale130 natural triple = 27). Composition, hierarchy, spacing, motion — **not** copy (Class G AND the ★ safety strings), **not** the send/transcript state machine, **not** the coach wire contract (ADR-016, FROZEN), **not** the crisis/help routing.

**Acceptance criteria (ADR-025 D7 row 5):** **`CoachHelpCard` remains a structurally DISTINCT widget TYPE from `CoachPersonaBubble`** (the help/crisis surface must never be confusable with an ordinary coach message — a structural, not a stylistic, guarantee); **the help-sticky latch still replaces the composer** (once help shows, the input is replaced by the help affordance — behaviour, do not "tidy" it); **ZERO ★ strings changed — the frozen-sentence digest stays green** (`coachDisclaimerTitle/Body/Cta`, `coachHelpTitle`, `coachPausedBody` are pinned; a reword turns `frozen_string_digest_test` red and is a SAFETY event, not a nuisance); every coach state still renders every state it did; Appendix A asserted; the declared golden set re-baselined per D8 with `git status --porcelain` pasted beside the declaration; **the three slice-0 guards green and unweakened**; `flutter analyze` clean; full suites green; coverage ≥68.

**The traps, named so they are checked rather than discovered:**
- **★ COPY IS FROZEN BY DIGEST.** The disclaimer + help/crisis + paused strings are the strings the ★ gate exists to protect. Re-lay-out and re-theme freely; reword NOTHING. If the digest turns red, you reworded a safety string — that is the defect, not the test. (Class G for the rest of the coach copy too: reword nothing; a native-review reset would go to operator item 1.)
- **`CoachHelpCard` vs `CoachPersonaBubble` — a TYPE distinction, not a colour one.** They must stay different widget TYPES so the state-ladder tests (`find.byType`) keep proving the help surface is not a persona bubble. Do not merge them into one styled widget even if it looks DRYer.
- **The help-sticky latch replaces the composer** — that is behaviour (the crisis/help path takes over the input). Layout only; do not touch the latch or the composer-replacement.
- **`onSurfaceVariant`/`outline` are still unset (#67).** A chat wants muted timestamps / a divider between messages / a secondary caption — the brandkit defines none, so it STOPS. Do not invent one.
- **Motion, if added, is a §6 rule** (like the reveal/activation unfold): reuse the shared `SoftUnfoldReveal` (`core/widgets/`) or `MotionTokens`, keep it review-enforced, do NOT add it to the parity test, and prove any transient motion with a widget test (no golden sees it). A message-enter animation is tempting on a chat — weigh it against restraint (≤1–2 animated elements/view) and the fact that goldens capture settled state only.

**Files likely to change:** `app/lib/features/coach/presentation/coach_screen.dart` (verify the exact widget tree — `CoachHelpCard`, `CoachPersonaBubble`, the composer, the disclaimer), possibly `core/widgets/` if a shared motion is reused, the declared golden set, `docs/adr/025-*.md` (slice-5 note), `docs/test-suite.md` if the golden matrix changes shape. **Do NOT touch `functions/src/coach/**` (the ★ lexicon/help/prompts live server-side; out of a UI slice).**

**Validation steps:** `cd app && ~/flutter/bin/flutter analyze && flutter test` · `~/flutter/bin/dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from the repo root · **confirm `frozen_string_digest_test` GREEN (no ★/consent/legal reword)** · **declare the golden set BEFORE `--update-goldens`, then paste `git status --porcelain -- 'app/test/**/*.png'`** · **look at the regenerated images, do not just count** · PR, watch, squash-merge · **watch the post-merge MAIN run** (`integration-emulator` is main-only; slice 5 touches app code, so it runs) · `codegraph sync`.

**Complexity:** medium-high — 27 goldens, the frozen ★ digest (the hardest copy boundary in the app), and the `CoachHelpCard`-type guarantee. Fan out the golden regen and LOOK at the images.

**Stopping conditions:** if improving a surface requires a colour, a motion value or a type step the brandkit does not define, **STOP and record it** (slice 1's precedent). If the ★ digest, a `CoachHelpCard`-type test, or the help-latch behaviour turns red, that is a SAFETY/guarantee regression, not a test to update.

**Explicitly out of scope:** slices 6–8; any copy edit (Class G + ★); the send/transcript state machine; the coach wire contract (ADR-016); the crisis/help routing; `functions/src/coach/**`; #63's Phosphor migration; #67's/#71's/#74's decisions; M5.3 unless item 6 answered; any deploy; hosting/domain; M6.5; signing/TestFlight; screenshots; hotline numbers (★); `coach_sessions`; the legal documents; any legal-version bump.

**S031 handoffs (binding):** the slice-0 guards are live — a slice that turns one red has broken something. `onSurfaceVariant`/`outline` stay unset until #67. **Motion is a §6 rule; the shared `SoftUnfoldReveal` (`core/widgets/`) is the home; a soft-unfold earns its place only on a SURPRISE reveal (slice 3's pairing activation), NOT a deliberately-navigated destination (slice 4's entitled view got none — the restraint discriminant).** The free-tier-byte-identity lesson generalises: a "must stay byte-identical" acceptance is a LEAK-CHECK ("must not move *unless declared*"), so a deliberate declared improvement to that very surface is permitted — but declare it and verify no other probe moved. The standing S028–S031 lesson: a golden matrix proves screens, not transient surfaces — motion/haptic/dialogs/snackbars need widget tests, and a motion test must prove BOTH the fade AND the rise.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file (expected next: whichever preemption fires, else **ADR-025 slice 6 — settings & data rights** (settings, PIN setup, PIN verify dialog, delete account, export, couple-ended notice, `SettingsErrorLine`; 55 goldens; Class N; the delete confirmation still reads irreversible + says the shared space goes for both, `SettingsErrorLine` still shared not forked per screen)), refresh `docs/operator-expected.md`, commit, push, verify CI, watch the post-merge main run, then `codegraph sync`.
