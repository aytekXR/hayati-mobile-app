# Resume Prompt

> This file contains ONE and only ONE objective. Every session executes ONLY this file. (See `project-rules.md` #1, `session-rules.md`.)
>
> **Standing de-gating note (ADR-007):** engineering milestones M1→M6 proceed without content-ops preconditions. Gates 1–3 are decision instruments for marketing/spend/launch posture, not build blockers. TikTok/content-ops work is out of session scope unless the founder re-activates it. First release target: the founder couple's own devices (personal-use-first).
>
> **Standing sequencing note (ADR-006):** iOS-first — milestones validate and ship on iOS first; Android work is re-sequenced into M6.5.
>
> **Standing tooling note (CodeGraph, founder directive 2026-07-09):** orient with CodeGraph at session start and use it for symbol/call-path/impact navigation throughout — `codegraph_explore`/`codegraph_node` MCP tools (CLI fallback `codegraph explore|node|callers`); sub-agents and workflow agents use the same tools via ToolSearch. Before the session ends, `codegraph sync` after the merge (session-rules §1 step 4 / §3 step 6). The index is machine-local (`.codegraph/`, gitignored).
>
> **Standing session-hygiene note (Sessions 013 + 014 incidents):** before writing ANYTHING, check for another live Claude session on this repo — `tmux ls` + `ps aux | grep claude` + `readlink /proc/<pid>/cwd` (the repo or its parent both count) and recent non-self mtimes in the tree. If one is alive, stop and hand the founder its PID/tmux window. **Session 014 addendum: an external `git pull --autostash` from a concurrent process swept the entire uncommitted tree into a stash mid-implementation** (recovered without loss, but only because the sweep landed in a recoverable stash). Mitigations that worked: commit design docs BEFORE launching workflows, checkpoint-commit implementation output IMMEDIATELY when a workflow returns, and if a stash named `gitPull auto-stash …` appears, verify then `git stash drop` — never blind-`pop`. Inherited uncommitted work: checkpoint-commit verbatim FIRST, verify after.
>
> **Standing review-ordering note (Sessions 015–017, now three-for-three):** write the ADR, commit it, then adversarially design-review it BEFORE writing code — that is where the defects are (S015: 12 pre-code finds, 0 post-code; S016: 1 blocking pre-code; S017: 3 confirmed pre-code, including one that would have **falsely downgraded a paying couple**). **S017 addendum: if a review workflow dies mid-run (usage limit, crash), adjudicate the unverified findings BY HAND — do not drop them.** All three of S017's confirmed findings came from the batch its workflow never got to verify.
>
> **Standing toolchain note (M2.1–M4.3):** the functions/rules emulator suites need Java 21+ on PATH (`~/.local/share/java/jdk-21.0.11+10-jre/bin` on this box — firebase-tools reads PATH only, never JAVA_HOME) and global `firebase-tools@15.22.4`. Build `functions/` first (`npm run build` = `tsc` + `scripts/bundle-packs.mjs`); the functions emulator never compiles TS. Full suite **from the repo root** (Sessions 013–015: running `emulators:exec` from `functions/` breaks the exec script's `cd functions`; and never trust a piped exit code — echo it): `firebase emulators:exec --only auth,firestore,functions --project demo-hayati 'cd functions && npm run test:ci'`. **M3.4: in-process tests that SEED answer docs must use the trigger-isolated second project** (`noTriggerFirestore`, `demo-hayati-notrigger`, in `functions/test/support/admin.ts`) or the live `answerReveal` trigger races the drive. **M4.1: the webhook's emulator token comes from the committed demo-only `functions/.env.demo-hayati`** (`RC_WEBHOOK_TOKEN=demo-rc-webhook-token`; the emulator loads `.env.<projectId>` dotenv files — no CI or command change needed; real env files are gitignored). **M4.2: `purchases_flutter` is pinned `^10.4.1` behind the `PurchasesRepository` seam** — ALL app tests run against `test/support/fake_purchases_repository.dart` + `purchases_fixtures.dart`; the real adapter configures only when the `REVENUECAT_IOS_API_KEY` dart-define is non-empty (fail-closed otherwise). App-side: `.g.dart` files are committed — run `dart run build_runner build --delete-conflicting-outputs` in `app/` after adding providers; ARB edits regenerate on `flutter pub get`; emulator dart-defines are `USE_AUTH_EMULATOR` / `USE_FIRESTORE_EMULATOR` / `USE_FUNCTIONS_EMULATOR` (functions instance is regional: `europe-west1`). Flutter lives at `~/flutter/bin` (not on the default PATH). Widget-test gotcha (M2.4): asset-backed providers in widget/golden tests use `test/support/static_asset_bundle.dart`. Content: packs are authored under `content/packs/` ONLY — `dart content/validator/validate.dart --sync` regenerates `app/assets/content/`. Timezones: couple dayKey comes from `coupleDayKey(now, couples.timezone)` (Dart) / `localDayKey` (TS), byte-pinned by `functions/test/fixtures/day-key-parity.json` on BOTH suites (ADR-011). **Coverage gates: app 68** (ratcheted at the M4 close; measured 86.50%; the gate script runs from the repo root: `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info`), **functions 80 hard / 85 target**.

## Objective — Session 018: M5.1 — AI coach v0: scope decision + the `coach_proxy` Function foundation, safety spine first

First M5 slice (`implementation-plan.md` M5; PRD F5; `mvp.md` IN #7). **M4's engineering is closed** — its only open item is the founder-gated sandbox purchase (operator item 0), which no session can advance. M5 is the next build.

**This slice is the safety spine and the server seam, NOT the chat UI.** The coach is the first feature in this codebase that puts a *generative* system in front of a couple in crisis, in three languages, and the accept line is a safety line: *seeded crisis phrases in all 3 languages route to the professional-help path, never to the persona.* Build that first, prove it, and let the UI (M5.2) mount on something that is already safe.

1. **Scope precisely at session start** (session-rules §1.5) and **write ADR-016 FIRST, then adversarially review it BEFORE code** (three-for-three; see the standing review-ordering note). The decisions that come due: **which LLM provider and how it is sealed behind a seam** (the repo's discipline is a fakeable port — the coach must be contract-tested against **recorded fixtures**, never a live model, so CI is hermetic and free); **the crisis-lexicon design** (TR/AR/EN seed lists — a *pre*-filter that runs before the model is ever called, plus a post-filter on the response; normalization is the hard part: Arabic diacritics/hamza forms, Turkish dotless-ı casefolding, leetspeak/spacing evasion — a naive `includes()` is a safety hole, and the register-owner review of the lexicons is an operator item); **where the caps live** (per-user daily + per-couple monthly, Remote Config per PRD — decide the storage: a Firestore counter doc keyed by couple/day, transactional, with the same lane/total-order discipline the entitlement mirror uses); **premium-gating** (coach is premium-only, free tier gets ZERO — it mounts on `isPremiumProvider`/`PremiumGate`, the ADR-014 seam, and the server must gate it too: never trust the client's premium claim — the `subscriptions/{coupleId}` mirror is the server's own truth and the Function must read it).
2. **The safety spine is the firm deliverable:** the crisis detector + the help-path response, as a **pure, exhaustively-tested core** (the `streak.ts`/`entitlement-core.ts` mold — no network, property-tested against evasion mutations), wired into a `coach_proxy` Function that **fails CLOSED to the help path on any doubt**. A crisis phrase must never reach the persona, never reach the provider, and never appear in a log or an analytics payload (`no coach text in analytics` is an explicit PRD accept line — assert it).
3. **Docs-with-code:** ADR-016; architecture §3/§4/§8/§10; test-suite §1; implementation-plan M5.1; this file; operator-expected (the lexicon native-review item joins the existing content-review item 1).

**Acceptance criteria:** the crisis detector routes every seeded phrase (TR/AR/EN, including normalization/evasion variants) to the help path with **zero** persona/provider calls, proven by tests that go red if the normalizer is weakened; the `coach_proxy` Function is emulator-proven against **recorded LLM fixtures** (no live calls in CI) with the premium gate enforced **server-side** off the entitlement mirror and the cap-exhaustion path tested; **no coach text in any log or analytics payload (asserted)**; disclaimer copy drafted (TR/AR/EN, founder native review flagged); functions suite green (gate 80); app suite green (gate 68); all CI jobs green.

**External dependencies (founder):** **none for this session.** An LLM provider API key becomes an operator item **only when a live call is first made** — this slice runs on recorded fixtures by design, so it is not blocked. Flag the key + the crisis-lexicon native review in `operator-expected.md` as *becoming* due at M5.2/M5.3. Operator item 0 (RC account) still blocks **only** M4's sandbox proof — do not let it block M5.

**Files likely to change:** `functions/src/coach/**` (+ tests), `firestore.rules` (the cap counter doc — function-write-only, member-read, mutation-tested), `docs/adr/016-*`, docs per item 3. App: none expected this slice (UI is M5.2).

**Validation steps:** functions gate (`npm run lint` → `typecheck` → `build` → full `emulators:exec` **from the repo root**, echo `EXIT_CODE`); app gate (`flutter analyze` → `flutter test --coverage` → `dart tool/coverage_gate.dart --min 68 app/coverage/lcov.info` from repo root); push PR; `gh pr checks --watch`; squash-merge; `gh run watch` on main; `codegraph sync`.

**Complexity:** medium-high — the safety core is subtle (multilingual normalization is where these systems fail), and the provider seam + caps + server-side premium gate are three new surfaces. **Estimated duration:** one session. If it overflows, the honest split is: safety core + provider seam this session, caps + rules next — do NOT stretch (session-rules §1.5).

**Stopping conditions:** if the LLM-provider decision cannot be made without founder cost input, record the seam as provider-agnostic (fixtures only), name the candidates with their costs in `operator-expected.md`, and proceed — the safety core does not depend on the choice. If the Arabic/Turkish normalization research shows the seed lexicons need a native speaker to be *safe* (not merely idiomatic), ship the mechanism with the conservative (over-triggering) lexicon and make the native review a **blocking** operator item before any coach ships to a device — an over-triggering crisis filter is safe; an under-triggering one is not.

**Explicitly out of scope this session:** the chat UI (M5.2); the coach's persona *content* beyond the system-prompt scaffolding; the private thread (still an M5 scope decision, not yet claimed); any deploy; the M4 sandbox slice (founder-gated, item 0); Android (M6.5); seasonal selection (#29, W9).

**M4 handoffs to keep in view (binding):** the entitlement mirror is the ONLY source of premium truth — **the coach Function must read `subscriptions/{coupleId}` server-side and must not trust a client claim**; `entitled` is never sufficient alone (pair it with the `expiresAtMs` future-check — ADR-013 Decision 5, binding on every consumer, server-side included); every premium *surface* mounts on `PremiumGate` + `isPremiumProvider(coupleId:)` (ADR-014); and the first session that runs with a real `REVENUECAT_IOS_API_KEY` owes three parked items: the sandbox purchase (M4's accept line), the identity-sync cursor hardening (ADR-014), and **RC-REST reconciliation** — which is the single fix for ADR-015's three named transfer costs *and* for webhooks dropped past RC's ~155-minute retry budget.

On completion, follow `session-rules.md` §3: append to `past-prompts.md`, regenerate this file with the next single objective (expected: M5.2 — the coach chat UI on the proven spine), refresh `docs/operator-expected.md`, commit, push, verify CI via `gh run watch`, then `codegraph sync`.
